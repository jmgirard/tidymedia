# Commits that changed only formatting or line endings.
# Enable locally (once per clone):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# Note: with that set, git blame is fatal on any ref where this file is
# absent. Skip it for one call with: git blame --no-ignore-revs-file <file>

# Normalize the repo to LF under `* text=auto` (no content change).
921a00f3e7f2ab495e9145e12e579e54b6a0207f
