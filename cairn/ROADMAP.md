# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-08-28 (M082 post-merge: all 16 `cairn_validate` checks PASS, all 7 advisories OK — `release window` did not fire; PR #86 squash-merged green on ten checks. M082 took two review rounds: an amendment return on AC4, whose "N distinct inputs" named no procedure bounding the count, then a clean re-review. Thirteen findings across the two rounds, all logged: F1/F3/F5/F10 fixed, F9/F6 to the two candidate rows added last round, F2/F7/F8/F11-F13 rejected. M077's row dropped at the 5-row terminal retention (its archive file stays). No lesson added: the round's one testing gotcha — a namespace mutation that `testthat::test_file` does not pick up — is the M44 line's M63 extension already. Nothing retired: M082 shipped no guard covering an existing lesson. Byte budgets by hand: ROADMAP.md 41,509 and LESSONS.md 42,232 — both still far over the 24,000/20,000 caps and still open for the user; ROADMAP grew, LESSONS is unchanged. No doctrine modules; two `references/` working artifacts state no budgets of their own.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M083 | The ROADMAP gets back under its byte budget | in-progress | — | normal | milestones/M083-roadmap-byte-budget.md |
| M084 | LESSONS gets back under its budget, and its biggest family graduates | planned | M083 | normal | milestones/M084-lessons-budget-doctrine.md |
| M082 | The track check has an off switch, and says what it costs | done | — | normal | milestones/archive/M082-track-check-opt-out.md |
| M081 | The unchecked flag, and the second copy of the one predicate | done | — | normal | milestones/archive/M081-unchecked-flag-and-second-copy.md |
| M080 | The guard says what is wrong, and refuses NA | done | — | normal | milestones/archive/M080-shipped-guard-findings.md |
| M079 | The floor harness measures what it reports | done | — | normal | milestones/archive/M079-floor-harness-hardening.md |
| M078 | The limit bounds the wait, not the process | done | — | normal | milestones/archive/M078-timeout-bounds-the-wait.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- A second doctrine module for the front-door guard ordering and precedence family (`cairn/LESSONS.md` lines 21, 33 and 41 at bd4d545: a message-only guard silently reassigning error precedence, "last in the front door" not being last overall, and a blame-reading test passing for the wrong reason on a malformed input). M084 declined it because the maturation exit asks for a family extended or consolidated at least twice and this one is at that edge, not past it — M33 extends M41 and absorbs an M45 line, corrected M48, while M41 itself stands alone. Promote on a third extension landing, or on `cairn/LESSONS.md` breaching 20,000 bytes again with this family still whole in it. — added 2026-08-28 — M084 Out; the maturation exit in the plugin's tracking-rules
- **The two shapes M082 left behind when it shipped the check's off switch**, folded here in full from the retired M44 row that stated them (promoted to M082 on 2026-08-28, row deleted by M083; both promotion conditions unchanged). **(a)** A per-verb `check_tracks =` argument, which M082's gate declined on D047's reasoning — a seam commits no exported signature — and which stays available under D014's pre-0.2.0 clean break; promote on a report of a caller needing two different answers inside one script, the case a session-wide switch cannot serve and D051 shipped `with_timeout()` for. **(b)** Probing lazily per row inside the fan-out so the cost parallelizes, which still needs the `ffm_batch()` hook D024/RR02 Q3 rejected and so still needs that entry superseded first; promote on a measured batch where the serial sweep, not the encoding, dominates wall clock even with progress shown. — added 2026-08-28 — M082 review F9; M44 review F4 (retired row, deleted by M083); D014; D024; D047; D051; M52 Out; M53 Out
- **`?tidymedia`'s closing `See vignette(…)` navigation paragraph is swallowed by whatever `\section{}` it happens to follow** — *Bounding a run that hangs* before M082, *Session options* after it — so a reader scanning the page's sections finds the package's own navigation filed under an unrelated heading. Pre-existing on `master`; M082 moved it rather than introducing it, and left it alone because a documentation-layout change is not what that milestone promised. The fix is one roxygen block boundary in `R/tidymedia-package.R`. Promote alongside the next milestone touching that topic's roxygen, or on a report of a reader missing the vignette pointer. — added 2026-08-28 — M082 review F6
- **Deferred review findings on the repo's own test instruments — five grouped rows, entombed verbatim at `cairn/references/instrument-findings.md`**: the flag-guard sweep (M081), the floor-measurement harness under `data-raw/` (M079), the parallel-carry harness (M071), the timeout-silence guards (M70), and the input-guard blame grid (M62/M63/M64/M080). None is a defect in shipped behavior; each row keeps its own finding ids and its own promote-on clause on that page, unchanged. Promote on a bug reaching a user through one of these gaps, or alongside the next milestone touching one of the five instruments. — added 2026-08-28 — `cairn/references/instrument-findings.md`; M081, M079, M071, M70 and M62/M63/M64/M080 reviews; D040; D041; D049; D050; D059
- `with_timeout()` and `local_timeout()` may take minutes, not seconds, to interrupt a program blocked on input on Linux. `run_program()` bounds a spawn with `system2(stdout = TRUE, input = , timeout = limit)` (`R/program_management.R:125`), and on Ubuntu noble / aarch64 under R 4.4.3 that call did not escalate when the child ignored the signal it was sent: an `ffmpeg` blocked opening a named pipe survives `kill -TERM` and dies only on `kill -KILL` (measured directly), one isolated `test-with-timeout.R` run took 191.8 s against a 2 s limit, and six consecutive full-suite runs in that container never returned at all (**corrected M078**: neither reproduced in a rebuild of that runner — the largest overrun measured is 40.41 s and the three files ran 45.61/267.30/52.11 s to completion; recorded as unreproduced rather than disproven, since D055's run had the nine floors pinned and testthat/furrr held back). The same tests pass in about two seconds on macOS 26.5 / arm64 under R 4.6.1, which is why nothing has reported it: CI's five jobs and the developer's machine all sit on the fast side. Reproduce with `docker run rocker/r-ver:4.4.3` + `apt install ffmpeg`, `mkfifo f`, then `system2("ffmpeg", c("-y","-i","f","o.mp4"), stdout = TRUE, timeout = 4)`. What a fix would owe (**restated M078 on the measurement**): base R's SIGKILL at +40 s DOES fire, so the fix is not "escalate harder" — it is that R waits on the child's stdout pipe until it closes, which is what costs the 40 s, and that a spawn not reading a pipe returns at +20 s with the program still alive. So: a mechanism that separates stopping the program from closing the pipe, a regression test that does not itself wedge, and a decision about D047's promise now that the docs no longer claim a bound. M077 excluded the three fixture files from its measurement rather than fixing this, which its AC4 entry discloses. **Measurement and the documentation correction shipped as M078 on 2026-08-28; the FIX remains open here.** M078 rebuilds the container, measures where the excess goes, and stops the docs promising a bound; what it deliberately leaves is the mechanism change, which is not plannable until M078 says whether base R's +40 s SIGKILL fires at all or R is blocked reading a pipe a dead child's descendants still hold. Promote on the first report that the lag is itself the reported problem, or alongside any milestone that revisits the spawn sites. — added 2026-08-28, split 2026-08-28, measured 2026-08-28 — M077 gate 3; D047; D051; D056; M078
- A permanent CI job that installs DESCRIPTION's declared `Imports` floors on every push, promoting M077's one-off `data-raw/imports-floors.R` into a standing check. M077's plan gate chose the one-off script and declined this, because `r-lib/actions/setup-r-dependencies@v2` has no oldest-version input — its `dependencies` input selects dependency TYPES, not versions — so the job must be hand-rolled, and a standing job commits the repo to keeping ten floors green on every push, including whenever a floor version stops building on a newer R. Promote on the first floor regression that reaches a user between audits, or on any milestone that has to re-run the M077 script by hand a second time. — added 2026-08-27 — M077 Out; M077 work log
- **The two gaps D055 discloses and does not close: no floor run alone, and one operating system.** Split out on 2026-08-28 when the carry row that had carried them was retired into M079, so retiring that row does not lose them. D055 item 4, verbatim: "*No floor was run alone, and the run was on one operating system. A joint pass attributes nothing to any single floor; the harness's `--only` mode is the attribution tool and was not needed, because the one failure named its own package. macOS and Windows were not run at all.*" Nine one-floor-at-a-time legs would attribute a failure to its own floor rather than to the pinned set; a three-OS run would say whether the floors hold off Linux. Both were declined at M077's plan gate on cost and both remain declined. M079 keeps `--only` alive precisely so these stay plannable. Promote on a user reporting a failure on a declared floor that the joint run does not reach — D055's own falsifier — or on any floor moving for a reason the joint run cannot attribute. — added 2026-08-28 — D055 item 4; M077 gate; M079 Out
- A tighter kill than base R's `timeout=` gives. Base R escalates SIGINT at the limit, SIGTERM at +20 s and SIGKILL at +40 s, so a program that ignores the first two outlives its limit by up to 40 seconds — measured 2026-08-09 on Linux CI at 42.0-42.1 s under a 2 s limit, which is what forced M69's AC3 from a 10 s bound to 60 s. `?system` states termination "works for typical commands, but is not guaranteed", so the lag is base R's contract and not a wiring fault. M69's return-2 gate rejected `processx` (or equivalent direct process control) because the Goal — a hung program stops the call rather than the session — is met by a bounded 42 s exactly as by a bounded 2 s, and a new dependency plus a rewiring of all four spawn sites was a fresh round of risk at a second return. That row was promised in M69's work log and never written; written here. M078 measured this on 2026-08-28 and the premise **holds**: over 14 cases on two platforms the largest overrun is 40.41 s, every overrunning case landing on limit + 40 s (R reads the child's stdout pipe and waits for SIGKILL) or limit + 20 s (it does not, and the program is still running afterwards — a path no tidymedia call takes, since every spawn site reads the pipe). D055's 191.8 s did not reproduce in a rebuild of the runner D055 itself names, nor did its wedged suite: the three excluded fixture files ran 45.61 s, 267.30 s and 52.11 s to completion and the full suite gave pass=6477 fail=0 skip=22 in 445.9 s. That is recorded as unreproduced rather than disproven — D055's run had the nine floors pinned and testthat/furrr held back — see D056. The rejection is untouched and this row stays open on its original trigger: the limit-plus-40s lag is real, measured, and now documented, and it is still a legitimate thing for a user to complain about. Promote on the first report that the limit-plus-40s lag is itself the reported problem, or alongside any milestone that revisits the spawn sites. — added 2026-08-26, measured 2026-08-28 — M69 return 2; D047; D056; M078
- Fixed-region *region blur* (no face tracking): split→crop→boxblur→overlay needs an IP2 filtergraph design call (new blessed composite verb vs Layer 0) plus a new `ffm_boxblur` filter; not plannable until that call. Box-fill half became M20/M21. Confirmed in-scope (defer) by M25. — added 2026-07-10, split 2026-07-12, reconciled 2026-07-13 — research-verbs family 4; M25 survey §3 D1
- `burn_timecode` / drawtext text-and-timecode burn-in for coders & reliability raters; in-scope but needs a new `ffm_drawtext` Layer-1 filter + a surface-scope call. — added 2026-07-13 — M25 survey §3 D2 (defer)
- Minor in-scope convenience verbs (grouped): split multi-view→per-person clips, orientation fix (rotate/flip), contact-sheet QC montage; each needs a small arg-shape design call. — added 2026-07-13 — M25 survey §3 D3 (defer-low)
- Two `separate_audio_video()` failure-path leftovers M45 measured but left out (grouped) — **the first half promoted to M68 on 2026-08-09**, leaving the second: the video command never runs once the audio one aborts, so a failed separation still writes no video even after M68 clears the zero-byte audio file. It is a behavior change on an error path rather than part of the multi-track fix, and the open call is whether the verb should attempt the video at all once the audio failed — M68 deliberately did not answer it. Promote on the first report of a failed separation leaving no video, or alongside the next milestone touching `separate_audio_video()`'s run path (`R/ffmpeg.R:882-891`). Also unfired: a symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both, and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. When this row is next groomed it should also cover `ffm_concat()` / `concatenate_videos()`, which map all streams unconditionally and so fail the same way D026 measured for `-map 0` on subtitle-bearing `.mkv` inputs into `.mp4` (exit 8, no default mp4 subtitle encoder) — RR03 Beyond-the-brief, deliberately not folded into M48. — added 2026-07-30, extended 2026-07-30 — M45 Out; RR03
- The multi-track separation abort is blind to WHY FFmpeg failed, and detects a non-zero exit by parsing a message rather than reading a field (grouped, both from M45 review): it fires on any failing audio command whose input carries >1 track and named no track, so an unknown `audio_codec`, or a missing output directory while writing `.mka`, still gets "Or keep all 3 by writing ... Matroska (`.mka`)" — advice the caller is already following (F1, scored 65, logged). Fixing it means gating on the output container's stream capacity or classifying FFmpeg's failure, both design calls M45 deliberately did not make. Alongside it, `ffmpeg_exit_status()` regex-scans `ffm_run()`'s formatted message, which also embeds user paths, where `run_program()` already returns a structured `status` attribute that `ffm_run()` discards — attaching it to the condition makes this a field lookup (F5, scored 52, logged). Promote on any report of the abort naming a cause that is not the cause. — added 2026-07-30 — M45 review F1/F5; D024; M45-D1
- **Per-call `timeout =` arguments** on the ~60 run-capable verbs and on `ffm_run()`/`ffm_batch()`, so one call can carry a different limit from the next inside a single script — M69's plan gate chose the `tidymedia.timeout` option seam precisely because it commits no exported signature, leaving this available under D014's pre-0.2.0 clean break rather than foreclosing it. The seam's grain is the session, which is the wrong grain for a script wanting different limits per call — D047 names that as this half's own falsifier. Promote on the first report that a whole-session limit is the wrong grain. **The other half of this row — parallel workers not seeing the parent's option, and M70's batch warning being structurally unreachable there — was promoted to M071 on 2026-08-26**, which also takes the symmetric `tidymedia.nvenc_encoders` divergence and supersedes D044's rejection of seeding workers. **The grain problem itself was taken by M072 on 2026-08-27**, which exports `with_timeout(expr, seconds)` — the option established for one call's dynamic extent — reaching all 53 members of `tm_timeout_domain()` where an argument reaches 32, since the 22 exports with no `run =` (`ffm_run()`, the Layer 0 hatches, the probe and MediaInfo readers, `verify_media()`) have no channel for one. So what remains on THIS row is the argument SHAPE alone, and D047's rejection of it still stands unsuperseded: M072 discharges only D047's session-grain falsifier clause, not its "sixty-odd new arguments is the largest irreversible-API commitment the package could make" verdict. Promote on a report that wrapping an expression is the wrong ergonomics — the shape's own falsifier — or on a caller needing a limit that varies per row within one batch, which no wrapper can give. — added 2026-08-09, extended 2026-08-26, split 2026-08-26, narrowed 2026-08-27 — M69 Scope Out; M46 Out; D014; D044; D047; M071; M072
- `find_ffmpeg()` is not memoized either, so one `Sys.which()` — a `system("which ffmpeg")` shell spawn — survives per row after M67 collapses the `-encoders` probe. Split off M67's Out on 2026-08-08: M67's plan gate weighed keying its memo on the resolved binary path and rejected it precisely because the key would recompute this on every build, and because `find_program()` prefers `Sys.which()` over the `set_program()` config (`R/program_management.R:23-27`) so the key cannot see a `set_program()` change while ffmpeg is on `PATH`. Memoizing `find_ffmpeg()` is a wider blast radius than nvenc — it is reached by `ffmpeg()`, `ffprobe()` and every execution path — and it interacts with `find_program()`'s missing-binary `cli_warn()`, so it wants its own D-entry. Unmeasured: nobody has timed `which` against FFmpeg startup on a large batch, and that measurement is the first task. Promote on a measured batch stall attributable to `Sys.which()` rather than to the encoder probe, or alongside the next milestone touching `R/program_management.R`. — added 2026-08-08 — M67 Out; M54 Out; D034
- M67's memo is per-process, so it is invisible to `parallel = TRUE` workers: `furrr::future_pmap()` (`R/ffm_batch.R:102`) runs `.f` in processes that `loadNamespace()` fresh, and `future` exports the closure's globals rather than the package's internal environment bindings, so a W-worker nvenc batch probes W times where the sequential one probes once (a forking `multicore` plan instead inherits a copy-on-write snapshot, so reads hit and writes stay child-local). Split off M67's Out on 2026-08-08 and disclosed in its D-entry rather than fixed: seeding workers from the parent means the package *writing* `tidymedia.nvenc_encoders`, and that option is read-only from the package's side today (`R/ffmpeg.R:2521` is the only `getOption()` in `R/`; nothing in `R/` calls `options()`), so it would change what the seam means and needs its own design call. W is bounded by the worker count, not the row count, so the magnitude is small — this is a completeness gap, not a stall. Promote on a measured parallel batch whose probe count is itself the reported problem, or on any request to share capability state across workers. **Narrowed by M071 (2026-08-27):** the caller's `tidymedia.nvenc_encoders` override now reaches workers, so what remains here is the remembered probe ANSWER, which does not; the read-only premise above is superseded by D050 (the package re-establishes the caller's value, never one of its own). — added 2026-08-08 — M67 Out; M53; D033
- Two `normalize_audio()` extensions the linear builder blocks (grouped; same blocker, IP2/D003 — one filter chain and one output). **(a) Per-track two-pass loudness normalization:** one measured set per mapped audio track, so the verb could normalize every track rather than one. This is the falsifier D028 names for its first-track `NULL` — an every-track map today makes the analysis pass print one JSON block per mapped track while `classify_loudnorm_output()` reads `hit[[1]]`, silently correcting every track with track 0's measurements (measured 2026-07-31 on a 3-track `.mkv`: 3 blocks under `0:a?`, 1 under `0:a:0?`). The blocker is not the parser but the builder: attaching one measured set to each mapped track needs per-stream filter options. Promote on the first request to normalize more than one track of a file, or alongside any milestone that gives the builder a per-stream filter slot. **(b) Normalize loudness while KEEPING the picture, in one call:** M49/D030 removed this from `normalize_audio()`, whose output is now one audio stream and no video; the replacement today is normalizing to an audio file and muxing it back through the `ffmpeg()` escape hatch, which the package otherwise never asks a research user to reach for. Shape is undecided and is the work: a `keep_video =` argument reopens exactly the output-container question D030 closed (the muxer must accept both streams), so the likelier shape is a separate mux verb, or a two-output pipeline the builder has no slot for. Note the same latent break on the pass-through verbs: `format_for_web()` into an audio container fails at exit 234 for the identical reason, measured 2026-07-31 and deliberately not fixed in M49 since its product is a web video file. Promote on the first report of the escape hatch being needed for this, or alongside any milestone that gives the builder a second output. — added 2026-07-31, grouped 2026-08-06 — M49; M49 Out; D028; D029 (superseded); D030; D003
- The compiled command's remaining bare token classes: codec names, pixel formats, seek values, and the raw output-option passthrough (`-movflags +faststart`, `-q:a 0`, `-f null`). D031 records these as deliberately unquoted — the first three are single clean tokens by their own validators, so quoting buys nothing today, and the passthrough cannot express it at all: the `output_opts` branch of `ffm_groups()` hands `ffm_group()` a finished `display` string (`R/ffm.R:1332`), bypassing the `quote=` mechanism, so covering it needs a signature change rather than another `quote = 2L`. M50 deliberately scoped itself to map specifiers, the one class a metacharacter had actually reached. Promote on the first report of any of these breaking a pasted command — a codec token or a seek value carrying a shell metacharacter is the shape to watch. — added 2026-07-31 — M50 Out; D031
- Renaming `audio_stream` or `audio`, or unifying the two `NULL` readings. M51 answered the confusion with documentation instead — one `?audio_stream` topic plus generated `@param` text — and D032 re-confirms the two-name call at eighteen verbs. Under D014's pre-0.2.0 clean break a rename is still available and gets its own milestone. Promote on the first report of a caller confused by the two names or by the two `NULL` readings, which is D025's and D026's own falsifier; never on the argument count alone, since eighteen verbs is not eighteen confused callers. — added 2026-07-31 — M51 Out; D014; D023; D025; D026; D032
- `probe_all()`'s sequential (default) path now materializes every file's probe before assembling, where the pre-M53 `for` loop let each file's raw result become garbage immediately. M53 replaced the loop body's inline `probe_one(f)` with a `purrr::map(infile, probe_one)` whose result list is held for the whole call, alongside the assembled `containers`/`streams_l` lists — so peak memory on a large corpus rises at `parallel = FALSE`, for no benefit, since only the parallel branch needs the materialized form. The magnitude is **unmeasured**: the review finding said "roughly doubling" and the scorer flagged that number as speculative, so the work starts with measuring peak memory over a few thousand many-stream files at `parallel = FALSE` on master versus post-M53, and only then decides whether to restore streaming on the sequential branch (keeping the map for the parallel one). Promote on a measurement showing a real regression, or on any report of `probe_all()` exhausting memory on a corpus that used to fit. — added 2026-08-06 — M53 review F7 (scored 74, logged); `R/ffprobe.R:92-117`
- A wrongly-**typed** value still answers differently depending on its form, where M61 made a wrongly-**valued** one uniform: `picture_in_picture_batch(jobs, margin = "x", audio_codec = "aac")` reports the contradiction, while the same `margin` carried in a `jobs` column reports the type error (measured 2026-08-08 on M61's branch). This is a disclosed consequence of an intentional boundary rather than a defect — M61's Scope Out keeps every column *type* guard above the contradiction sweep, because the row-sweep reads the table's shape before it can read a value, and the sentence M61 pins promises uniformity for a value error and not a type error. The work is the design call that boundary defers: whether a column's type check can be split from its shape check so the value half can move below the sweep, and what that costs at every `_batch` verb rather than these two. Promote on any report of a type mistake and a contradiction reporting differently by form, or alongside the next milestone touching the column type guards. — added 2026-08-08 — M61 review round 3 H11 (scored 40, logged); M61 Scope Out; D036; D039
- The normalize pair's copy/audio_stream form divergence (this row's part (a), the batch row-locator, promoted to M66 on 2026-08-08): `audio_codec = "copy"` and a fractional `audio_stream` report before a bad loudness target on the batch form and after it on the scalar (M65-D3's disclosure); uniformity means moving the batch's early copy/stream guards below its input sweep, reopening M34/M45's fail-before-Phase-1 placement. Promote on the first report of the divergence confusing a caller. — added 2026-08-08, regroomed 2026-08-08 — M65 review F1; M65-D3; D039; D043; M66
- Video quality / rate-control knob (CRF↔CQ, `-preset p1–p7`, bitrate) — the package has no quality abstraction today; a cross-encoder mapping is opinionated + an irreversible-API commitment. Deferred from M31. — added 2026-07-26 — M31 Q4
- GPU *decode* / `-hwaccel cuda` input acceleration + GPU filter pipelines — needs a new engine input-options slot (none exists; only `seek_pre` goes before `-i`) + an IP2 filtergraph design call. — added 2026-07-26 — M31 Out
- Other hardware encode backends (videotoolbox/qsv/vaapi/amf) generalizing the `hardware=` arg beyond nvenc — needs a backend-detection + arg-vocabulary design call. — added 2026-07-26 — M31 Out
- CRAN readiness (release mechanics only): win-builder + R-hub, cran-comments, bump toward 0.2.0. Deliberately last. API-surface cleanup shipped as M23; the vignette pass is M30; a roxygen `@examples` pass remains the only open docs slice under this row. — added 2026-07-10, trimmed 2026-07-12, reconciled 2026-07-12 (M30) — see M22/M23/M30
