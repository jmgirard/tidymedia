# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-09-09 (M119 done and archived; PR #123 squash-merged, all ten CI legs green. No defect and no amendment returns — every one of the seven criteria passed first time, three of them against a discriminating control (an injected 404 for the URL check, a deleted `CITATION` for the citation, a deleted pkgdown index row for the reference index). The seven acceptance boxes arrived pre-ticked with no Review evidence; unticked at review entry and re-ticked one at a time under AC fencing. Three review lenses, 13 findings, all from the [O] lens: five fixed on the branch, every one a claim the branch had itself added to `NEWS.md` or the get-started vignette (the citation's ORCID, which `print.bibentry` never renders; a release note for a leak users never saw; "every task verb", which `separate_audio_video()` falsifies; a wrong cause for a silent chunk; an orphaned `@return`) — the derived-claims rule catching what the criteria could not. Three deferred and four rejected. The docs-gaps row's absorbing disposition went to the user, who chose a bounded milestone: the surviving `find_program()` cross-references become a new **[high]** candidate row, the README temp path became that row's (m). M116's row pruned to hold the three-terminal-row cap, paying for the new candidate line. LESSONS 38 lines, 19,995/20,000 — M119's lesson added and paid for by trimming four lines (M46's covered half, M088, M113, M118), not by pruning one, so no lesson was lost. `false-greens.md` 25,996/26,000 and `guard-ordering.md` 5,451/11,000, both unchanged and under. **`ROADMAP.md` is 35,401 bytes against its 24,000-byte budget** at 59 of 60 lines, worse again than M118's 34,221; `/cairn-triage` is overdue by four passes and remains the only remedy. `cairn_validate` clean.)_ _Plan pass 2026-09-07 (M117-M122, the pre-CRAN set): six milestone rows added at 60 lines' hard cap, paid for by folding five new candidates into the rows they belong to and merging four pairs of same-mechanism rows — the two `find_program()` remembered-location paths, the separation-cause pair, the `install_on_win()` internals pair, and the M25 verb families into the bundled-tasks row. Nothing was dropped; every merge preserved its content and citations. The check-surface row's (b) graduated to M122 and its (a) was remeasured rather than guessed. **`ROADMAP.md` is now 32,050 bytes against its 24,000-byte budget** at 59 of 60 lines — the line cap is the binding one now, and the graduate-or-prune remedy (`/cairn-triage`) is overdue. `cairn_validate` clean.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M118 | The binary-executing tests skip on CRAN's own check | done | — | normal | milestones/archive/M118-cran-skips-binary-tests.md |
| M119 | The documentation defects the pre-CRAN audit measured are corrected | done | — | normal | milestones/archive/M119-shipped-docs-defects.md |
| M120 | NEWS.md reads as release notes | in-progress | — | normal | milestones/M120-news-reads-as-release-notes.md |
| M121 | A directory becomes a jobs tibble | planned | — | normal | milestones/M121-directory-to-jobs-tibble.md |
| M122 | macOS and Windows run the package's FFmpeg code | planned | M118 | normal | milestones/M122-mac-windows-run-ffmpeg.md |
| M117 | A test run leaves no location behind in the user's real config directories | done | — | normal | milestones/archive/M117-test-run-config-leak.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- [high] **`find_program()` is still recommended by the help pages it was just taken off.** M119 dropped it from `\usage{}` and the pkgdown index, but eleven cross-references survive in shipped prose — `program_status.Rd:22,51,56`, `set_program.Rd:41,64,68`, `unset_program.Rd:22,32,42,52`, `install_on_win.Rd:132` — and `program_status.Rd:51` reads "`find_program()` for one program at a time", so a reader who follows it gets `could not find function`. The unreleased `NEWS.md` section has the same split, explaining `unset_program()` in terms of `find_program()` while a later entry calls it internal. Pre-existing at `origin/master`, outside M119's AC3, and dispositioned at M119's hygiene gate as a bounded milestone to cut. — added 2026-09-09 — M119 review [O]1, [O]7
- **Seven gaps around the CRAN skip and the instrument that measured it.** **(a)** Nothing fails if `r-lib/actions/setup-r@v2` stops exporting `NOT_CRAN`: both workflows go green with the execution coverage silently gone. **(b)** `test-cran-skip-helpers.R`'s `NOT_CRAN=true` control covers only the three name helpers, so either hardware-probe helper reduced to a bare `skip_on_cran()` passes every block — and the naive fix falsifies AC2, since calling either under `NOT_CRAN=true` runs its probe encode, so closing it needs a mock. **(c)** Two hand-rolled guards of the class M118 patched survive (`test-program-status-and-unset.R:33`, `test-nvenc-memo.R:92`), each safe today only by a neighbour, and `ffplay` is shimmed by nothing. **(d)** `@examplesIf` examples and the vignette chunks still spawn on a CRAN machine that has the binary — D090 records this as deliberate. **(e)-(g)** `tools/cran_spawn_check.R` hygiene: `--lib=DIR` has no freshness guard; `TIDYMEDIA_SPAWN_LOG` is set and never read, `R_LIBS` gains a trailing empty entry, the working directory is undocumented; `--mode=emptypath` exits 0 however the suite died. Promote (a) on CI dropping the variable, (b)-(c) on a spawn the instrument catches, (d) on a CRAN complaint, (e)-(g) on the next milestone running the script. — added 2026-09-09 — M118 review V7, V9, V10-V15; D090
- **Two remembered-location paths `find_program()` still reads wrong.** **(a)** A location that exists but is not executable is reported as gone: `Sys.which()` answers `""` for a missing file and a present non-executable one alike, so `tidymedia_location_gone` says the binary "no longer seems to exist" when the fault is the permission bit; fold into a `file.access()` check when one is added. **(b)** A config path that is a *directory* is still fatal — it never reaches the guard M116 added, because `readLines()` dies first with `cannot open the connection`, stopping `find_ffmpeg()` and `program_status()` both (measured 2026-09-06); outside M116's AC1, which is scoped to what `readLines()` returns (cairn plugin decision D-118, not a repo `D0xx` entry). Promote each on a report of the wrong diagnosis or of a call stopping. — merged 2026-09-07 — M116 review [O]11; M116 re-review [O]5
- **Three gaps in how the timeout sweeps cover a conditionally-spawning member.** `program_status()` is the first — it probes only a program it resolved, so a runner with no binaries spawns nothing. **(a)** A guarded `tm_timeout_post_baseline()` excludes it from the byte-for-byte spawn-trace baseline and no present-day reading pins its trace. **(b)** It calls `find_program()` generically rather than through `tm_find_program_for_version()`'s `switch`, walking past the `find_*()` mocks that comment exists to respect — why T7 had to add a `find_program` mock. **(c)** It joins the absorber partition with no explanatory paragraph. M116 rewords that warning without touching the sweeps' domain. Promote on a second member whose spawning is conditional. — added 2026-09-06 — M113 review 6, 8, 13; M094; M096; M116 Out
- **`tools/config_leak_check.R` watches the wrong pair of directories on a machine that sets the config path in `~/.Renviron`.** It computes the two watched paths in a `--vanilla` subprocess, which reads neither `~/.Renviron` nor `Renviron.site`, while the run it measures reads both — so where `R_USER_CONFIG_DIR` or `XDG_CONFIG_HOME` is set there, a real leak reports "no difference". Detectable rather than silent, since the two positive controls resolve their target inside the measured run and would fail loudly on such a machine, but nothing tells anyone to re-run them there. Not fixed at M117's gate: the repair changes what "the watched directories" means, a design call rather than a defect in the instrument. Promote on a run of the harness on any machine or CI image setting either variable outside the process environment. — added 2026-09-08 — M117 review [O]5
- **`tools/pkgdown_duplicate_topics.R` silently drops any `contents:` entry containing a parenthesis**, so a duplicate between a selector's expansion and a literal entry would be invisible. Latent: `_pkgdown.yml` has no parenthesised entry today. Promote on the first selector-form entry added. — added 2026-09-05 — M111 review [O]2
- **Four bundled tasks with no single-knob verb, and one with no verb at all.** Trimming to a range runs through `segment_video()`'s plural-output shape; transcoding, resizing and frame-rate changes run only through `standardize_video()`, which also forces libx264 and yuv420p; muting or replacing an audio track has no Layer 2 route, only `ffm_drop()`/`ffm_map()`. Declined at M111-M115's plan gate: GP1 prefers refusing a feature to growing toward coverage, and each unbundled verb needs a `_batch` sibling under D014 — six exports for the trio alone. The replace-audio half reopens the mux question. **The M25 survey's three deferred families sit here too**, each blocked on its own design call: fixed-region *region blur* (split→crop→boxblur→overlay, needing an IP2 filtergraph call plus `ffm_boxblur`; the box-fill half became M20/M21); `burn_timecode` / drawtext burn-in (needs `ffm_drawtext` plus a surface-scope call); and minor convenience verbs — split multi-view→per-person clips, orientation fix, contact-sheet montage. **And `ffm_batch()` has no resume or skip-existing**: it has progress (`R/ffm_batch.R:39-41`, `:240`) and dry-run (`:71`) but a 500-file run that dies at 300 restarts from zero (`grep -rn "resume\|skip_existing" R/` finds only `overwrite` at `R/ffm.R:11`); declined at M117-M122's gate as outside the pre-release set. Promote each on the first request naming it. — merged 2026-09-07 — M113/M114 gate; M25 survey §3 D1/D2/D3; M121 Out; GP1; D001
- **Four `unset_*()` wrappers mirroring the `set_*()` exports.** M113 ships one generic `unset_program()`; the wrappers are additive later and four exports is the larger trade against GP1. Promote on a report that the asymmetry with `set_ffmpeg()` is what a caller trips on. — added 2026-09-05 — M113 Out
- **Ten gaps in the shipped docs — eight in their guards and instruments, two in their content (corrected M119).** **(a)** `vignette_chunk_guards.R`'s pass-2 child clears `PATH` but not the config seam its sibling clears, so a guard written on `find_ffmpeg()` reads TRUE there. **(b)** Both `tools/` scripts drop `/usr/bin` wholesale when a program lives there — the normal Linux/CI layout — losing the system tools; shim the three out instead. **(c)** `.gitignore`'s `vignettes/*.R` and `*.mp4` are broader than the artifacts they target and would swallow a future committed helper or fixture (none today). **(d)** The vignettes and `README.Rmd:21-23` guard on `Sys.which()` where the package resolves through `find_program()`, so a reader or contributor who ran `set_ffmpeg()` sees empty chunks though every call would work (`metadata.Rmd:18-19` set the precedent). **(e)** `?tidymedia`'s warn-path paragraph lists `probe_all()` and the `get_*()` helpers above a condition-class sentence naming only the two sites that carry a class. **(f)** The chunk sweep decides "spawning" by what happened on the dev machine and knits with `error = TRUE` where the real build uses `error = FALSE`. **(g)** Four builder verbs are missing from `tidymedia.Rmd:111-176`'s Layer 1 tour (`ffm_fps`, `ffm_drawbox`, `ffm_loudnorm`, `ffm_output_options`). **(h)** The capability family (`ffmpeg_codecs`, `ffmpeg_encoders`, `hardware_encoder`, `refresh_ffmpeg_capabilities`) appears nowhere but `README.Rmd:132`; only `has_hardware_encoder()` is taught, at `workflow.Rmd:83`. **(j)** `covered_programs()` expands `has_ffmpeg` to `ffmpeg` then greps, so `eval = !has_ffmpeg` would read as covered (no chunk is written that way), and the no-binaries build's artifact scan greps `^#> ` — a prefix the swept file's own `comment` option sets, not the scan. **(k)** The Windows `install_on_win()` offer in `find_program()`'s warning fires for `ffplay`, which `tm_install_registers` lists as a candidate: the installer's `absent_optional` path exists because an archive can complete without it. **(l)** `README.Rmd:167-217` shows no batch example at all — `grep '_batch\|list.files' README.Rmd` returns nothing, though batch is the stated purpose and 15 of 89 exports; declined at M117-M122's gate for the pre-release set, and M121's new export is what the example would use. **(m)** `README.md:193,201` embed an absolute session temp path (`/private/var/folders/px/.../temp_libpath.../`) in knitted example output, so `build_readme()` churns those two lines on every run and a local path ships; pre-existing at `origin/master`, routed here at M119's hygiene gate. Promote (a)-(f) and (j)-(m) on a CI image, caller or reader they bite, the rest at the 0.2.0 docs pass. — merged 2026-09-07 — M114 review [O]4, 7, 10, 12 and pass-1 5, 14; M114 Out/T3; M115 review O3, O4, O6, O7
- **The check-surface gap is now measured, and the residual is the pure-R suite, not the binaries.** The 2026-09-05 row guessed a tooled CRAN machine running the full execution suite was the realistic path to a "checking tests ... exceeds 10 min" NOTE. Measured 2026-09-07 on an Apple Silicon Mac: `R CMD check --as-cran` 7m47s with the three binaries present, tests step `[368s/436s]`, against a 5m06s `devtools::test()` with them hidden — so ffmpeg execution looked like about one minute of six. That estimate was low (corrected M118): the measured saving is 7m47s to 4m33.2s, tests step `[368s/436s]` to `[223s/230s]`, because skipping the execution tests also drops their fixture building and their waiting. Cutting further means consolidating among 1,568 `test_that()` bodies across 34,763 lines, which risks real coverage; declined at M117-M122's gate. The macOS/Windows half graduated to **M122**. Promote on a CRAN check-time NOTE that survives M118. — remeasured 2026-09-07 — pre-CRAN audit; M118 Out; M69; M105; M108
- **A downloader for macOS or Linux, and the dispatcher that would front it.** tidymedia installs FFmpeg on Windows only (M108 makes that a refusal, not an assumption); macOS and Linux get `brew`/`apt-get` lines from the README. Declined at M108's plan gate under GP1: each downloader needs its own source, digest format (evermeet.cx ships ffmpeg and ffprobe separately; johnvansickle.com ships per-arch tarballs with `.md5`, not the SHA-256 D081 bought) and arch matrix, plus the M102-M105 hardening arc repeated. Absorbs a dispatching `install_ffmpeg()`. Promote on a report from a user the package-manager route does not serve. — compressed 2026-09-05 — M108 Out; D081; D001
- **Two of `tm_forbid_spending()`'s four spending stubs can never fire**, so M108's AC1 claim that the gate is proven above all four calls is nominal for half: `dir.create()` and `tm_fetch()` sit strictly below `tm_confirm()` (`R/program_management.R:1043`, `:1063`, `:1075`), whose stub aborts first. The gate's position above the first write and first fetch follows from source order, not an assertion. Closing it means making the earlier stubs pass through. Promote alongside any milestone moving a spending call in `install_on_win()`. **Three produced-set edges sit beside it:** a libarchive directory entry counts as produced (`tm_files_on_disk()`, `:679`), so a fully-quarantined extraction still reports a non-empty `on_disk` when `bin/` is listed while excluding directories breaks the `dir` spoil form (unmeasured); the `unpacked_here` site (`:1117`) is unobservable; and an empty-string entry counts as on disk — promote those on a refusal naming a directory that holds nothing. **And `install_on_win()` is still never run against a real archive on CI** even after M122 puts FFmpeg on the Windows leg; doing so spends bandwidth on every push and touches D080's consent gate and D086's platform gate, so it needs its own decision — promote on an installer defect a green fleet missed. — merged 2026-09-07 — M108 review P2; M105 review F11, F4, F10; M122 Out; D080; D084; D086
- **The install's registration check is static; it never runs the binary it registers.** M104 asks that the produced path resolve under `Sys.which()` and that the file is not empty, which passes a wrong-architecture or corrupt executable of nonzero size. Running each unpacked program with `-version` would settle it, but it is the first probe in this seam to EXECUTE a downloaded binary, so it needs its own D024 call, and it turns a slow or blocked spawn into an install failure. Declined at M104's gate for both reasons. Promote on a report of an install registering a binary that cannot run. — added 2026-09-03 — M104 Out; D024; D001
- **Unpack into a scratch directory and move the result into place**, so a failed install never writes into the install directory. M103 measured why this is the only route that works on Windows: the handle libarchive writes a failed entry through is leaked for the process lifetime, so the file cannot be deleted there, and neither `gc()` nor a pause frees it (`windows-latest`, 2026-09-02). Declined at M103's gate because it replaces the mechanism that milestone was planned around and costs a full copy across volumes. Promote on a Windows caller harmed by what a refused install leaves. — compressed 2026-09-05 — M103; D082; D046
- **Two provenance limits in the installer's digest path.** **(a)** The check cannot detect a SUBSTITUTED archive: `<url>.sha256` is fetched from the same host over the same TLS session, so it catches corruption, not a compromised source; a signature the source does not publish or a pinned digest would, and a pin goes stale every FFmpeg release. **(b)** `tm_parse_sidecar()` takes the first digest on any line — right for gyan.dev's single-digest sidecar, wrong for a multi-file `sha256sum` manifest, and matching on the filename field is guesswork about a format nothing in play emits. Promote (a) on the source publishing a signature or a substituted archive passing; (b) on a source publishing a multi-file manifest. — merged 2026-09-06 — M102 Out; M102 review pass 1 [O]12; M103; D081; D001
- **Two argument classes still refused below the verb**, from M096's census (437 of 1530 cells at HEAD). **(a) Gate booleans** `run`, `parallel`, `progress`, `manifest`, `checksums`, `verify` at the ~18 verbs reaching `ffm_finish()`/`ffm_batch()`: `run = 123` is blamed on `ffm_finish()`, and fixing it reopens D074 property 2 at every one. **(b) `ffm_batch()`'s `output` column**, blamed on `purrr::pmap`: its contract names no column, so a guard invents one and changes the per-row shape D007 fixes. Others are ACCEPTED in DESIGN Known issues. Promote each on a mistake blamed below the verb. — compressed 2026-09-02 — M096; D007; D074
- **`tm_outfiles_baseline()` records a merge-base table with no committed generator.** Its four constants in `tests/testthat/helper-timeout-sweep.R` carry provenance in a comment only, and every cell is a value M096's guard passes, so nothing distinguishes a table recorded at `4063faa` from one at HEAD. M096's review closed the EVIDENCE gap by re-measuring against a worktree at that commit. Promote alongside any milestone touching `segment_video()`'s compiled output or re-recording that table. — compressed 2026-08-31 — M096 review F7; M095 AC2
- **The build-time hardware probe's defaultless `call` cannot be asserted by a test.** M094 removed the default so a site forgetting the verb's frame cannot inherit the helper's own (D074); the helper is `hardware_encoder_available()` since M099/D077 (`R/ffmpeg.R:3181`). Measured 2026-08-31: R never forces the promise unless an abort is built, so a missing `call` returns silently either way — a static sweep, not a runtime assertion. Promote alongside any milestone adding a call site. — compressed 2026-09-03 — M094 review H8; D074; D042; D077
- **The SIGINT leftover from M088's failure-path findings.** A SIGINT reaching only the child could surface as a non-zero exit and spawn a second run instead of returning to the prompt — low confidence, never reproduced. M090's gate left it here because an unreproduced Ctrl-C interaction has no bounded investigation, and M078 is the precedent for what an unbounded one costs. Promote on the first report of Ctrl-C failing to return to the prompt. — compressed 2026-08-31 — M088 review pass 2 F3; D065; M078
- **`references/false-greens.md` sits 4 bytes under its own budget** (25,996/26,000, 55 of 60 lines; M112's entry paid for by compressing three neighbours); the module header's remedy is compress-or-retire, never growth. Promote on the module breaching either figure with nothing left worth compressing or retiring. — added 2026-08-29, remeasured 2026-09-05 — M089 review F2; M084; D067; M112
- **The shape M082 left behind when it shipped the check's off switch:** probing lazily per row inside the fan-out so the cost parallelizes, which needs the `ffm_batch()` hook D024/RR02 Q3 rejected, so that entry must be superseded first. (The per-verb `check_tracks =` half was declined for good at M099; D077/D078 carry it and its post-0.2.0 `jobs`-column reopening clause.) Promote on a batch where the serial sweep dominates wall clock. — decided 2026-09-01, compressed 2026-09-03 — M082 review F9; D024; D047; D077
- **A `probe_all_impl()` argument refusal would name `infile`** through `verify_media()`, which has no such argument — a shipped message naming an argument its frame lacks. Unreachable today: `check_file_exists()` refuses first, which is why M70 logged it. M70's O11, classed **runtime** by M092's ledger. Promote on any change letting `verify_media()` refuse before the file-existence check. — added 2026-08-30 — M70 review O11; M092 ledger
- **The condition-class pairing test binds a claim to a help TOPIC, not to a SITE**, which is why the same over-attribution shipped green twice — `?tidymedia` and `?normalize_audio_batch` each naming `tidymedia_ffmpeg_exit` for `R/loudnorm_two_pass.R:112`. Strengthening it over the union of a topic's paired sites catches neither, since both also pair with `:151`, which does raise that class. M092's ledger prunes the finding as **instrument** and keeps the call plannable here. Promote on a third topic over-attributing a class. — compressed 2026-08-31 — M087 review p1 F5, p2 F1/F2; M092 ledger
- A permanent CI job that installs DESCRIPTION's declared `Imports` floors on every push, promoting M077's one-off `data-raw/imports-floors.R` into a standing check. M077's plan gate chose the one-off and declined this, because `setup-r-dependencies@v2` has no oldest-version input — its `dependencies` input selects dependency TYPES, not versions — so the job must be hand-rolled, and it commits the repo to keeping ten floors green on every push. Promote on the first floor regression that reaches a user between audits. **The one-off has itself gone stale:** `data-raw/imports-floors.R` and `r-floor.R` last changed at `c08ba293` (2026-08-28) while `DESCRIPTION` has changed four times since (`a666800`, `f38f059`, `40143f9`, `fd39838`) and M110-M116 added exported surface the last measured run never exercised, so D055's "each floor says what was measured" is stale on its face and nothing in the repo records a run — re-run it at the release walk. — extended 2026-09-07 — M077 Out; D055; M079
- **The two gaps D055 discloses and does not close: no floor run alone, and one operating system.** Nine one-floor-at-a-time legs would attribute a failure to its own floor rather than to the pinned set, and a three-OS run would say whether the floors hold off Linux. Both declined at M077's plan gate on cost; M079 keeps the harness's `--only` mode alive so they stay plannable. Promote on a failure on a declared floor the joint run does not reach — D055's own falsifier. — compressed 2026-08-31 — D055 item 4; M077 gate; M079 Out
- A tighter kill than base R's `timeout=` gives. Base R escalates SIGINT at the limit, SIGTERM at +20 s, SIGKILL at +40 s, so a program ignoring the first two outlives its limit by up to 40 s — measured 42.0-42.1 s on Linux CI under a 2 s limit, which forced M69's AC3 to a 60 s bound; `?system` calls termination not guaranteed, so the lag is base R's contract. M69's return-2 gate rejected `processx`: a bounded 42 s meets the Goal as a bounded 2 s does, against a new dependency and four rewired spawn sites. Absorbs the `with_timeout()` interrupt lag (`R/program_management.R:125`), whose FIX stays open. Promote on the first report that the lag is itself the problem. — compressed 2026-09-05 — M69 return 2; D047; D056; M078; D055
- **Route the two direct `resolve_hw_encoder()` sites through the codec seam** — `format_for_web_pipeline()` (`R/ffmpeg.R:1541`, literal codec) and `anonymize_pipeline()` (`:2126`, checked above). Declined at M106's gate: rerouting moves the emission point against the surrounding filters, risking the compiled bytes. Promote on a third direct site whose token is neither. — added 2026-09-03 — M106 Out; M095
- Two leftovers from the same M45 measurement, now that its video-after-failure half is **M088**. **(a)** A symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. **(b)** `ffm_concat()` / `concatenate_videos()`, which map all streams unconditionally and fail the way D026 measured for `-map 0`; promote alongside any milestone touching either verb's map. — added 2026-07-30, narrowed 2026-08-29 — M45 Out; D026; RR03; M088
- **The multi-track separation abort is still blind to WHY FFmpeg failed**, for every cause the output container cannot reveal. **The container half shipped as M091**; D069/D071 carry the rest. What stays: **(a)** classifying the failure from FFmpeg's stderr, which `ffm_run()` does not capture (`R/program_management.R:105`) and capturing would stop live console output; **(b)** a differential re-run with `-map 0:a:0` into a temp path — decisive, but the first probe here to execute FFmpeg and write a file, so it needs a D-entry. Promote on the abort naming a wrong cause. **(c)** The batch form cannot carry a status at all: `ffm_batch()`'s `run_one()` reduces each row to `list(success =, timed_out =)` and discards the condition (`R/ffm_batch.R:143-147`), the warning fires for any cause (which D062 forbids classing as an exit), and `tryCatch()` on a warning unwinds the batch against D007 — the fix changes the per-row result contract. Promote (c) on a batch caller needing a failed row's status. — merged 2026-09-07 — M45 review F1; M091; M086 Out; D007; D024; D062; D069; D071
- **The package's remaining unclassed aborts stay uncatchable by class** — 13 of 173 `cli_abort()` sites carry a `tidymedia_*` class (2026-08-29). M086 declined the sweep twice: no grep enumerates the domain (a non-zero status surfaces at every `run_program()` caller, not only where "ffmpeg" appears), and a multi-cause site cannot honestly carry `tidymedia_ffmpeg_exit` under D062's event rule — so this needs a naming pass, not one class. M110 classed `set_program()`'s own abort `tidymedia_program_not_found` with `tm_program`/`tm_location`; whether that name fits `find_program()`'s two not-found warnings is the naming pass's call. M116 classes `find_program()`'s two remembered-location warnings and leaves its plain not-found warning to this pass. Promote on a caller needing an abort M085/M086 left unclassed. — compressed 2026-09-06 — M086 Out; M087; D062; M104 Out; M100; M116 Out
- **Two lookup-memoization gaps M67 left open (one memo, two axes).** **(a) `find_ffmpeg()` is not memoized**, so one `Sys.which()` spawn survives per row; M67's gate rejected keying the memo on the resolved path, since `find_program()` prefers `Sys.which()` and cannot see a config change while ffmpeg is on `PATH`. **(b) M67's memo is per-process**, invisible to `parallel = TRUE` workers: a W-worker batch probes W times; D044 carries mechanism and magnitude. Promote each on a batch whose probe count is the problem. — compressed 2026-09-02 — M67 Out; D033; D044; M071
- Two `normalize_audio()` extensions the linear builder blocks (same blocker, IP2/D003 — one filter chain and one output). **(a) Per-track two-pass loudness normalization**, one measured set per mapped audio track: D028 is its own falsifier and carries the 3-track `.mkv` measurement; the blocker is the builder, not the parser. **(b) Normalize loudness while KEEPING the picture, in one call**, which M49/D030 removed; `keep_video =` reopens the container question D030 closed, so the likelier shape is a mux verb or two-output pipeline. Promote each on the first request naming it. — compressed 2026-09-02 — M49 Out; D003; D028; D030
- The compiled command's remaining bare token classes: codec names, pixel formats, seek values, and the raw output-option passthrough (`-movflags +faststart`, `-q:a 0`, `-f null`). D031 records these as deliberately unquoted — the first three are single clean tokens by their own validators, and the passthrough cannot express quoting at all: `ffm_groups()`'s `output_opts` branch hands `ffm_group()` a finished `display` string (`R/ffm.R:1332`), bypassing `quote=`, so covering it needs a signature change. Promote on the first report of any of these breaking a pasted command. — added 2026-07-31, compressed 2026-08-31 — M50 Out; D031
- `probe_all()`'s sequential (default) path materializes every file's probe before assembling, where the pre-M53 `for` loop let each raw result become garbage at once: `purrr::map(infile, probe_one)` holds the list for the whole call, so peak memory at `parallel = FALSE` rises for no benefit — only the parallel branch needs it. The magnitude is **unmeasured**, so the work starts by measuring peak memory over a few thousand many-stream files, master versus post-M53. Promote on a measurement showing a real regression, or on any report of `probe_all()` exhausting memory. — compressed 2026-09-02 — M53 review F7; `R/ffprobe.R:92-117`
- A wrongly-**typed** value still answers differently depending on its form, where M61 made a wrongly-**valued** one uniform: `picture_in_picture_batch(jobs, margin = "x", audio_codec = "aac")` reports the contradiction, while the same `margin` in a `jobs` column reports the type error (2026-08-08). A disclosed consequence of an intentional boundary: M61's Scope Out keeps every column *type* guard above the contradiction sweep, and D039 promises uniformity for a value error, not a type one. Promote on any report of a type mistake and a contradiction reporting differently by form. — compressed 2026-09-02 — M61 review r3 H11; D036; D039
- The normalize pair's copy/audio_stream form divergence (the batch row-locator half went to M66): `audio_codec = "copy"` and a fractional `audio_stream` report before a bad loudness target on the batch form and after it on the scalar (M65-D3's disclosure); uniformity means moving the batch's early copy/stream guards below its input sweep, reopening M34/M45's fail-before-Phase-1 placement. Promote on the first report of the divergence confusing a caller. — compressed 2026-08-31 — M65 review F1; D039; D043; M66
- **Eight things left out of the hardware surface**, each promotable alone. From M100: **(a)** `qsv`, `vaapi`, `amf` — a table row plus a probe, none testable on hardware this project reaches; **(b)** `prores_videotoolbox` writes a 0-byte file into `.mp4` (measured 2026-08-31) while `.mov`/`.mkv` take it, needing a container guard the table has no slot for; **(c)** a generic `has_encoder(name)`; **(d)** an export answering which backends encode a family; **(e)** the out-of-table refusal names the family, not the caller's `video_codec`, so a `_batch` row cannot be identified (M107 widened this to the `fallback = TRUE` arm); **(f)** AC1's sweep is syntactic. From M31, each blocked on a design call the engine has no slot for: **(g)** a video quality / rate-control knob (CRF↔CQ, `-preset p1–p7`, bitrate) — no quality abstraction exists and a cross-encoder mapping is opinionated and irreversible; **(h)** GPU *decode* / `-hwaccel` + GPU filter pipelines, needing a new engine input-options slot (only `seek_pre` goes before `-i`) plus an IP2 filtergraph design call. Promote each on the first caller or request naming it. — merged 2026-09-06 — M100 Out; M100 review; M31 Q4; M31 Out; D079
- **Five `data-raw/` guard generators still spell `audio =`** (`value-guard`, `input-guard`, `contradiction-guard`, `codec-guard`, `blame-precedence-m65`; 20 sites), the name M099 retired on the fan-in verbs. A pinned-ref run is unaffected and none writes a committed fixture, but a `ref = NULL` run records R's partial-match error in the audio cells that exist to measure a guard. The fix spells the cell by ref, since the pinned refs need the old name. Promote alongside any milestone re-running one of them against the working tree. — added 2026-09-01 — M099 review F4; M096; M061
- **`cairn/DESIGN.md` documents the naming surface stalely.** `## Conventions` carries none of D014's naming scheme, promised by M22's audit once D014 was ratified (2026-07-12; renames shipped in M22/M23/M24), so what is left is the doc pointer, not the work. `## Function families` is stale on the same axis (measured 2026-09-05): it names retired `audio_as_mp3`, `get_codecs`, `get_encoders` and the removed tidy-eval reexports, and omits `anonymize_video`, `strip_metadata`, `convert_audio`, `sample_frames`. Dropped from M111's criteria as circular — `NAMESPACE` carries no layer labels. Promote alongside any milestone touching the public naming surface, or at the 0.2.0 docs pass. — compressed 2026-09-05 — M22, routed from Known issues 2026-09-03; D014; M23; M099
- CRAN readiness (release mechanics only): win-builder + R-hub, `cran-comments.md`, the version bump to **0.2.0** (confirmed at M117-M122's gate; D014's free-rename window closes when 0.2.0 reaches CRAN). Deliberately last; **no window is declared** — release timing is the maintainer's call, re-asked and re-declined 2026-09-07. Re-measured 2026-09-07: `devtools::check()` still 0/0/0 at 7m47s with binaries present. What the walk inherits: `DESCRIPTION` is `0.1.0.9000`, which `--as-cran` NOTEs as a large version component; `cran-comments.md` is untouched since `4b04fad9` (2026-07-10) and asserts a 0/0/0 that a new submission cannot have; no git tag has ever been cut. The pre-release fixes are **M117-M122**; earlier policy defects went to **M097**/**M098** and D014's rename window to **M099**. Promote on the maintainer declaring a window. — recompressed 2026-09-07 — M22/M23/M30; M097; M098; M099; M117-M122; D014
