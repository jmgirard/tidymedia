# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-09-03 (`/cairn-triage` over 44 items — 40 candidates, 4 Known issues. Merged: the `set_program()` unclassed-abort row into the remaining-unclassed-aborts row, the `with_timeout()` interrupt-lag row into the tighter-kill row; both absorbed triggers survive. Dropped, both **already shipped** — M099 declined each for good and D077/D078 carry the decline and its post-0.2.0 reopening clause: per-call `timeout =`; renaming `audio_stream`. Routed: hand-written container names to DESIGN Known issues; M22's naming-and-docs Known issue to a candidate row for the DESIGN naming conventions D014's ratification never got. Compressed: the produced-set edges, the build-time hardware probe's defaultless `call` (renamed at M099/D077), the M082 leftover, the six M100 gaps. Promoted, not planned here: the M095/M56 codec seam. No decision entry — no principled drop, no lost trigger. `cairn_validate` green 16/16, no advisories; ROADMAP back under its 24,000 b budget on 36 candidate rows.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M106 | The codec seam is bound and the probe grid covers the codec sentinel | in-progress | — | normal | milestones/M106-codec-seam-bound-sentinel-covered.md |
| M105 | `install_on_win()`'s refusals say what is on disk | done | — | normal | milestones/archive/M105-install-refusals-key-on-disk.md |
| M104 | `install_on_win()` registers every program or none | done | — | normal | milestones/archive/M104-install-registers-all-or-none.md |
| M101 | `install_on_win()` asks before it downloads and installs | done | — | normal | milestones/archive/M101-install-on-win-confirmation.md |
| M102 | `install_on_win()` verifies the archive and the unpacked programs before it registers anything | done | — | normal | milestones/archive/M102-install-verify-before-register.md |
| M103 | A refused `install_on_win()` leaves the install directory as it found it | done | — | normal | milestones/archive/M103-install-leaves-dir-as-found.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- **Three edges the produced-set intersection does not settle.** A libarchive directory entry counts as produced (`tm_files_on_disk()`, `R/program_management.R:679`), so a fully-quarantined extraction still reports a non-empty `on_disk` when `bin/` is listed — and excluding directories breaks the `dir` spoil form. Unmeasured against a real archive. Two lesser edges: the `unpacked_here` site (`:1117`) is unobservable, and an empty-string entry counts as on disk. Promote on a report of a refusal naming a directory that holds nothing. — added 2026-09-03, compressed 2026-09-03 — M105 review F11, F4, F10; D084
- **The install's registration check is static; it never runs the binary it registers.** M104 asks that the produced path resolve under `Sys.which()` and that the file is not empty — which passes a wrong-architecture, AV-neutered, or otherwise corrupt executable of nonzero size. Running each unpacked program with `-version` would settle it, and would be the first probe in this seam to EXECUTE a downloaded binary, so it needs its own D024 call; it also turns a slow or blocked spawn into an install failure. Declined at M104's gate for both reasons. Promote on a report of an install registering a binary that cannot run. — added 2026-09-03 — M104 Out; D024; D001
- **Unpack into a scratch directory and move the result into place**, so a failed install never writes into the install directory and nothing has to be deleted. M103 measured why this is the only route that would work on Windows: the handle libarchive writes a failed entry through is leaked for the process lifetime, so the file cannot be deleted there, and neither `gc()` nor a pause frees it (`windows-latest`, 2026-09-02). Declined at M103's gate because it replaces the mechanism that milestone was planned around and costs a full copy when the scratch directory is on another volume. Promote on a report of a Windows caller harmed by what a refused install leaves, or on `archive` gaining a way to close that handle. — added 2026-09-02 — M103; D082; D046
- **The installer's digest check cannot detect a SUBSTITUTED archive.** M102 fetches `<url>.sha256` from the same host over the same TLS session as the archive, so it catches corruption and truncation, not a compromised source. Provenance needs a signature the source does not publish, or a pinned digest — declined at M102's gate because the upstream build is refreshed every FFmpeg release, so a pin goes stale and refuses a good archive. Promote on the source publishing a signature, or on a substituted archive passing the check. — added 2026-09-02 — M102 Out; D081; D001
- **`tm_parse_sidecar()` takes the first digest on any line** — right for gyan.dev's single-digest sidecar, wrong for a multi-file `sha256sum` manifest, where the first line's digest belongs to another file. The fix selects the line whose filename field matches `basename(download_url)` and refuses where none does, which is guesswork about a format nothing in play emits; left here at M103's gate for that reason. The group's other two leftovers became **M103**. Promote on a source publishing a multi-file manifest. — added 2026-09-02, narrowed 2026-09-02 — M102 review pass 1 [O]12; M103
- **Two argument classes still refused below the verb**, from M096's census (437 of 1530 cells at HEAD). **(a) Gate booleans** `run`, `parallel`, `progress`, `manifest`, `checksums`, `verify` at the ~18 verbs reaching `ffm_finish()`/`ffm_batch()`: `run = 123` is blamed on `ffm_finish()`, and fixing it reopens D074 property 2 at eighteen front doors. **(b) `ffm_batch()`'s `output` column**, blamed on `purrr::pmap`: its contract names no column, so a guard invents one and changes the per-row shape D007 fixes. Other census arguments are ACCEPTED in DESIGN Known issues. Promote each on a mistake blamed below the verb. — compressed 2026-09-02 — M096; D007; D074
- **`tm_outfiles_baseline()` records a merge-base table with no committed generator.** Its four constants in `tests/testthat/helper-timeout-sweep.R` carry provenance in a comment only, and every cell is a value M096's guard passes, so nothing distinguishes a table recorded at `4063faa` from one at HEAD. M096's review closed the EVIDENCE gap by re-measuring against a worktree at that commit. Promote alongside any milestone touching `segment_video()`'s compiled output or re-recording that table. — compressed 2026-08-31 — M096 review F7; M095 AC2
- **The build-time hardware probe's defaultless `call` cannot be asserted by a test.** M094 removed the default so a site forgetting the verb's frame cannot inherit the helper's own (D074); the helper is `hardware_encoder_available()` since M099/D077 renamed `nvenc_available()` (`R/ffmpeg.R:3181`). Measured 2026-08-31: R never forces the promise unless an abort is built, so a missing `call` returns silently either way — a static sweep, not a runtime assertion. Promote alongside any milestone adding a call site, or on the first site that forgets. — compressed 2026-09-03 — M094 review H8; D074; D042; D077
- **The SIGINT leftover from M088's failure-path findings.** A SIGINT reaching only the child could surface as a non-zero exit and spawn a second run instead of returning to the prompt — low confidence, never reproduced. M090's gate left it here because an unreproduced Ctrl-C interaction has no bounded investigation, and M078 is the precedent for what an unbounded one costs. Promote on the first report of Ctrl-C failing to return to the prompt. — compressed 2026-08-31 — M088 review pass 2 F3; D065; M078
- **`references/false-greens.md` sits 190 bytes under its own budget** (25,810/26,000, 55 of 60 lines); the module header's remedy is compress-or-retire, never growth. Promote on the module breaching either figure with nothing left worth compressing or retiring. — added 2026-08-29 — M089 review F2; M084; D067
- **The shape M082 left behind when it shipped the check's off switch:** probing lazily per row inside the fan-out so the cost parallelizes, which needs the `ffm_batch()` hook D024/RR02 Q3 rejected, so that entry must be superseded first. (The per-verb `check_tracks =` half was declined for good at M099; D077/D078 carry it and its post-0.2.0 `jobs`-column reopening clause.) Promote on a batch where the serial sweep dominates wall clock. — decided 2026-09-01, compressed 2026-09-03 — M082 review F9; D024; D047; D077
- **A `probe_all_impl()` argument refusal would name `infile`** through `verify_media()`, which has no such argument — a shipped message naming an argument its frame lacks. Unreachable today: `check_file_exists()` refuses first, which is why M70 logged it. M70's O11, classed **runtime** by M092's ledger. Promote on any change letting `verify_media()` refuse before the file-existence check. — added 2026-08-30 — M70 review O11; M092 ledger
- **The condition-class pairing test binds a claim to a help TOPIC, not to a SITE**, which is why the same over-attribution shipped green twice — `?tidymedia` and `?normalize_audio_batch` each naming `tidymedia_ffmpeg_exit` for `R/loudnorm_two_pass.R:112`. Strengthening it over the union of a topic's paired sites catches neither, since both also pair with `:151`, which does raise that class. M092's ledger prunes the finding as **instrument** and keeps the call plannable here. Promote on a third topic over-attributing a class. — compressed 2026-08-31 — M087 review p1 F5, p2 F1/F2; M092 ledger
- A permanent CI job that installs DESCRIPTION's declared `Imports` floors on every push, promoting M077's one-off `data-raw/imports-floors.R` into a standing check. M077's plan gate chose the one-off and declined this, because `setup-r-dependencies@v2` has no oldest-version input — its `dependencies` input selects dependency TYPES, not versions — so the job must be hand-rolled, and a standing job commits the repo to keeping ten floors green on every push, including whenever a floor version stops building on a newer R. Promote on the first floor regression that reaches a user between audits. — compressed 2026-08-31 — M077 Out
- **The two gaps D055 discloses and does not close: no floor run alone, and one operating system.** Nine one-floor-at-a-time legs would attribute a failure to its own floor rather than to the pinned set, and a three-OS run would say whether the floors hold off Linux. Both were declined at M077's plan gate on cost; M079 keeps the harness's `--only` mode alive so they stay plannable. Promote on a user reporting a failure on a declared floor the joint run does not reach — D055's own falsifier — or on any floor moving for a reason it cannot attribute. — compressed 2026-08-31 — D055 item 4; M077 gate; M079 Out
- A tighter kill than base R's `timeout=` gives. Base R escalates SIGINT at the limit, SIGTERM at +20 s, SIGKILL at +40 s, so a program ignoring the first two outlives its limit by up to 40 s — measured 42.0-42.1 s on Linux CI under a 2 s limit, which forced M69's AC3 to a 60 s bound; `?system` calls termination not guaranteed, so the lag is base R's contract. M69's return-2 gate rejected `processx`: a bounded 42 s meets the Goal as a bounded 2 s does, against a new dependency and four rewired spawn sites. Absorbs the `with_timeout()`/`local_timeout()` interrupt lag (`R/program_management.R:125`; M078 measured it and corrected the docs, reproducing neither the minutes-scale interrupt nor the wedge — the FIX, separating stopping the program from closing the pipe, stays open). Promote on the first report that the lag is itself the reported problem. — added 2026-08-26, compressed 2026-09-03 — M69 return 2; D047; D056; M078; M077 gate; D055
- **Route the two direct `resolve_hw_encoder()` sites through the codec seam** — `format_for_web_pipeline()` (`R/ffmpeg.R:1541`, literal codec) and `anonymize_pipeline()` (`:2126`, checked above). Declined at M106's gate: rerouting moves the emission point against the surrounding filters, risking the compiled bytes. Promote on a third direct site whose token is neither. — added 2026-09-03 — M106 Out; M095
- **The probe grid's mocked encoder pools are nvenc-only**, so every crossed cell's videotoolbox arm is unmeasured (`data-raw/nvenc-probe-order-baseline.R`). Left out of M106 on grid runtime. Promote alongside any milestone touching the videotoolbox mapping. — added 2026-09-03 — M106 Out; D079
- Fixed-region *region blur* (no face tracking): split→crop→boxblur→overlay needs an IP2 filtergraph design call (new blessed composite verb vs Layer 0) plus a new `ffm_boxblur` filter; not plannable until that call. Box-fill half became M20/M21. Confirmed in-scope (defer) by M25. — added 2026-07-10, split 2026-07-12, reconciled 2026-07-13 — research-verbs family 4; M25 survey §3 D1
- `burn_timecode` / drawtext text-and-timecode burn-in for coders & reliability raters; in-scope but needs a new `ffm_drawtext` Layer-1 filter + a surface-scope call. — added 2026-07-13 — M25 survey §3 D2 (defer)
- Minor in-scope convenience verbs (grouped): split multi-view→per-person clips, orientation fix (rotate/flip), contact-sheet QC montage; each needs a small arg-shape call. — added 2026-07-13 — M25 survey §3 D3 (defer-low)
- Two leftovers from the same M45 measurement, now that its video-after-failure half is **M088**. **(a)** A symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. **(b)** `ffm_concat()` / `concatenate_videos()`, which map all streams unconditionally and fail the way D026 measured for `-map 0`; promote alongside any milestone touching either verb's map. — added 2026-07-30, narrowed 2026-08-29 — M45 Out; D026; RR03; M088
- **The multi-track separation abort is still blind to WHY FFmpeg failed**, for every cause the output container cannot reveal. **The container half shipped as M091**; D069/D071 and its archive carry the rest. What stays: **(a)** classifying the failure from FFmpeg's stderr, which `ffm_run()` does not capture (`R/program_management.R:105`) and capturing would stop live console output; **(b)** a differential re-run with `-map 0:a:0` into a temp path — decisive, but the first probe here to execute FFmpeg and write a file, so it needs a D-entry. Promote on the abort naming a cause that is not the cause. — compressed 2026-09-02 — M45 review F1; M091; D024; D069; D071
- **The package's remaining unclassed aborts stay uncatchable by class** — 13 of 173 `cli_abort()` sites carry a `tidymedia_*` class (measured 2026-08-29). M086's criteria audit declined the sweep on two grounds: no grep enumerates the domain, since a non-zero status surfaces at every `run_program()` caller, not only where "ffmpeg" appears; and a multi-cause site cannot honestly carry `tidymedia_ffmpeg_exit` under D062's event rule, so this needs a naming pass, not one class. Absorbs **`set_program()`'s own abort is unclassed and blamed on itself** (`R/program_management.R:205` carries no `tidymedia_*` class and no `call =`, so the refusal names `set_program()` rather than the frame the user typed; M100's lesson governs the fix — an exported `call` defaults to `rlang::current_env()`), added 2026-09-03 — M104 Out; M100. Promote on a caller needing an abort M085/M086 left unclassed, or alongside a milestone giving the package a class hierarchy. — compressed 2026-09-02, merged 2026-09-03 — M086 Out; M087; D062; M104 Out; M100
- **`separate_audio_video_batch()`'s post-fan-out warning cannot carry an exit status, and cannot be classed as one.** `ffm_batch()`'s `run_one()` reduces each row to `list(success =, timed_out =)` and discards the condition (`R/ffm_batch.R:143-147`), so no status reaches the warning site; the warning fires for any cause, which D062 forbids classing as an exit; and `tryCatch()` on a warning unwinds the batch against D007, so the recipe stays `suppressWarnings(classes = )`. The fix changes `ffm_batch()`'s per-row result contract. Promote on a batch caller needing a failed row's status. — compressed 2026-08-31 — M086 Out; D007; D062
- **Two lookup-memoization gaps M67 left open (one memo, two axes).** **(a) `find_ffmpeg()` is not memoized**, so one `Sys.which()` spawn survives per row; M67's gate rejected keying the memo on the resolved path, since `find_program()` prefers `Sys.which()` and cannot see a config change while ffmpeg is on `PATH`. **(b) M67's memo is per-process**, invisible to `parallel = TRUE` workers: a W-worker batch probes W times where the sequential one probes once; D044 carries mechanism and magnitude. Promote each on a batch whose probe count is the problem. — compressed 2026-09-02 — M67 Out; D033; D044; M071
- Two `normalize_audio()` extensions the linear builder blocks (same blocker, IP2/D003 — one filter chain and one output). **(a) Per-track two-pass loudness normalization**, one measured set per mapped audio track: D028 is its own falsifier and carries the 3-track `.mkv` measurement; the blocker is the builder, not the parser. **(b) Normalize loudness while KEEPING the picture, in one call**, which M49/D030 removed; `keep_video =` reopens the container question D030 closed, so the likelier shape is a mux verb or two-output pipeline. Promote each on the first request naming it. — compressed 2026-09-02 — M49 Out; D003; D028; D030
- The compiled command's remaining bare token classes: codec names, pixel formats, seek values, and the raw output-option passthrough (`-movflags +faststart`, `-q:a 0`, `-f null`). D031 records these as deliberately unquoted — the first three are single clean tokens by their own validators, and the passthrough cannot express quoting at all: `ffm_groups()`'s `output_opts` branch hands `ffm_group()` a finished `display` string (`R/ffm.R:1332`), bypassing `quote=`, so covering it needs a signature change. Promote on the first report of any of these breaking a pasted command. — added 2026-07-31, compressed 2026-08-31 — M50 Out; D031
- `probe_all()`'s sequential (default) path materializes every file's probe before assembling, where the pre-M53 `for` loop let each raw result become garbage at once: `purrr::map(infile, probe_one)` holds the list for the whole call, so peak memory at `parallel = FALSE` rises for no benefit — only the parallel branch needs the materialized form. The magnitude is **unmeasured**, so the work starts by measuring peak memory over a few thousand many-stream files, master versus post-M53. Promote on a measurement showing a real regression, or on any report of `probe_all()` exhausting memory. — compressed 2026-09-02 — M53 review F7; `R/ffprobe.R:92-117`
- A wrongly-**typed** value still answers differently depending on its form, where M61 made a wrongly-**valued** one uniform: `picture_in_picture_batch(jobs, margin = "x", audio_codec = "aac")` reports the contradiction, while the same `margin` in a `jobs` column reports the type error (2026-08-08). A disclosed consequence of an intentional boundary: M61's Scope Out keeps every column *type* guard above the contradiction sweep, and D039 promises uniformity for a value error, not a type error. Promote on any report of a type mistake and a contradiction reporting differently by form. — compressed 2026-09-02 — M61 review r3 H11; D036; D039
- The normalize pair's copy/audio_stream form divergence (the batch row-locator half went to M66): `audio_codec = "copy"` and a fractional `audio_stream` report before a bad loudness target on the batch form and after it on the scalar (M65-D3's disclosure); uniformity means moving the batch's early copy/stream guards below its input sweep, reopening M34/M45's fail-before-Phase-1 placement. Promote on the first report of the divergence confusing a caller. — compressed 2026-08-31 — M65 review F1; D039; D043; M66
- **Two encoder-surface extensions deferred from M31** (each blocked on a design call the engine has no slot for). **(a) Video quality / rate-control knob** (CRF↔CQ, `-preset p1–p7`, bitrate): no quality abstraction exists and a cross-encoder mapping is opinionated and irreversible. **(b) GPU *decode* / `-hwaccel` + GPU filter pipelines:** needs a new engine input-options slot (only `seek_pre` goes before `-i`) plus an IP2 filtergraph design call. The third part went to M100. Promote either on the first request naming its knob or platform. — compressed 2026-09-02 — M31 Q4; M31 Out; M100
- **Six things M100 leaves out of the hardware surface**, each promotable alone. **(a)** `qsv`, `vaapi`, `amf` — a table row plus a probe, none testable on hardware this project reaches. **(b)** `prores_videotoolbox` writes a 0-byte file into `.mp4` (measured 2026-08-31) while `.mov`/`.mkv` take it, needing a container guard the table has no slot for. **(c)** a generic `has_encoder(name)`; **(d)** an export answering which backends encode a family. **(e)** the out-of-table refusal names the family, not the caller's `video_codec`, so a `_batch` row cannot be identified. **(f)** AC1's sweep is syntactic: a verb keeping a default AND a bare `arg_match` still passes (16/16 measured correct at review). Promote (b)-(f) on the first caller needing each. — added 2026-08-31, compressed 2026-09-03 — M100 Out; M100 review; D079
- **`set_program()` writes a remembered location with no confirmation**, to a persistent user directory M097 sanctioned. Unlike `install_on_win()` it downloads nothing and writes a path the caller typed, which is why M101's gate stops at the installer. Promote on a report of the config write surprising a caller, or on a CRAN comment reaching the config dir rather than the data dir. — added 2026-09-02 — M101 Out; M097; D001
- **Five `data-raw/` guard generators still spell `audio =`** (`value-guard`, `input-guard`, `contradiction-guard`, `codec-guard`, `blame-precedence-m65`; 20 sites), the name M099 retired on the fan-in verbs. A pinned-ref run is unaffected and none writes a committed fixture, but a `ref = NULL` run records R's partial-match error in the audio cells that exist to measure a guard. The fix spells the cell by ref, since the pinned refs need the old name. Promote alongside any milestone re-running one of them against the working tree. — added 2026-09-01 — M099 review F4; M096; M061
- **`cairn/DESIGN.md`'s `## Conventions` carries none of D014's naming scheme**, which M22's audit promised would land there once D014 was ratified. D014 was ratified 2026-07-12 and its renames shipped (M22/M23/M24: `acodec`/`vcodec`/`ts_start`/`ts_stop` and the unused tidy-eval reexports are gone from the surface, `get_*` is per-file metadata scalars only), so what is left is the doc pointer, not the work. Promote alongside any milestone touching the public naming surface, or at the 0.2.0 docs pass. — added 2026-07-12 — M22, routed from Known issues 2026-09-03; D014; M23; M099
- CRAN readiness (release mechanics only): win-builder + R-hub, `cran-comments.md`, bump toward 0.2.0. Deliberately last; **no window is declared** — release timing is the maintainer's call. Measured 2026-08-31: `devtools::check()` 0/0/0 and the suite clean with no binaries on `PATH` (287 skipped, 206 s); the docs slice is closed (78 of 81 topics carry `\examples`, the three without being concept topics). That audit's policy defects went to **M097** and **M098**; D014's pre-0.2.0 rename window is **M099**, and shipping 0.2.0 closes it. Promote on the maintainer declaring a window. — compressed 2026-09-02 — M22/M23/M30; M097; M098; M099; D014
