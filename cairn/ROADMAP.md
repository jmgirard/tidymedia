# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-08-31 (M095 and M096 planned from the M094 review row, which they absorb; what neither closes stays as two new candidate rows — the gate-boolean class the criteria audit surfaced, and `nvenc_available()`'s unassertable `call`. Earlier that day: M094 merged as PR #98 and archived; its round-3 findings split — the doc half fixed at the gate, the rest into one new candidate row for where a timeout refusal sits relative to a check the verb reaches only later. ROADMAP 25,627 bytes / 46 lines, LESSONS 19,608 / 32, both inside budget; the M50 test-sweep-grammar lesson pruned to make room for M094's, and the two doctrine modules untouched (false-greens 25,810/55, guard-ordering 5,451/27, both inside their headers' budgets). Terminal rows trimmed to five, M089 rolled off (archive holds it).)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M095 | A wrong argument outranks the nvenc availability probe | planned | — | normal | milestones/M095-argument-error-outranks-nvenc-probe.md |
| M096 | A wrong argument is refused by the verb, not by `purrr::pmap()` or after FFmpeg runs | planned | M095 | normal | milestones/M096-unguarded-argument-front-doors.md |
| M094 | An invalid `tidymedia.timeout` is refused by the function the caller typed | done | — | normal | milestones/archive/M094-timeout-refusal-blame.md |
| M093 | The separation help pages render their container list from the vector | done | — | normal | milestones/archive/M093-separation-container-list-generated.md |
| M092 | The deferred-findings backlog is triaged and the page retired | done | — | normal | milestones/archive/M092-instrument-backlog-triage.md |
| M091 | The multi-track advice stops arriving when the caller is already following it | done | — | normal | milestones/archive/M091-separation-diagnostic-container-gate.md |
| M090 | The both-fail path stops throwing away what it knows | done | — | normal | milestones/archive/M090-separation-failure-path-leftovers.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- **Two argument classes the verb still does not refuse, left standing when M095/M096 absorbed the M094 review row on 2026-08-31.** **(a) The gate booleans.** `run`, `parallel`, `progress`, `manifest`, `checksums` and `verify` carry no front-door guard on any of the ~18 verbs that reach `ffm_finish()`/`ffm_batch()`: `standardize_video(v, "o.mp4", run = 123)` is refused by `ffm_finish(p, run)`, a function the caller never typed, and under `options(tidymedia.timeout = 0.5)` the limit displaces it outright (both measured 2026-08-31). The displacement is not a defect but a decision — D074 property 2 sites `resolve_timeout()` ABOVE the run gate so a `run = FALSE` compile is refused too — so fixing the blame needs D074 property 2 revisited alongside six guards at eighteen front doors, which is M094's scope over again. M095/M096's sweeps drop these cells by measurement (the no-limit reference is refused from a frame BELOW the member) and name every one in their evidence, so the class stays visible rather than hidden. Promote on a report of a `run =`/`parallel =` mistake blamed on `ffm_finish()` or `ffm_batch()`, or alongside any milestone reopening D074 property 2. **(b) `ffm_batch()`'s `output` column**, which `purrr::pmap` still reports and a limit still displaces: `ffm_batch()`'s contract names no column, so guarding one invents a contract and changes the per-row result shape D007 fixes. M096 fixes the sibling case — `segment_video()`'s `outfiles`, a formal a front door CAN judge — and re-discloses this one. Promote on a batch caller reporting the pmap blame on an `output` value, or alongside a milestone reopening the D007 contract. — added 2026-08-31 — M094 review H1, H2, H3, H7, H8; M095; M096; D007; D074
- **`nvenc_available()`'s defaultless `call` cannot be asserted by a test.** M094 removed the argument's default so a site that forgets to thread the verb's frame cannot silently inherit the helper's own (D074, `R/ffmpeg.R:3004`), and left the property in a code comment. Measured 2026-08-31: R's lazy evaluation never forces the promise unless an abort is actually built, so `nvenc_available("h264")` with no `call` returns silently under a set `tidymedia.nvenc_encoders` and under a valid limit alike, and errors under an invalid one with the limit's own message rather than a missing-argument error. M096's criteria audit cut the criterion that promised otherwise. The work is finding a check that binds the property at all — a static sweep of the helper's call sites, not a runtime assertion. Promote alongside any milestone adding a call site for it, or on the first site that forgets. — added 2026-08-31 — M094 review H8; M096 Out; D074; D042
- **Container names written by HAND, outside the generated enumeration.** Two EXEMPLARS naming `.mka` and `.m4a`: `audio_stream_extras$separation_container` (`R/audio-stream-doc.R:133-141`) and the cli messages at `R/ffmpeg.R:745` and `:859`; promote on the first REMOVAL from `multi_audio_extensions`, since an exemplar illustrates rather than claiming the whole list and binding one would make a cli abort's wording depend on a vector order nothing guarantees. Plus the `.avi`/`.nut` COUNTER-EXAMPLES both separation help pages hand-write as containers the vector does NOT hold; promote on the first ADDITION of either to it, which would make each page contradict itself unnoticed. Successor to the hand-spelled-enumeration row M093 absorbed; `NEWS.md` stays hand-written as a historical record. Extended once by explicit disposition. — added 2026-08-30, extended 2026-08-30 — M091 review F4 round 3, F1 round 4; M093 review F5; D069; D071
- **The SIGINT leftover from M088's failure-path findings.** A SIGINT reaching only the child could surface as a non-zero exit and spawn a second run instead of returning to the prompt — low confidence, never reproduced. **The other four findings went to M090 on 2026-08-29**: the discarded video condition (a), `wrote` inferred from a zero exit (b), `abort_after_video()`'s untested non-`rlang` branch (c), and the undocumented second spawn on a reached limit (d). M090's gate left this one here because an unreproduced Ctrl-C interaction has no bounded investigation and M078 is the precedent for what an unbounded one costs. Promote on the first report of Ctrl-C failing to return to the prompt. — added 2026-08-29, narrowed 2026-08-29 — M088 review pass 2 F3; M090; D065; D066; M078
- **`references/false-greens.md` sits 190 bytes under its own budget** (25,810/26,000, over 55 of 60 lines); M089 cleared the other two tracked files and left this one untouched, and the module header's remedy is compress-or-retire, never growth. Successor to the three-file budget row M089 absorbed. Promote on the module breaching either figure with nothing left worth compressing or retiring. — added 2026-08-29 — M089 review F2; M084; D067
- **The two shapes M082 left behind when it shipped the check's off switch**, folded here in full from the retired M44 row (promoted to M082 on 2026-08-28, deleted by M083; both promotion conditions unchanged). **(a)** A per-verb `check_tracks =` argument, which M082's gate declined on D047's reasoning — a seam commits no exported signature — and which stays available under D014's pre-0.2.0 clean break; promote on a report of a caller needing two different answers inside one script, the case a session-wide switch cannot serve and D051 shipped `with_timeout()` for. **(b)** Probing lazily per row inside the fan-out so the cost parallelizes, which still needs the `ffm_batch()` hook D024/RR02 Q3 rejected and so still needs that entry superseded first; promote on a measured batch where the serial sweep, not the encoding, dominates wall clock even with progress shown. — added 2026-08-28 — M082 review F9; M44 review F4 (retired row, deleted by M083); D014; D024; D047; D051; M52 Out; M53 Out
- **A `probe_all_impl()` argument refusal would name `infile`** through `verify_media()`, which has no such argument — a shipped message naming an argument its frame lacks. Unreachable today: `check_file_exists()` refuses first, which is why M70 logged it. M70's O11, classed **runtime** by M092's ledger. Promote on any change letting `verify_media()` refuse before the file-existence check, or on a report of such a refusal. — added 2026-08-30 — M70 review O11; M092 ledger
- **The condition-class pairing test binds a claim to a help TOPIC, not to a SITE**, which is why the same over-attribution shipped green twice — `?tidymedia` and `?normalize_audio_batch` each naming `tidymedia_ffmpeg_exit` for `R/loudnorm_two_pass.R:112`, a human reader catching both. Strengthening it over the union of a topic's paired sites catches neither, since both also pair with `:151`, which does raise that class; binding a claim to a site is a design call of its own. M092's Scope put that call out; its ledger prunes the finding as **instrument** and keeps the call plannable here. Promote on a third topic over-attributing a class, or alongside a milestone reworking that instrument. — added 2026-08-30 — M087 review pass 1 F5, pass 2 F1/F2; M092 ledger
- `with_timeout()` and `local_timeout()` may take minutes, not seconds, to interrupt a program blocked on input on Linux: `run_program()` bounds a spawn with `system2(stdout = TRUE, input = , timeout = limit)` (`R/program_management.R:125`), and an `ffmpeg` blocked opening a named pipe survives `kill -TERM` and dies only on `kill -KILL`. **Measurement and the documentation correction shipped as M078 on 2026-08-28; the FIX — the mechanism separating stopping the program from closing the pipe — remains open here.** **corrected M078**: neither the minutes-scale interrupt nor the wedge reproduced in a rebuild of that runner. D056 and M078 carry every figure, the unreproduced-not-disproven reading, why nothing has reported it (macOS and all five CI jobs sit on the fast side), and the `rocker/r-ver:4.4.3` + `mkfifo` recipe. Promote on the first report that the lag is itself the reported problem, or alongside any milestone revisiting the spawn sites. — added 2026-08-28, split 2026-08-28, measured 2026-08-28 — M077 gate 3; D047; D051; D055; D056; M078
- A permanent CI job that installs DESCRIPTION's declared `Imports` floors on every push, promoting M077's one-off `data-raw/imports-floors.R` into a standing check. M077's plan gate chose the one-off script and declined this, because `r-lib/actions/setup-r-dependencies@v2` has no oldest-version input — its `dependencies` input selects dependency TYPES, not versions — so the job must be hand-rolled, and a standing job commits the repo to keeping ten floors green on every push, including whenever a floor version stops building on a newer R. Promote on the first floor regression that reaches a user between audits, or on any milestone that has to re-run the M077 script by hand a second time. — added 2026-08-27 — M077 Out; M077 work log
- **The two gaps D055 discloses and does not close: no floor run alone, and one operating system.** D055 item 4 states both verbatim: nine one-floor-at-a-time legs would attribute a failure to its own floor rather than to the pinned set, and a three-OS run would say whether the floors hold off Linux. Both were declined at M077's plan gate on cost and both remain declined; M079 keeps the harness's `--only` mode alive precisely so they stay plannable. Split out on 2026-08-28 when the carry row that had held them was retired into M079 (that row deleted by M083), so retiring it lost nothing. Promote on a user reporting a failure on a declared floor that the joint run does not reach — D055's own falsifier — or on any floor moving for a reason the joint run cannot attribute. — added 2026-08-28 — D055 item 4; M077 gate; M079 Out
- A tighter kill than base R's `timeout=` gives. Base R escalates SIGINT at the limit, SIGTERM at +20 s and SIGKILL at +40 s, so a program that ignores the first two outlives its limit by up to 40 seconds — measured 2026-08-09 on Linux CI at 42.0-42.1 s under a 2 s limit, which is what forced M69's AC3 from a 10 s bound to 60 s. `?system` states termination "works for typical commands, but is not guaranteed", so the lag is base R's contract and not a wiring fault. M69's return-2 gate rejected `processx` (or equivalent direct process control) because a bounded 42 s meets the Goal exactly as a bounded 2 s does, and a new dependency plus a rewiring of all four spawn sites was fresh risk at a second return. D056's M078 measurement confirms the premise and leaves the rejection untouched. Promote on the first report that the limit-plus-40s lag is itself the reported problem, or alongside any milestone that revisits the spawn sites. — added 2026-08-26, measured 2026-08-28 — M69 return 2; D047; D056; M078
- Fixed-region *region blur* (no face tracking): split→crop→boxblur→overlay needs an IP2 filtergraph design call (new blessed composite verb vs Layer 0) plus a new `ffm_boxblur` filter; not plannable until that call. Box-fill half became M20/M21. Confirmed in-scope (defer) by M25. — added 2026-07-10, split 2026-07-12, reconciled 2026-07-13 — research-verbs family 4; M25 survey §3 D1
- `burn_timecode` / drawtext text-and-timecode burn-in for coders & reliability raters; in-scope but needs a new `ffm_drawtext` Layer-1 filter + a surface-scope call. — added 2026-07-13 — M25 survey §3 D2 (defer)
- Minor in-scope convenience verbs (grouped): split multi-view→per-person clips, orientation fix (rotate/flip), contact-sheet QC montage; each needs a small arg-shape design call. — added 2026-07-13 — M25 survey §3 D3 (defer-low)
- Two leftovers from the same M45 measurement, now that its video-after-failure half is **M088** (planned 2026-08-29, implemented 2026-08-29 on its branch: the video command runs even after the audio one fails, and the audio failure is still what aborts the call — D065). **(a)** A symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. **(b)** `ffm_concat()` / `concatenate_videos()`, which map all streams unconditionally and fail the way D026 measured for `-map 0`; promote alongside any milestone touching either verb's map. — added 2026-07-30, extended 2026-07-30, narrowed 2026-08-29 — M45 Out; D026; RR03 Beyond-the-brief; M088
- **The multi-track separation abort is still blind to WHY FFmpeg failed, for every cause the output container cannot reveal** (M45 review F1, scored 65). **The container half shipped as M091 on 2026-08-30**; the nine containers, the measurement rule and the causes left indistinguishable are in D069/D071 and M091's archive summary, which this row no longer restates. What stays, folded in from M091's Out: **(a)** classifying the failure from FFmpeg's stderr, which `ffm_run()` does not capture (`stderr = ""`, `R/program_management.R:105`) and capturing would stop the live console output; **(b)** a differential re-run with `-map 0:a:0` into a temp path, decisive across every cause but the first probe here to execute FFmpeg and write a file, so it needs its own D-entry — D024 licenses the effect, not the shape; **(c)** the three causes D069 enumerates and M091 does not fix. **F5 went to M085 and F4 to M086, both 2026-08-29.** Promote (a) or (b) on a report of the abort naming a cause that is not the cause; promote (c)'s missing-directory cause alongside any milestone adding a front-door output-path guard. — added 2026-07-30, narrowed 2026-08-29 and 2026-08-30, compressed 2026-08-30 (twice) — M45 review F1; M085 review F4; M086; M091; D024; D069; D071; M45-D1
- **The package's remaining unclassed aborts stay uncatchable by class** — 13 of 173 `cli_abort()` sites carry a `tidymedia_*` class (measured 2026-08-29). M086's criteria audit declined the sweep on two grounds: no grep enumerates the domain, since a non-zero status surfaces at every `run_program()` caller and not only where "ffmpeg" appears; and a multi-cause site cannot honestly carry `tidymedia_ffmpeg_exit` under D062's event rule, so this needs a naming pass, not one class. **The M086-review-F5 naming half went to M087 on 2026-08-29**, leaving the sweep alone here. Promote on a caller needing an abort M085/M086 left unclassed, or alongside a milestone giving the package a class hierarchy. — added 2026-08-29, narrowed 2026-08-29 — M086 Out; M087; D062
- **`separate_audio_video_batch()`'s post-fan-out warning cannot carry an exit status, and cannot be classed as one.** `ffm_batch()`'s `run_one()` reduces each row to `list(success =, timed_out =)` and discards the condition (`R/ffm_batch.R:143-147`), so no status reaches the warning site; the warning fires for any failure cause, which D062 forbids classing as an exit; and `tryCatch()` on a warning unwinds the batch against D007, so the recipe stays `suppressWarnings(classes = )`. The fix changes `ffm_batch()`'s per-row result contract — D007 surface. **The F6 half — one event, two severities, two class vectors — went to M087 on 2026-08-29**, which records it; what stays here is the contract change. Promote on a batch caller needing a failed row's status, or alongside a milestone reopening that contract. — added 2026-08-29, narrowed 2026-08-29 — M086 Out; M087; D007; D062
- **Per-call `timeout =` arguments** on the ~60 run-capable verbs and on `ffm_run()`/`ffm_batch()`, so one call can carry a different limit from the next inside one script. D047 records why M69 chose the `tidymedia.timeout` option seam instead, and that the argument shape stays available under D014's pre-0.2.0 clean break. **The other half went to M071 (2026-08-26) and the grain problem to M072 (2026-08-27)**, which exports `with_timeout(expr, seconds)`: it reaches all 53 members of `tm_timeout_domain()` where an argument reaches 31, since the 22 exports with no `run =` have no channel for one (count corrected M083). What remains here is the argument SHAPE alone; D047's rejection stands unsuperseded, D051 discharging only its session-grain falsifier clause and not its irreversible-API verdict. Promote on a report that wrapping an expression is the wrong ergonomics, or on a caller needing a limit that varies per row within one batch, which no wrapper can give. — added 2026-08-09, extended 2026-08-26, split 2026-08-26, narrowed 2026-08-27 — M69 Scope Out; M46 Out; D014; D044; D047; D051; M071; M072
- **Two lookup-memoization gaps M67 left open (grouped; one memo, two axes).** **(a) `find_ffmpeg()` is not memoized**, so one `Sys.which()` shell spawn survives per row after M67 collapses the `-encoders` probe. M67's gate rejected keying the memo on the resolved binary path: the key recomputes on every build, and `find_program()` prefers `Sys.which()` over the `set_program()` config (`R/program_management.R:23-27`), so it cannot see a `set_program()` change while ffmpeg is on `PATH`. Wider blast radius than nvenc; nobody has timed `which` against FFmpeg startup, and that measurement is task one. **(b) M67's memo is per-process**, invisible to `parallel = TRUE` workers: a W-worker nvenc batch probes W times where the sequential one probes once. D044 carries mechanism and magnitude — W is bounded by the worker count, not the row count, a completeness gap rather than a stall — and M071 narrowed it to the remembered probe ANSWER alone (D050 supersedes the read-only premise). Promote (a) on a measured batch stall attributable to `Sys.which()` rather than to the encoder probe; promote (b) on a measured parallel batch whose probe count is itself the reported problem, or on any request to share capability state across workers; either alongside the next milestone touching `R/program_management.R`. — added 2026-08-08, grouped 2026-08-29, compressed 2026-08-29 — M67 Out; M54 Out; M53; D033; D034; D044; D050; M071
- Two `normalize_audio()` extensions the linear builder blocks (grouped; same blocker, IP2/D003 — one filter chain and one output). **(a) Per-track two-pass loudness normalization**, one measured set per mapped audio track: D028 records this as its own falsifier and carries the 3-track `.mkv` measurement. The blocker is the builder, not the parser: per-stream filter options do not exist. Promote on the first request to normalize more than one track, or alongside a milestone giving the builder a per-stream filter slot. **(b) Normalize loudness while KEEPING the picture, in one call**, which M49/D030 removed; the shape is undecided and is the work — `keep_video =` reopens the output-container question D030 closed, so the likelier shape is a separate mux verb or a two-output pipeline the builder has no slot for. Same latent break on the pass-through verbs (`format_for_web()` into an audio container, exit 234, measured 2026-07-31, left unfixed in M49). Promote on the first report of the escape hatch being needed for this, or alongside a milestone giving the builder a second output. — added 2026-07-31, grouped 2026-08-06 — M49; M49 Out; D003; D028; D029 (superseded); D030
- The compiled command's remaining bare token classes: codec names, pixel formats, seek values, and the raw output-option passthrough (`-movflags +faststart`, `-q:a 0`, `-f null`). D031 records these as deliberately unquoted — the first three are single clean tokens by their own validators, so quoting buys nothing today, and the passthrough cannot express it at all: the `output_opts` branch of `ffm_groups()` hands `ffm_group()` a finished `display` string (`R/ffm.R:1332`), bypassing the `quote=` mechanism, so covering it needs a signature change rather than another `quote = 2L`. M50 deliberately scoped itself to map specifiers, the one class a metacharacter had actually reached. Promote on the first report of any of these breaking a pasted command — a codec token or a seek value carrying a shell metacharacter is the shape to watch. — added 2026-07-31 — M50 Out; D031
- Renaming `audio_stream` or `audio`, or unifying the two `NULL` readings. M51 answered the confusion with documentation instead — one `?audio_stream` topic plus generated `@param` text — and D032 re-confirms the two-name call at eighteen verbs. Under D014's pre-0.2.0 clean break a rename is still available and gets its own milestone. Promote on the first report of a caller confused by the two names or by the two `NULL` readings, which is D025's and D026's own falsifier; never on the argument count alone, since eighteen verbs is not eighteen confused callers. — added 2026-07-31 — M51 Out; D014; D023; D025; D026; D032
- `probe_all()`'s sequential (default) path now materializes every file's probe before assembling, where the pre-M53 `for` loop let each file's raw result become garbage immediately: M53 replaced the loop body's inline `probe_one(f)` with a `purrr::map(infile, probe_one)` whose result list is held for the whole call, so peak memory on a large corpus rises at `parallel = FALSE` for no benefit — only the parallel branch needs the materialized form. The magnitude is **unmeasured**: the review finding said "roughly doubling" and the scorer flagged that as speculative, so the work starts by measuring peak memory over a few thousand many-stream files at `parallel = FALSE`, master versus post-M53, and only then decides whether to restore streaming on the sequential branch. Promote on a measurement showing a real regression, or on any report of `probe_all()` exhausting memory on a corpus that used to fit. — added 2026-08-06 — M53 review F7 (scored 74, logged); `R/ffprobe.R:92-117`
- A wrongly-**typed** value still answers differently depending on its form, where M61 made a wrongly-**valued** one uniform: `picture_in_picture_batch(jobs, margin = "x", audio_codec = "aac")` reports the contradiction, while the same `margin` carried in a `jobs` column reports the type error (measured 2026-08-08 on M61's branch). A disclosed consequence of an intentional boundary, not a defect — M61's Scope Out keeps every column *type* guard above the contradiction sweep, and D039 promises uniformity for a value error, not a type error. The work is the design call that boundary defers: whether a column's type check can split from its shape check so the value half moves below the sweep, and what that costs at every `_batch` verb. Promote on any report of a type mistake and a contradiction reporting differently by form, or alongside the next milestone touching the column type guards. — added 2026-08-08 — M61 review round 3 H11 (scored 40, logged); M61 Scope Out; D036; D039
- The normalize pair's copy/audio_stream form divergence (this row's part (a), the batch row-locator, promoted to M66 on 2026-08-08): `audio_codec = "copy"` and a fractional `audio_stream` report before a bad loudness target on the batch form and after it on the scalar (M65-D3's disclosure); uniformity means moving the batch's early copy/stream guards below its input sweep, reopening M34/M45's fail-before-Phase-1 placement. Promote on the first report of the divergence confusing a caller. — added 2026-08-08, regroomed 2026-08-08 — M65 review F1; M65-D3; D039; D043; M66
- **Three encoder-surface extensions deferred from M31 (grouped; each blocked on a design call the engine has no slot for).** **(a) Video quality / rate-control knob** (CRF↔CQ, `-preset p1–p7`, bitrate): no quality abstraction exists today and a cross-encoder mapping is opinionated and an irreversible-API commitment. **(b) GPU *decode* / `-hwaccel cuda` input acceleration + GPU filter pipelines:** needs a new engine input-options slot (none exists; only `seek_pre` goes before `-i`) plus an IP2 filtergraph design call. **(c) Other hardware encode backends** (videotoolbox/qsv/vaapi/amf) generalizing the `hardware=` arg beyond nvenc: needs backend detection and an arg vocabulary. Promote any part on the first request naming that part's knob, platform, or backend, or alongside a milestone that gives the engine an input-options slot. — added 2026-07-26, grouped 2026-08-29 — M31 Q4; M31 Out
- CRAN readiness (release mechanics only): win-builder + R-hub, cran-comments, bump toward 0.2.0. Deliberately last. API-surface cleanup shipped as M23; the vignette pass is M30; a roxygen `@examples` pass remains the only open docs slice under this row. — added 2026-07-10, trimmed 2026-07-12, reconciled 2026-07-12 (M30) — see M22/M23/M30
