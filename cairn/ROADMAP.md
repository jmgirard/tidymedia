# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-08-31 (M095 merged as PR #99 and archived; its two deferred review findings became one new candidate row for the codec seam it split. `ROADMAP.md` was found 2,852 bytes over its 24,000-byte budget — an overrun the M094 stamp recorded as inside it — and fifteen candidate rows were compressed against their durable records to clear it: now 23,985 bytes / 49 lines. LESSONS 19,608 / 32, inside budget; doctrine modules untouched (false-greens 25,810/55, guard-ordering 5,451/27). Terminal rows back to five, M090 rolled off. Nothing retired.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M095 | A wrong argument outranks the nvenc availability probe | done | — | normal | milestones/archive/M095-argument-error-outranks-nvenc-probe.md |
| M096 | A wrong argument is refused by the verb, not by `purrr::pmap()` or after FFmpeg runs | review | M095 | normal | milestones/M096-unguarded-argument-front-doors.md |
| M094 | An invalid `tidymedia.timeout` is refused by the function the caller typed | done | — | normal | milestones/archive/M094-timeout-refusal-blame.md |
| M093 | The separation help pages render their container list from the vector | done | — | normal | milestones/archive/M093-separation-container-list-generated.md |
| M092 | The deferred-findings backlog is triaged and the page retired | done | — | normal | milestones/archive/M092-instrument-backlog-triage.md |
| M091 | The multi-track advice stops arriving when the caller is already following it | done | — | normal | milestones/archive/M091-separation-diagnostic-container-gate.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- **Two gaps M095's review left in the codec seam it split.** **(a)** `apply_video_codec()` became `check_video_codec()` + `emit_video_codec()` (`R/ffmpeg.R:3211`, `:3220`) with nothing binding the halves — `emit_` without `check_` above it reintroduces M56's hole verbatim, since `resolve_hw_encoder()` rewrites the token before `ffm_codec()`'s `check_token()` sees it. All three sites are correct today, so the work is a static sweep pinning the pairing. **(b)** AC2's grid overwrites `video_codec` at every cell (`data-raw/nvenc-probe-order-baseline.R:132`), leaving `emit_video_codec()`'s `NULL` sentinel branch outside the byte-identical evidence — hand-checked identical at both refs, so a coverage gap, not a defect. Promote (a) alongside any milestone adding a call site for either half; (b) alongside any milestone regenerating that grid. — added and compressed 2026-08-31 — M095 review F4, F6; M56 review F3; D016; D036
- **Arguments still refused below the verb, from M096's member x formal x wrong-form census (2026-08-31; 445 of 1530 cells dropped).** **(a) Gate booleans** `run`, `parallel`, `progress`, `manifest`, `checksums`, `verify` at the ~18 verbs reaching `ffm_finish()`/`ffm_batch()`: `run = 123` is blamed on `ffm_finish()`, and a set limit displaces even that; fixing it reopens D074 property 2 at eighteen front doors. **(b) `ffm_batch()`'s `output` column**, blamed on `purrr::pmap`: its contract names no column, so a guard invents one and changes the per-row shape D007 fixes (M096 shipped the sibling `segment_video()`/`outfiles`). **(c)** `fallback` at eight fan-out verbs (D075 calls its position an accepted cost), `anonymize_video_batch()`'s `pixel_format`/`color`, `normalize_audio_batch()`'s `channels`/`sample_rate`, `has_nvenc()`'s `codec`, six `verify_media()` arguments blamed on an assignment. Both sweeps drop each cell by measurement and name its frame. Promote (a) on a `run =` mistake blamed below the verb or a milestone reopening D074 property 2; (b) or (c) on a report naming one of their arguments. — 2026-08-31 — M094 review H1-H3, H7, H8; M095; M096; D007; D074; D075
- **`nvenc_available()`'s defaultless `call` cannot be asserted by a test.** M094 removed the argument's default so a site forgetting to thread the verb's frame cannot silently inherit the helper's own (D074, `R/ffmpeg.R:3004`), leaving the property in a code comment. Measured 2026-08-31: R's laziness never forces the promise unless an abort is actually built, so `nvenc_available("h264")` with no `call` returns silently under a set `tidymedia.nvenc_encoders` and under a valid limit alike. M096's criteria audit cut the criterion that promised otherwise. The work is finding a check that binds the property at all — a static sweep of the call sites, not a runtime assertion. Promote alongside any milestone adding a call site, or on the first site that forgets. — added 2026-08-31, compressed 2026-08-31 — M094 review H8; M096 Out; D074; D042
- **Container names written by HAND, outside the generated enumeration.** Two EXEMPLARS naming `.mka` and `.m4a`: `audio_stream_extras$separation_container` (`R/audio-stream-doc.R:133-141`) and the cli messages at `R/ffmpeg.R:745` and `:859`; promote on the first REMOVAL from `multi_audio_extensions`, since an exemplar illustrates rather than claims the whole list, and binding one would make an abort's wording depend on a vector order nothing guarantees. Plus the `.avi`/`.nut` COUNTER-EXAMPLES both separation help pages hand-write as containers the vector does NOT hold; promote on the first ADDITION of either, which would make each page contradict itself unnoticed. `NEWS.md` stays hand-written as a historical record. — added 2026-08-30, extended 2026-08-30, compressed 2026-08-31 — M091 review F4 round 3, F1 round 4; M093 review F5; D069; D071
- **The SIGINT leftover from M088's failure-path findings.** A SIGINT reaching only the child could surface as a non-zero exit and spawn a second run instead of returning to the prompt — low confidence, never reproduced. **The other four findings went to M090 on 2026-08-29**: the discarded video condition (a), `wrote` inferred from a zero exit (b), `abort_after_video()`'s untested non-`rlang` branch (c), and the undocumented second spawn on a reached limit (d). M090's gate left this one here because an unreproduced Ctrl-C interaction has no bounded investigation and M078 is the precedent for what an unbounded one costs. Promote on the first report of Ctrl-C failing to return to the prompt. — added 2026-08-29, narrowed 2026-08-29 — M088 review pass 2 F3; M090; D065; D066; M078
- **`references/false-greens.md` sits 190 bytes under its own budget** (25,810/26,000, over 55 of 60 lines); M089 cleared the other two tracked files and left this one untouched, and the module header's remedy is compress-or-retire, never growth. Successor to the three-file budget row M089 absorbed. Promote on the module breaching either figure with nothing left worth compressing or retiring. — added 2026-08-29 — M089 review F2; M084; D067
- **The two shapes M082 left behind when it shipped the check's off switch** (folded from the retired M44 row; both promotion conditions unchanged). **(a)** A per-verb `check_tracks =` argument, declined at M082's gate on D047's reasoning — a seam commits no exported signature — and still available under D014's pre-0.2.0 clean break; promote on a report of a caller needing two different answers inside one script, the case a session-wide switch cannot serve. **(b)** Probing lazily per row inside the fan-out so the cost parallelizes, which needs the `ffm_batch()` hook D024/RR02 Q3 rejected and so needs that entry superseded first; promote on a measured batch where the serial sweep, not the encoding, dominates wall clock. — added 2026-08-28, compressed 2026-08-31 — M082 review F9; M44 review F4; D014; D024; D047; D051; M52 Out; M53 Out
- **A `probe_all_impl()` argument refusal would name `infile`** through `verify_media()`, which has no such argument — a shipped message naming an argument its frame lacks. Unreachable today: `check_file_exists()` refuses first, which is why M70 logged it. M70's O11, classed **runtime** by M092's ledger. Promote on any change letting `verify_media()` refuse before the file-existence check, or on a report of such a refusal. — added 2026-08-30 — M70 review O11; M092 ledger
- **The condition-class pairing test binds a claim to a help TOPIC, not to a SITE**, which is why the same over-attribution shipped green twice — `?tidymedia` and `?normalize_audio_batch` each naming `tidymedia_ffmpeg_exit` for `R/loudnorm_two_pass.R:112`, a human reader catching both. Strengthening it over the union of a topic's paired sites catches neither, since both also pair with `:151`, which does raise that class; binding a claim to a site is a design call of its own. M092's Scope put that call out; its ledger prunes the finding as **instrument** and keeps the call plannable here. Promote on a third topic over-attributing a class, or alongside a milestone reworking that instrument. — added 2026-08-30 — M087 review pass 1 F5, pass 2 F1/F2; M092 ledger
- `with_timeout()`/`local_timeout()` may take minutes, not seconds, to interrupt a program blocked on input on Linux: `run_program()` bounds a spawn with `system2(timeout = )` (`R/program_management.R:125`), and an `ffmpeg` blocked opening a named pipe survives `kill -TERM`. **Measurement and the documentation correction shipped as M078; the FIX — separating stopping the program from closing the pipe — remains open.** **corrected M078**: neither the minutes-scale interrupt nor the wedge reproduced in a rebuild of that runner. D056 and M078 carry every figure, the unreproduced-not-disproven reading, and the reproduction recipe. Promote on the first report that the lag is itself the reported problem, or alongside any milestone revisiting the spawn sites. — added, split and measured 2026-08-28, compressed 2026-08-31 — M077 gate 3; D047; D051; D055; D056; M078
- A permanent CI job that installs DESCRIPTION's declared `Imports` floors on every push, promoting M077's one-off `data-raw/imports-floors.R` into a standing check. M077's plan gate chose the one-off script and declined this, because `r-lib/actions/setup-r-dependencies@v2` has no oldest-version input — its `dependencies` input selects dependency TYPES, not versions — so the job must be hand-rolled, and a standing job commits the repo to keeping ten floors green on every push, including whenever a floor version stops building on a newer R. Promote on the first floor regression that reaches a user between audits, or on any milestone that has to re-run the M077 script by hand a second time. — added 2026-08-27 — M077 Out; M077 work log
- **The two gaps D055 discloses and does not close: no floor run alone, and one operating system.** D055 item 4 states both verbatim: nine one-floor-at-a-time legs would attribute a failure to its own floor rather than to the pinned set, and a three-OS run would say whether the floors hold off Linux. Both were declined at M077's plan gate on cost and both remain declined; M079 keeps the harness's `--only` mode alive precisely so they stay plannable. Split out on 2026-08-28 when the carry row that had held them was retired into M079 (that row deleted by M083), so retiring it lost nothing. Promote on a user reporting a failure on a declared floor that the joint run does not reach — D055's own falsifier — or on any floor moving for a reason the joint run cannot attribute. — added 2026-08-28 — D055 item 4; M077 gate; M079 Out
- A tighter kill than base R's `timeout=` gives. Base R escalates SIGINT at the limit, SIGTERM at +20 s and SIGKILL at +40 s, so a program ignoring the first two outlives its limit by up to 40 s — measured 42.0-42.1 s on Linux CI under a 2 s limit, which forced M69's AC3 from a 10 s bound to 60 s. `?system` calls termination not guaranteed, so the lag is base R's contract, not a wiring fault. M69's return-2 gate rejected `processx` because a bounded 42 s meets the Goal as a bounded 2 s does, and a new dependency plus rewiring four spawn sites was fresh risk at a second return; D056's M078 measurement leaves that rejection untouched. Promote on the first report that the lag is itself the reported problem, or alongside any milestone revisiting the spawn sites. — added 2026-08-26, measured 2026-08-28, compressed 2026-08-31 — M69 return 2; D047; D056; M078
- Fixed-region *region blur* (no face tracking): split→crop→boxblur→overlay needs an IP2 filtergraph design call (new blessed composite verb vs Layer 0) plus a new `ffm_boxblur` filter; not plannable until that call. Box-fill half became M20/M21. Confirmed in-scope (defer) by M25. — added 2026-07-10, split 2026-07-12, reconciled 2026-07-13 — research-verbs family 4; M25 survey §3 D1
- `burn_timecode` / drawtext text-and-timecode burn-in for coders & reliability raters; in-scope but needs a new `ffm_drawtext` Layer-1 filter + a surface-scope call. — added 2026-07-13 — M25 survey §3 D2 (defer)
- Minor in-scope convenience verbs (grouped): split multi-view→per-person clips, orientation fix (rotate/flip), contact-sheet QC montage; each needs a small arg-shape design call. — added 2026-07-13 — M25 survey §3 D3 (defer-low)
- Two leftovers from the same M45 measurement, now that its video-after-failure half is **M088** (planned 2026-08-29, implemented 2026-08-29 on its branch: the video command runs even after the audio one fails, and the audio failure is still what aborts the call — D065). **(a)** A symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. **(b)** `ffm_concat()` / `concatenate_videos()`, which map all streams unconditionally and fail the way D026 measured for `-map 0`; promote alongside any milestone touching either verb's map. — added 2026-07-30, extended 2026-07-30, narrowed 2026-08-29 — M45 Out; D026; RR03 Beyond-the-brief; M088
- **The multi-track separation abort is still blind to WHY FFmpeg failed, for every cause the output container cannot reveal** (M45 review F1). **The container half shipped as M091**; D069/D071 and M091's archive carry the containers, the measurement rule and the causes left indistinguishable. What stays: **(a)** classifying the failure from FFmpeg's stderr, which `ffm_run()` does not capture (`R/program_management.R:105`) and capturing would stop the live console output; **(b)** a differential re-run with `-map 0:a:0` into a temp path — decisive, but the first probe here to execute FFmpeg and write a file, so it needs its own D-entry; **(c)** the three causes D069 enumerates. Promote (a)/(b) on the abort naming a cause that is not the cause; (c) alongside any front-door output-path guard. — added 2026-07-30, narrowed and compressed 2026-08-29 to 2026-08-31 — M45 review F1; M086; M091; D024; D069; D071
- **The package's remaining unclassed aborts stay uncatchable by class** — 13 of 173 `cli_abort()` sites carry a `tidymedia_*` class (measured 2026-08-29). M086's criteria audit declined the sweep on two grounds: no grep enumerates the domain, since a non-zero status surfaces at every `run_program()` caller and not only where "ffmpeg" appears; and a multi-cause site cannot honestly carry `tidymedia_ffmpeg_exit` under D062's event rule, so this needs a naming pass, not one class. **The M086-review-F5 naming half went to M087 on 2026-08-29**, leaving the sweep alone here. Promote on a caller needing an abort M085/M086 left unclassed, or alongside a milestone giving the package a class hierarchy. — added 2026-08-29, narrowed 2026-08-29 — M086 Out; M087; D062
- **`separate_audio_video_batch()`'s post-fan-out warning cannot carry an exit status, and cannot be classed as one.** `ffm_batch()`'s `run_one()` reduces each row to `list(success =, timed_out =)` and discards the condition (`R/ffm_batch.R:143-147`), so no status reaches the warning site; the warning fires for any failure cause, which D062 forbids classing as an exit; and `tryCatch()` on a warning unwinds the batch against D007, so the recipe stays `suppressWarnings(classes = )`. The fix changes `ffm_batch()`'s per-row result contract. Promote on a batch caller needing a failed row's status. — added and narrowed 2026-08-29, compressed 2026-08-31 — M086 Out; M087; D007; D062
- **Per-call `timeout =` arguments** on the ~60 run-capable verbs and on `ffm_run()`/`ffm_batch()`, so one call can carry a different limit from the next inside one script. D047 records why M69 chose the option seam and that the argument shape stays available under D014's pre-0.2.0 clean break. **The other half went to M071 and the grain problem to M072**, which exports `with_timeout(expr, seconds)` — reaching all 53 members of `tm_timeout_domain()` where an argument reaches 31. What remains is the argument SHAPE alone. Promote on a report that wrapping an expression is the wrong ergonomics, or on a caller needing a limit that varies per row within one batch. — added 2026-08-09, extended and split 2026-08-26, narrowed 2026-08-27, compressed 2026-08-31 — M69 Scope Out; M46 Out; D014; D044; D047; D051; M071; M072
- **Two lookup-memoization gaps M67 left open (one memo, two axes).** **(a) `find_ffmpeg()` is not memoized**, so one `Sys.which()` spawn survives per row after M67 collapses the `-encoders` probe. M67's gate rejected keying the memo on the resolved binary path: the key recomputes on every build, and `find_program()` prefers `Sys.which()` over `set_program()` (`R/program_management.R:23-27`), so it cannot see a config change while ffmpeg is on `PATH`. Timing `which` against FFmpeg startup is task one. **(b) M67's memo is per-process**, invisible to `parallel = TRUE` workers: a W-worker nvenc batch probes W times where the sequential one probes once; D044 carries mechanism and magnitude. Promote (a) on a measured batch stall attributable to `Sys.which()`; (b) on a measured parallel batch whose probe count is itself the problem. — added 2026-08-08, grouped and compressed 2026-08-29 to 2026-08-31 — M67 Out; M54 Out; M53; D033; D034; D044; D050; M071
- Two `normalize_audio()` extensions the linear builder blocks (same blocker, IP2/D003 — one filter chain and one output). **(a) Per-track two-pass loudness normalization**, one measured set per mapped audio track: D028 records this as its own falsifier and carries the 3-track `.mkv` measurement. The blocker is the builder, not the parser. Promote on the first request to normalize more than one track. **(b) Normalize loudness while KEEPING the picture, in one call**, which M49/D030 removed; the shape is undecided and is the work — `keep_video =` reopens the container question D030 closed, so the likelier shape is a mux verb or a two-output pipeline. Promote on the first report of the escape hatch being needed here. — added 2026-07-31, grouped 2026-08-06, compressed 2026-08-31 — M49; M49 Out; D003; D028; D029 (superseded); D030
- The compiled command's remaining bare token classes: codec names, pixel formats, seek values, and the raw output-option passthrough (`-movflags +faststart`, `-q:a 0`, `-f null`). D031 records these as deliberately unquoted — the first three are single clean tokens by their own validators, so quoting buys nothing today, and the passthrough cannot express it at all: the `output_opts` branch of `ffm_groups()` hands `ffm_group()` a finished `display` string (`R/ffm.R:1332`), bypassing the `quote=` mechanism, so covering it needs a signature change rather than another `quote = 2L`. M50 deliberately scoped itself to map specifiers, the one class a metacharacter had actually reached. Promote on the first report of any of these breaking a pasted command — a codec token or a seek value carrying a shell metacharacter is the shape to watch. — added 2026-07-31 — M50 Out; D031
- Renaming `audio_stream` or `audio`, or unifying the two `NULL` readings. M51 answered the confusion with documentation instead — one `?audio_stream` topic plus generated `@param` text — and D032 re-confirms the two-name call at eighteen verbs. Under D014's pre-0.2.0 clean break a rename is still available. Promote on the first report of a caller confused by the two names or the two `NULL` readings, which is D025's and D026's own falsifier; never on the argument count alone. — added 2026-07-31, compressed 2026-08-31 — M51 Out; D014; D023; D025; D026; D032
- `probe_all()`'s sequential (default) path materializes every file's probe before assembling, where the pre-M53 `for` loop let each raw result become garbage immediately: M53 replaced the inline `probe_one(f)` with a `purrr::map(infile, probe_one)` whose list is held for the whole call, so peak memory on a large corpus rises at `parallel = FALSE` for no benefit — only the parallel branch needs the materialized form. The magnitude is **unmeasured** (the review's "roughly doubling" was flagged speculative), so the work starts by measuring peak memory over a few thousand many-stream files at `parallel = FALSE`, master versus post-M53. Promote on a measurement showing a real regression, or on any report of `probe_all()` exhausting memory on a corpus that used to fit. — added 2026-08-06, compressed 2026-08-31 — M53 review F7; `R/ffprobe.R:92-117`
- A wrongly-**typed** value still answers differently depending on its form, where M61 made a wrongly-**valued** one uniform: `picture_in_picture_batch(jobs, margin = "x", audio_codec = "aac")` reports the contradiction, while the same `margin` in a `jobs` column reports the type error (measured 2026-08-08). A disclosed consequence of an intentional boundary — M61's Scope Out keeps every column *type* guard above the contradiction sweep, and D039 promises uniformity for a value error, not a type error. The work is whether a column's type check can split from its shape check. Promote on any report of a type mistake and a contradiction reporting differently by form. — added 2026-08-08, compressed 2026-08-31 — M61 review round 3 H11; M61 Scope Out; D036; D039
- The normalize pair's copy/audio_stream form divergence (the batch row-locator half went to M66): `audio_codec = "copy"` and a fractional `audio_stream` report before a bad loudness target on the batch form and after it on the scalar (M65-D3's disclosure); uniformity means moving the batch's early copy/stream guards below its input sweep, reopening M34/M45's fail-before-Phase-1 placement. Promote on the first report of the divergence confusing a caller. — added and regroomed 2026-08-08, compressed 2026-08-31 — M65 review F1; M65-D3; D039; D043; M66
- **Three encoder-surface extensions deferred from M31 (each blocked on a design call the engine has no slot for).** **(a) Video quality / rate-control knob** (CRF↔CQ, `-preset p1–p7`, bitrate): no quality abstraction exists and a cross-encoder mapping is opinionated and an irreversible-API commitment. **(b) GPU *decode* / `-hwaccel cuda` input acceleration + GPU filter pipelines:** needs a new engine input-options slot (only `seek_pre` goes before `-i`) plus an IP2 filtergraph design call. **(c) Other hardware encode backends** (videotoolbox/qsv/vaapi/amf) generalizing `hardware=` beyond nvenc: needs backend detection and an arg vocabulary. Promote any part on the first request naming that part's knob, platform, or backend. — added 2026-07-26, grouped 2026-08-29, compressed 2026-08-31 — M31 Q4; M31 Out
- CRAN readiness (release mechanics only): win-builder + R-hub, cran-comments, bump toward 0.2.0. Deliberately last. API-surface cleanup shipped as M23; the vignette pass is M30; a roxygen `@examples` pass remains the only open docs slice under this row. — added 2026-07-10, trimmed 2026-07-12, reconciled 2026-07-12 (M30) — see M22/M23/M30
