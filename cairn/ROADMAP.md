# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-07-30 (M46 planned from the subtitle-fixture hang row, which is struck through as promoted and carries the reproduction; 1 candidate row added for M46's Out — no timeout on the package's own runtime path; 5 terminal rows, still exactly at the retention cap, so none pruned; LESSONS unchanged at 47. 36 lines.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M41 | Front-door validation parity for the codec arguments | done | — | normal | milestones/archive/M41-codec-arg-front-door-guards.md |
| M42 | What `NULL` and column `NA` mean, settled across the codec family | done | M41 | normal | milestones/archive/M42-codec-null-na-semantics.md |
| M43 | Pick which audio track the extraction verbs take (`audio_stream`) | done | M41, M42 | normal | milestones/archive/M43-audio-stream-selection.md |
| M44 | Say something when audio tracks are dropped | done | M43 | normal | milestones/archive/M44-implicit-track-drop-warning.md |
| M45 | Give a multi-track `separate_audio_video()` caller a way out | done | M43 | normal | milestones/archive/M45-separate-av-multitrack.md |
| M46 | Stop the subtitle fixture hanging, and bound every fixture command | planned | — | normal | milestones/M46-fixture-hang-timeout.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- Fixed-region *region blur* (no face tracking): split→crop→boxblur→overlay needs an IP2 filtergraph design call (new blessed composite verb vs Layer 0) plus a new `ffm_boxblur` filter; not plannable until that call. Box-fill half became M20/M21. Confirmed in-scope (defer) by M25. — added 2026-07-10, split 2026-07-12, reconciled 2026-07-13 — research-verbs family 4; M25 survey §3 D1
- `burn_timecode` / drawtext text-and-timecode burn-in for coders & reliability raters; in-scope but needs a new `ffm_drawtext` Layer-1 filter + a surface-scope call. — added 2026-07-13 — M25 survey §3 D2 (defer)
- Minor in-scope convenience verbs (grouped): split multi-view→per-person clips, orientation fix (rotate/flip), contact-sheet QC montage; each needs a small arg-shape design call. — added 2026-07-13 — M25 survey §3 D3 (defer-low)
- ~~A scalar `audio_codec = NA` on a `_batch` verb silently compiles the default~~ — promoted to M41 (2026-07-29, corrected there): probes found only `normalize_audio_batch` affected, not the three verbs claimed; `standardize_video_batch` and `anonymize_video_batch` already abort via M39's `check_string()`. M41 also carries seven verb/argument pairs whose abort leaks Layer-1 names or fires mid-fan-out. — added 2026-07-26, promoted 2026-07-29 — M41
- Carry `audio_stream` to the **four remaining** verbs that pass audio *through* (`standardize_video`, `crop_video`, `segment_video`, `anonymize_video` — `separate_audio_video` was absorbed by M45 on 2026-07-30, so this row no longer covers it), and reconcile it with the 0-based `audio =` *input* index on `compare_videos`/`picture_in_picture` (D009) — one argument or two. **Now plannable** (M43 shipped 2026-07-30; D023 fixes the shape as a 0-based index among one input's audio streams and keeps it a separate name from D009's input index); promote if a caller needs to choose a track on any of the four. Note M45 makes `NULL` mean *every* track on its verb, so this row must settle whether the four follow M45 or D023 — D025 records that split and observes all four pass audio *through*, so on their face they are M45's shape. — added 2026-07-29, unblocked 2026-07-30, narrowed 2026-07-30 — M43 Out; M45; D009; D023; D025
- ~~`separate_audio_video()` aborts on a multi-track input when the audio output is a single-stream container~~ — promoted to M45 (2026-07-30): the design call landed on keeping every track by default plus an `audio_stream` selector and an enriched abort, not on a front-door container check. Re-measured 2026-07-30 (ffmpeg 8.1.2): `.aac`/`.mp3`/`.wav` fail with a zero-byte file, `.mka`/`.m4a` succeed carrying all 3, and the audio command running first means the video file is never written either. — added 2026-07-29, promoted 2026-07-30 — M45; found by the convert_audio `-map a` hotfix (PR #44); D023
- Two `separate_audio_video()` failure-path leftovers M45 measured but left out (grouped): FFmpeg's zero-byte output file survives a failed run, and the video command never runs once the audio one aborts, so a failed separation writes nothing usable. Both are behavior changes on an error path rather than parts of the multi-track fix. Also unfired: a symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both, and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. — added 2026-07-30 — M45 Out
- The multi-track separation abort is blind to WHY FFmpeg failed, and detects a non-zero exit by parsing a message rather than reading a field (grouped, both from M45 review): it fires on any failing audio command whose input carries >1 track and named no track, so an unknown `audio_codec`, or a missing output directory while writing `.mka`, still gets "Or keep all 3 by writing ... Matroska (`.mka`)" — advice the caller is already following (F1, scored 65, logged). Fixing it means gating on the output container's stream capacity or classifying FFmpeg's failure, both design calls M45 deliberately did not make. Alongside it, `ffmpeg_exit_status()` regex-scans `ffm_run()`'s formatted message, which also embeds user paths, where `run_program()` already returns a structured `status` attribute that `ffm_run()` discards — attaching it to the condition makes this a field lookup (F5, scored 52, logged). Promote on any report of the abort naming a cause that is not the cause. — added 2026-07-30 — M45 review F1/F5; D024; M45-D1
- ~~The subtitle fixture in `test-audio-stream.R` intermittently hangs FFmpeg forever~~ — promoted to M46 (2026-07-30): planning reproduced it (10 hangs in 25 runs under a 20 s limit, ffmpeg 8.1.2/macOS) and isolated the cause to `-shortest` **plus** a mapped subtitle stream — removing `-shortest` gave 0/15 and keeping it while dropping the subtitle map also gave 0/15, so the flag is the one line to cut and the lavfi sources were already `duration=`-bounded. M46 also takes the suite-wide half: a test-only timeout helper, since nothing in the suite bounds a hung FFmpeg. — added 2026-07-30, second observation 2026-07-30, promoted 2026-07-30 — found during M45 review; M43 ccfaf64; M46
- The package's own runtime path has no timeout: `ffmpeg()`, `run_program()` and `ffm_run()` all block forever on a hung FFmpeg, so a user hits locally exactly what M46 fixes for the test suite (M46 deliberately kept its timeout test-only). Base R suffices — `system()`/`system2()` both take `timeout=`, verified returning `status` 124 — so the work is not the mechanism but the exported-API call: which functions take the argument, what the default is (a silent ceiling would abort long legitimate encodes), and whether it aborts or warns. Needs a D-entry. Promote on any report of a package call hanging, or alongside the first verb that wants a per-call budget. — added 2026-07-30 — M46 Out
- `ffm_copy()` and `ffm_concat()` are no longer idempotent since M43 made `ffm_map()` append: a second `ffm_copy()` emits `-map 0` twice and duplicates every output stream (measured 2026-07-30: 1-video/1-audio input → 4-stream output), and `ffm_concat()` calls `ffm_copy()` internally so `ffm_concat() |> ffm_copy()` doubles too. No in-package pipeline does this; the exposure is user-composed Layer-1 chains. Fix is small (`unique()` on the appended maps) but needs tests over the composite verbs M43's `≤1 -map` guard never covered. Promote on any report of a doubled output, or when the `audio_stream`-carry milestone next touches `ffm_map()`. — added 2026-07-30 — M43 review F2 (scored 76, logged); M43-D1; D023
- A bad codec *token* still leaks Layer-1's argument name on the verbs whose pipelines call `ffm_codec()` directly instead of the `apply_video_codec()` / `apply_audio_codec()` seams: `extract_audio(audio_codec = "a c")` reports "`audio` must be a single clean token" blaming `ffm_codec()`, where `anonymize_video` names `video_codec` and blames the verb (measured 2026-07-30). M41 fixed this class for non-string *values* only; tokens were never probed. Routing `extract_audio_pipeline()`, `standardize_pipeline()` and `anonymize_pipeline()` through the two seams closes it and would also make D022's bullet 1 true, which names those seams as carrying the family rule though none of the three uses them. — added 2026-07-30 — M42 review F5 (scored 76, logged); M41 archive; D016/D022
- The dropped-audio-track check has no opt-out and its cost is all up front: every default-`audio_stream` call to `extract_audio`/`convert_audio` spawns an FFprobe process (0.041 s measured, against 0.187 s for `probe_audio()`), and the `_batch` verbs probe every unique input **serially, before `ffm_batch()`**, so the cost ignores `parallel = TRUE` and a large jobs table stalls before its first encode with no progress output. `suppressWarnings()` silences the message but does not skip the probe, and neither the four verbs' roxygen nor NEWS mentions the cost. Candidate shapes: a `check_tracks =` argument, an option seam, or probing lazily per row inside the fan-out — the last needs the `ffm_batch()` hook D024/RR02 Q3 rejected, so it would reopen that. Promote on any report of a batch stalling before its first encode, or on any request to turn the check off. — added 2026-07-30 — M44 review F4 (scored 72, logged); D024
- Video quality / rate-control knob (CRF↔CQ, `-preset p1–p7`, bitrate) — the package has no quality abstraction today; a cross-encoder mapping is opinionated + an irreversible-API commitment. Deferred from M31. — added 2026-07-26 — M31 Q4
- GPU *decode* / `-hwaccel cuda` input acceleration + GPU filter pipelines — needs a new engine input-options slot (none exists; only `seek_pre` goes before `-i`) + an IP2 filtergraph design call. — added 2026-07-26 — M31 Out
- Other hardware encode backends (videotoolbox/qsv/vaapi/amf) generalizing the `hardware=` arg beyond nvenc — needs a backend-detection + arg-vocabulary design call. — added 2026-07-26 — M31 Out
- CRAN readiness (release mechanics only): win-builder + R-hub, cran-comments, bump toward 0.2.0. Deliberately last. API-surface cleanup shipped as M23; the vignette pass is M30; a roxygen `@examples` pass remains the only open docs slice under this row. — added 2026-07-10, trimmed 2026-07-12, reconciled 2026-07-12 (M30) — see M22/M23/M30
