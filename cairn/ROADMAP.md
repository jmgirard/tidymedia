# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Migrated from `project/` on 2026-07-11 (adopt-in-place); pre-cairn history in git log._
_Last hygiene check: 2026-09-01 (M098 done: `DESCRIPTION` declares FFmpeg and MediaInfo in `SystemRequirements`, and `install_on_win()`'s default install dir moved to `tools::R_user_dir("tidymedia", "data")/ffmpeg`. `devtools::check()` 0/0/0, suite clean (18 skipped), CI green on all ten legs. Five review findings fixed on the branch; a sixth absorbed into the existing `install_on_win()` candidate row. M094's terminal row pruned to keep five. No lesson owed — M097's line already records the `R/<pkg>` component `tools::R_user_dir()` interposes. LESSONS and the doctrine modules unchanged and within budget; ROADMAP compressed to stay under its byte budget. No release window declared. Nothing retired.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M097 | A remembered binary location lives where CRAN policy sanctions | done | — | normal | milestones/archive/M097-cran-policy-user-dirs.md |
| M098 | `DESCRIPTION` declares the tools the package interfaces, and the data dir follows policy | done | — | normal | milestones/archive/M098-system-requirements-and-data-dir.md |
| M100 | Hardware encoding is a backend vocabulary, and videotoolbox is the second member | in-progress | M099 | normal | milestones/M100-videotoolbox-backend.md |
| M099 | D014's pre-0.2.0 rename window is reviewed before it closes | done | — | normal | milestones/archive/M099-d014-rename-window-review.md |
| M095 | A wrong argument outranks the nvenc availability probe | done | — | normal | milestones/archive/M095-argument-error-outranks-nvenc-probe.md |
| M096 | A wrong argument is refused by the verb, not by `purrr::pmap()` or after FFmpeg runs | done | M095 | normal | milestones/archive/M096-unguarded-argument-front-doors.md |

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- **Two gaps M095's review left in the codec seam it split.** **(a)** `apply_video_codec()` became `check_video_codec()` + `emit_video_codec()` (`R/ffmpeg.R:3211`, `:3220`) with nothing binding the halves — `emit_` without `check_` reintroduces M56's hole, since `resolve_hw_encoder()` rewrites the token before `ffm_codec()`'s `check_token()` sees it. All three sites are correct today, so the work is a static sweep pinning the pairing. **(b)** AC2's grid overwrites `video_codec` at every cell (`data-raw/nvenc-probe-order-baseline.R:132`), leaving the `NULL` sentinel branch outside the byte-identical evidence. Promote (a) alongside any milestone adding a call site for either half; (b) alongside one regenerating that grid. — compressed 2026-08-31 — M095 review F4, F6; M56 review F3; D016; D036
- **Two argument classes still refused below the verb**, from M096's census (437 of 1530 cells at HEAD). **(a) Gate booleans** `run`, `parallel`, `progress`, `manifest`, `checksums`, `verify` at the ~18 verbs reaching `ffm_finish()`/`ffm_batch()`: `run = 123` is blamed on `ffm_finish()`, and a set limit displaces even that; fixing it reopens D074 property 2 at eighteen front doors. **(b) `ffm_batch()`'s `output` column**, blamed on `purrr::pmap`: its contract names no column, so a guard invents one and changes the per-row shape D007 fixes. The census's other eleven arguments were ACCEPTED and moved to `DESIGN.md` Known issues. Promote (a) on a `run =` mistake blamed below the verb; (b) on a batch caller reporting the pmap blame on an `output` value. — 2026-08-31 — M096; D007; D074
- **`tm_outfiles_baseline()` records a merge-base table with no committed generator.** Its four constants in `tests/testthat/helper-timeout-sweep.R` carry provenance in a comment only — unlike M095's `data-raw/nvenc-probe-order-baseline.R` — and every cell is a value M096's guard passes, so nothing distinguishes a table recorded at `4063faa` from one at HEAD. M096's review closed the EVIDENCE gap by re-measuring against a worktree at that commit. Promote alongside any milestone touching `segment_video()`'s compiled output or re-recording that table. — compressed 2026-08-31 — M096 review F7; M095 AC2
- **`nvenc_available()`'s defaultless `call` cannot be asserted by a test.** M094 removed the default so a site forgetting to thread the verb's frame cannot inherit the helper's own (D074, `R/ffmpeg.R:3004`), leaving the property in a comment. Measured 2026-08-31: R's laziness never forces the promise unless an abort is built, so `nvenc_available("h264")` with no `call` returns silently either way. The work is a static sweep of the call sites, not a runtime assertion. Promote alongside any milestone adding a call site, or on the first site that forgets. — compressed 2026-08-31 — M094 review H8; D074; D042
- **Container names written by HAND, outside the generated enumeration.** Two EXEMPLARS naming `.mka` and `.m4a`: `audio_stream_extras$separation_container` (`R/audio-stream-doc.R:133-141`) and the cli messages at `R/ffmpeg.R:745` and `:859`; promote on the first REMOVAL from `multi_audio_extensions`, since an exemplar illustrates rather than claims the whole list. Plus the `.avi`/`.nut` COUNTER-EXAMPLES both separation help pages hand-write as containers the vector does NOT hold; promote on the first ADDITION of either, which would make each page contradict itself unnoticed. — compressed 2026-08-31 — M091 review F4 r3, F1 r4; M093 review F5; D069; D071
- **The SIGINT leftover from M088's failure-path findings.** A SIGINT reaching only the child could surface as a non-zero exit and spawn a second run instead of returning to the prompt — low confidence, never reproduced. The other four findings went to M090. M090's gate left this one here because an unreproduced Ctrl-C interaction has no bounded investigation and M078 is the precedent for what an unbounded one costs. Promote on the first report of Ctrl-C failing to return to the prompt. — compressed 2026-08-31 — M088 review pass 2 F3; D065; M078
- **`references/false-greens.md` sits 190 bytes under its own budget** (25,810/26,000, 55 of 60 lines); the module header's remedy is compress-or-retire, never growth. Promote on the module breaching either figure with nothing left worth compressing or retiring. — added 2026-08-29 — M089 review F2; M084; D067
- **The two shapes M082 left behind when it shipped the check's off switch.** **(a)** A per-verb `check_tracks =` argument, declined for good at M099 (D077, D078): the per-call form already exists twice and the per-row form is the `audio_stream` column; reopens only as a `jobs` column for rows naming no track, under a deprecation cycle after 0.2.0. **(b)** Probing lazily per row inside the fan-out so the cost parallelizes, which needs the `ffm_batch()` hook D024/RR02 Q3 rejected and so needs that entry superseded first; promote on a measured batch where the serial sweep, not the encoding, dominates wall clock. — added 2026-08-28, decided 2026-09-01 — M082 review F9; D014; D024; D047; D077
- **A `probe_all_impl()` argument refusal would name `infile`** through `verify_media()`, which has no such argument — a shipped message naming an argument its frame lacks. Unreachable today: `check_file_exists()` refuses first, which is why M70 logged it. M70's O11, classed **runtime** by M092's ledger. Promote on any change letting `verify_media()` refuse before the file-existence check. — added 2026-08-30 — M70 review O11; M092 ledger
- **The condition-class pairing test binds a claim to a help TOPIC, not to a SITE**, which is why the same over-attribution shipped green twice — `?tidymedia` and `?normalize_audio_batch` each naming `tidymedia_ffmpeg_exit` for `R/loudnorm_two_pass.R:112`. Strengthening it over the union of a topic's paired sites catches neither, since both also pair with `:151`, which does raise that class; binding a claim to a site is a design call of its own. M092's ledger prunes the finding as **instrument** and keeps the call plannable here. Promote on a third topic over-attributing a class. — compressed 2026-08-31 — M087 review p1 F5, p2 F1/F2; M092 ledger
- `with_timeout()`/`local_timeout()` may take minutes, not seconds, to interrupt a program blocked on input on Linux: `run_program()` bounds a spawn with `system2(timeout = )` (`R/program_management.R:125`), and an `ffmpeg` blocked opening a named pipe survives `kill -TERM`. **Measurement and the documentation correction shipped as M078; the FIX — separating stopping the program from closing the pipe — remains open.** **Corrected M078**: neither the minutes-scale interrupt nor the wedge reproduced in a rebuild of that runner; D056 and M078 carry every figure. Promote on the first report that the lag is itself the problem. — compressed 2026-08-31 — M077 gate; D055; D056; M078
- A permanent CI job that installs DESCRIPTION's declared `Imports` floors on every push, promoting M077's one-off `data-raw/imports-floors.R` into a standing check. M077's plan gate chose the one-off and declined this, because `setup-r-dependencies@v2` has no oldest-version input — its `dependencies` input selects dependency TYPES, not versions — so the job must be hand-rolled, and a standing job commits the repo to keeping ten floors green on every push, including whenever a floor version stops building on a newer R. Promote on the first floor regression that reaches a user between audits. — compressed 2026-08-31 — M077 Out
- **The two gaps D055 discloses and does not close: no floor run alone, and one operating system.** Nine one-floor-at-a-time legs would attribute a failure to its own floor rather than to the pinned set, and a three-OS run would say whether the floors hold off Linux. Both were declined at M077's plan gate on cost; M079 keeps the harness's `--only` mode alive so they stay plannable. Promote on a user reporting a failure on a declared floor the joint run does not reach — D055's own falsifier — or on any floor moving for a reason it cannot attribute. — compressed 2026-08-31 — D055 item 4; M077 gate; M079 Out
- A tighter kill than base R's `timeout=` gives. Base R escalates SIGINT at the limit, SIGTERM at +20 s and SIGKILL at +40 s, so a program ignoring the first two outlives its limit by up to 40 s — measured 42.0-42.1 s on Linux CI under a 2 s limit, which forced M69's AC3 from a 10 s bound to 60 s. `?system` calls termination not guaranteed, so the lag is base R's contract, not a wiring fault. M69's return-2 gate rejected `processx` because a bounded 42 s meets the Goal as a bounded 2 s does, and a new dependency plus rewiring four spawn sites was fresh risk; D056's M078 measurement leaves that rejection untouched. Promote on the first report that the lag is itself the reported problem. — added 2026-08-26, compressed 2026-08-31 — M69 return 2; D047; D056; M078
- Fixed-region *region blur* (no face tracking): split→crop→boxblur→overlay needs an IP2 filtergraph design call (new blessed composite verb vs Layer 0) plus a new `ffm_boxblur` filter; not plannable until that call. Box-fill half became M20/M21. Confirmed in-scope (defer) by M25. — added 2026-07-10, split 2026-07-12, reconciled 2026-07-13 — research-verbs family 4; M25 survey §3 D1
- `burn_timecode` / drawtext text-and-timecode burn-in for coders & reliability raters; in-scope but needs a new `ffm_drawtext` Layer-1 filter + a surface-scope call. — added 2026-07-13 — M25 survey §3 D2 (defer)
- Minor in-scope convenience verbs (grouped): split multi-view→per-person clips, orientation fix (rotate/flip), contact-sheet QC montage; each needs a small arg-shape design call. — added 2026-07-13 — M25 survey §3 D3 (defer-low)
- Two leftovers from the same M45 measurement, now that its video-after-failure half is **M088**. **(a)** A symmetric `video_stream` for `-map 0:v` — measured 2026-07-30, a two-video-stream input into `.mp4` carries both and no verb default writes video to a single-video-stream container, so promote on the first container that refuses. **(b)** `ffm_concat()` / `concatenate_videos()`, which map all streams unconditionally and fail the way D026 measured for `-map 0`; promote alongside any milestone touching either verb's map. — added 2026-07-30, narrowed 2026-08-29 — M45 Out; D026; RR03; M088
- **The multi-track separation abort is still blind to WHY FFmpeg failed**, for every cause the output container cannot reveal. **The container half shipped as M091**; D069/D071 and M091's archive carry the containers, the measurement rule and the causes left indistinguishable. What stays: **(a)** classifying the failure from FFmpeg's stderr, which `ffm_run()` does not capture (`R/program_management.R:105`) and capturing would stop the live console output; **(b)** a differential re-run with `-map 0:a:0` into a temp path — decisive, but the first probe here to execute FFmpeg and write a file, so it needs its own D-entry. Promote on the abort naming a cause that is not the cause. — added 2026-07-30, compressed 2026-08-31 — M45 review F1; M091; D024; D069; D071
- **The package's remaining unclassed aborts stay uncatchable by class** — 13 of 173 `cli_abort()` sites carry a `tidymedia_*` class (measured 2026-08-29). M086's criteria audit declined the sweep on two grounds: no grep enumerates the domain, since a non-zero status surfaces at every `run_program()` caller and not only where "ffmpeg" appears; and a multi-cause site cannot honestly carry `tidymedia_ffmpeg_exit` under D062's event rule, so this needs a naming pass, not one class. The M086-F5 naming half went to M087. Promote on a caller needing an abort M085/M086 left unclassed, or alongside a milestone giving the package a class hierarchy. — added and narrowed 2026-08-29 — M086 Out; M087; D062
- **`separate_audio_video_batch()`'s post-fan-out warning cannot carry an exit status, and cannot be classed as one.** `ffm_batch()`'s `run_one()` reduces each row to `list(success =, timed_out =)` and discards the condition (`R/ffm_batch.R:143-147`), so no status reaches the warning site; the warning fires for any failure cause, which D062 forbids classing as an exit; and `tryCatch()` on a warning unwinds the batch against D007, so the recipe stays `suppressWarnings(classes = )`. The fix changes `ffm_batch()`'s per-row result contract. Promote on a batch caller needing a failed row's status. — compressed 2026-08-31 — M086 Out; D007; D062
- **Per-call `timeout =` arguments** on the run-capable verbs and on `ffm_run()`/`ffm_batch()` — declined for good at M099 (D077, D078): a scalar cannot carry the per-row residual that justified it and would respell `with_timeout()`'s `seconds`. Reopens only as a `jobs` column carried on the pipeline object, for a limit varying per row within one batch, under a deprecation cycle after 0.2.0. — added 2026-08-09, decided 2026-09-01 — M69 Scope Out; D014; D044; D047; M071; M072; D077
- **Two lookup-memoization gaps M67 left open (one memo, two axes).** **(a) `find_ffmpeg()` is not memoized**, so one `Sys.which()` spawn survives per row after M67 collapses the `-encoders` probe; M67's gate rejected keying the memo on the resolved binary path, since `find_program()` prefers `Sys.which()` over `set_program()` and cannot see a config change while ffmpeg is on `PATH`. **(b) M67's memo is per-process**, invisible to `parallel = TRUE` workers: a W-worker nvenc batch probes W times where the sequential one probes once; D044 carries mechanism and magnitude. Promote (a) on a measured batch stall attributable to `Sys.which()`; (b) on a measured parallel batch whose probe count is the problem. — compressed 2026-08-31 — M67 Out; D033; D034; D044; M071
- Two `normalize_audio()` extensions the linear builder blocks (same blocker, IP2/D003 — one filter chain and one output). **(a) Per-track two-pass loudness normalization**, one measured set per mapped audio track: D028 records this as its own falsifier and carries the 3-track `.mkv` measurement. The blocker is the builder, not the parser. Promote on the first request to normalize more than one track. **(b) Normalize loudness while KEEPING the picture, in one call**, which M49/D030 removed; `keep_video =` reopens the container question D030 closed, so the likelier shape is a mux verb or a two-output pipeline. Promote on the first report of the escape hatch being needed here. — compressed 2026-08-31 — M49 Out; D003; D028; D030
- The compiled command's remaining bare token classes: codec names, pixel formats, seek values, and the raw output-option passthrough (`-movflags +faststart`, `-q:a 0`, `-f null`). D031 records these as deliberately unquoted — the first three are single clean tokens by their own validators, and the passthrough cannot express quoting at all: the `output_opts` branch of `ffm_groups()` hands `ffm_group()` a finished `display` string (`R/ffm.R:1332`), bypassing the `quote=` mechanism, so covering it needs a signature change. Promote on the first report of any of these breaking a pasted command. — added 2026-07-31, compressed 2026-08-31 — M50 Out; D031
- Renaming `audio_stream`, or unifying the two `NULL` readings — declined for good at M099 (D077), where `audio` became `audio_input`. `audio_stream` reopens for a Layer 2 verb that must select both an input and a track in one call; the `NULL` unification for a verb whose product can be one stream or many (a third family, not a merge); each under a deprecation cycle after 0.2.0, never on a user report. — added 2026-07-31, decided 2026-09-01 — M51 Out; D014; D025; D026; D032; D077
- `probe_all()`'s sequential (default) path materializes every file's probe before assembling, where the pre-M53 `for` loop let each raw result become garbage immediately: M53 replaced the inline `probe_one(f)` with a `purrr::map(infile, probe_one)` whose list is held for the whole call, so peak memory at `parallel = FALSE` rises for no benefit — only the parallel branch needs the materialized form. The magnitude is **unmeasured** (the review's "roughly doubling" was flagged speculative), so the work starts by measuring peak memory over a few thousand many-stream files, master versus post-M53. Promote on a measurement showing a real regression, or on any report of `probe_all()` exhausting memory. — compressed 2026-08-31 — M53 review F7; `R/ffprobe.R:92-117`
- A wrongly-**typed** value still answers differently depending on its form, where M61 made a wrongly-**valued** one uniform: `picture_in_picture_batch(jobs, margin = "x", audio_codec = "aac")` reports the contradiction, while the same `margin` in a `jobs` column reports the type error (measured 2026-08-08). A disclosed consequence of an intentional boundary — M61's Scope Out keeps every column *type* guard above the contradiction sweep, and D039 promises uniformity for a value error, not a type error. The work is whether a column's type check can split from its shape check. Promote on any report of a type mistake and a contradiction reporting differently by form. — compressed 2026-08-31 — M61 review r3 H11; D036; D039
- The normalize pair's copy/audio_stream form divergence (the batch row-locator half went to M66): `audio_codec = "copy"` and a fractional `audio_stream` report before a bad loudness target on the batch form and after it on the scalar (M65-D3's disclosure); uniformity means moving the batch's early copy/stream guards below its input sweep, reopening M34/M45's fail-before-Phase-1 placement. Promote on the first report of the divergence confusing a caller. — compressed 2026-08-31 — M65 review F1; D039; D043; M66
- **Two encoder-surface extensions deferred from M31** (each blocked on a design call the engine has no slot for). **(a) Video quality / rate-control knob** (CRF↔CQ, `-preset p1–p7`, bitrate): no quality abstraction exists and a cross-encoder mapping is opinionated and an irreversible-API commitment. **(b) GPU *decode* / `-hwaccel` input acceleration + GPU filter pipelines:** needs a new engine input-options slot (only `seek_pre` goes before `-i`) plus an IP2 filtergraph design call. **The third part — other hardware encode backends — was promoted to M100 on 2026-08-31.** Promote either part on the first request naming its knob or platform. — added 2026-07-26, narrowed 2026-08-31 — M31 Q4; M31 Out; M100
- **What M100 leaves out of the hardware surface.** **(a)** `qsv`, `vaapi`, `amf` each enter through M100's per-backend table and widened `hardware=` vocabulary — a table row plus its probe, no new design call — but none is testable on hardware this project reaches (the gap M094 measured for nvenc); promote on a request naming one, or on CI gaining a runner that has it. **(b) `prores` under videotoolbox**, the family nvenc lacks: `prores_videotoolbox` writes a 0-byte file into `.mp4` (measured 2026-08-31, detail in M100 Out) while `.mov` and `.mkv` take it, so it needs a container guard M100's table has no slot for; promote on the first request for ProRes output, or alongside any milestone giving the table a container axis. **(c)** a generic `has_encoder(name)` over the whole pool, additive beside `has_hardware_encoder()` (RR06 R11); promote on a caller needing to ask about a software encoder by name. — added 2026-08-31, (c) 2026-09-01 — M100 Out; M31 Q4; D044; M094; RR06
- **Two things `install_on_win()` still gets wrong.** **(a)** It downloads and installs third-party software with no confirmation — a 7z from a hardcoded gyan.dev URL, extracted into a persistent user directory (`R/program_management.R:227-241`); the behaviour a CRAN reviewer is most likely to question, and the `\dontrun{}` guard keeps it out of `R CMD check` so nothing flags it. M097 and M098 moved where it writes and left what it does alone; the fix is an interactive confirmation, which changes an exported function's behaviour. Promote on a CRAN submission that draws the comment, or on a report of the function surprising a caller. **(b)** Its `@description` and `@param download_url` both say "zip" where the default is a `.7z` — docs-only, rides (a) or any milestone touching the function. — added 2026-08-31, (b) 2026-09-01 — M098 Out; M098 review F9; D001
- **Five `data-raw/` guard generators still spell `audio =`** (`value-guard`, `input-guard`, `contradiction-guard`, `codec-guard`, `blame-precedence-m65`; 20 sites), the name M099 retired on the fan-in verbs. A pinned-ref run is unaffected and none writes a committed fixture, but a `ref = NULL` run records R's partial-match error in the audio cells that exist to measure a guard. The fix spells the cell by ref, since the pinned refs need the old name. Promote alongside any milestone re-running one of them against the working tree. — added 2026-09-01 — M099 review F4; M096; M061
- CRAN readiness (release mechanics only): win-builder + R-hub, `cran-comments.md`, bump toward 0.2.0. Deliberately last, and **no window is declared** — release timing is the maintainer's to call. Measured 2026-08-31: `devtools::check()` is 0/0/0 and the suite is clean with no binaries on `PATH` (287 skipped, 206 s). The docs slice this row used to name is closed — 78 of 81 topics carry `\examples`, the three without being concept topics. Policy defects found at that audit went to **M097** (config dir, shipped 2026-09-01) and **M098** (`SystemRequirements`, data dir); D014's pre-0.2.0 rename window is **M099**, and shipping 0.2.0 closes it. Promote on the maintainer declaring a window. — added 2026-07-10, reconciled 2026-08-31 — M22/M23/M30; M097; M098; M099; D014
