# M080: The guard says what is wrong, and refuses NA

**Status:** done (2026-08-28, PR #84 https://github.com/jmgirard/tidymedia/pull/84)

**Goal:** The four defects in SHIPPED behavior carried by the M62/M63/M64 finding row — an `NA` that crashes bare, an `NA` that compiles, an abort naming a carrier that is correct, and a duplication guard that hides the typo under it.

**Outcome:** `check_dim()` tests `!anyNA(x)` ahead of both halves of its predicate at its one site, refusing all four NA types at its existing wording — closing both the `NA_real_` bare `simpleError` and the `NA_character_` passthrough that compiled `crop=w=NA:h=100:...`. A sweep over the namespace's one-required-formal `check_*` predicates (15 names, derived by formals, never listed) reddened three more: `check_overlay_scale()` reuses its range wording, `check_region_values()` re-calls `check_regions()`, `check_codec_needs_reencode()` takes `rlang::check_bool()`. `check_batch_inputs()` filters `col` to the carriers holding an unreadable path before calling `check_paths_readable()`, leaving D041's one predicate and one abort site untouched. The three inline duplicated-input aborts became `reject_duplicate_inputs()`, and in `anonymize_video_batch()`, `standardize_video_batch()` and `normalize_audio_batch()` the input sweep now reports above it. Test domains derive from `tm_call_graph()` and `ls(asNamespace())` with a completeness reader over declared carriers; `data-raw/input-guard-baseline.R` gained a `derived_output` crossing plus `column_type:stream` and `scalar_arg`.

**Decisions:** D057 (the derived-output duplication refusal reports after the input sweep), D058 (the sweep is never lifted past a check that already sat above it, carving out that refusal).

**Review:** Three-lens fan-out, three rounds; two defect returns, both on AC6 — NEWS.md's ordering paragraph not describing the shipped order, understating the change in round 1 and overstating it in round 2. Thrash trigger (b) fired at return 2; the recorded alternative (revert the reorder, amend NEWS instead) was offered and declined. Round 3 verified AC1–AC6 fresh and returned nine findings. N1–N5 fixed at the gate: D058's rule carved out for the guard D057 relocates, its `scale` cell corrected to a per-CHECK split, an M42→M41 citation fixed, the `check_batch_*_col()` glob replaced by the three type guards, the grid's `scalar_arg` comment narrowed. N6, N7 and F5–F8 extended onto the M62/M63/M64 row at a §7 disposition; F4, F9, F10 and F11 promoted out of it as a candidate.
