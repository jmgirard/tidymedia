# M083: The ROADMAP gets back under its byte budget

**Status:** done (2026-08-28, PR #87 https://github.com/jmgirard/tidymedia/pull/87)

**Goal:** Bring `cairn/ROADMAP.md` under its 24,000-byte budget by entombing the instrument-findings rows, pruning the already-promoted ones, and compressing what stays.

**Outcome:** 42,552 bytes over 53 lines → 22,256 over 46; 33 candidate rows → 26. Three mechanisms, in that order. (a) The five rows grouping deferred review findings on the repo's own test instruments (M62/M63, M70, M071, M079, M081 — 13,303 bytes) moved verbatim into `cairn/references/instrument-findings.md`, indexed under Working artifacts, behind one grouped row with a shared promote-on clause. (b) The three struck-through fully-promoted rows deleted, after folding the retired M44 row's (a)/(b) promotion conditions verbatim into the M082-leftovers row. One fact those bodies held alone, recovered at review: M080's **F10 was withdrawn as not a defect** because `rlang::caller_arg()` resolves at the guard's own frame, so `check_bool(reencode, call = call)` already aborts naming `` `reencode` `` — the row had claimed it would render `` `reencode_rows[[i]]` ``. (c) The eleven surviving rows over 1,000 bytes compressed to hook + promote-on + trailer, 16,706 → 11,124 bytes, each fact checked against the entry, archive or review it cites and kept in the row where no cited file held it. Every surviving row keeps its `— added` trailer, and every one that carried a promote-on clause still does.

**Decisions:** none cross-cutting. The plan gate chose entombing over pruning the instrument rows (a pruned finding surfaces at no later plan gate); the implement gate chose checking each row's citations before dropping narrative.

**Review:** One [O] reviewer (internal tier, markdown-only diff) re-derived all five criteria independently and agreed. Twelve findings, none reaching the return floor. Six fixed on the branch: the F10 rationale above carried here; the scaffolding page `roadmap-candidates-baseline-M083.md` deleted with its index line; a "candidate row below" reference repaired by a note outside the verbatim rows; the dropped `corrected M078` marker restored; `R/ffmpeg.R:882-891` corrected to `911-912`; the timeout domain's 32 corrected to 31 (measured: 53 members, 31 take `run =`, 22 do not). Six rejected: four stale plan-time figures in text this archive replaces, one append-only work-log figure accurate when written, one blank-line nitpick. Nothing graduated or retired.
