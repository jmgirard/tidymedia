# M087: A diagnostic answers to the same class from the scalar verb and its batch sibling

**Status:** done (2026-08-29, PR #91 https://github.com/jmgirard/tidymedia/pull/91)

**Goal:** A caller who writes a condition handler from a documented class name gets that class from a verb's scalar form and from its `_batch` form, or the documentation states where the two differ and why.

**Outcome:** The `loudnorm` analysis pass raises one shared event class, `tidymedia_loudnorm_no_measurement`, at all three of its abort sites — `R/loudnorm_two_pass.R:112` (scalar, FFmpeg exits zero and prints nothing parseable; previously carried no class at all and was uncatchable), `:151` (scalar non-zero exit, adding `tidymedia_ffmpeg_exit` and `tm_status`), and `:253` (batch, carrying `tm_rows`/`tm_row_status`). `tidymedia_loudnorm_analysis` is retired; the silence abort at `:103` is deliberately left outside the shared class, since a silent input *was* measured at `-inf`. The multi-track separation pair (`R/ffmpeg.R:681` error, `:742` warning) is unchanged and its severity asymmetry recorded instead. Six help topics, `NEWS.md` and `?tidymedia`'s vignette pointer corrected; the pointer now renders outside every `\section{}`.

**Decisions:** D063 (the class-vector rule D062 left implicit: a site's vector asserts every fact established *at that site*, so vectors at two severities of one event may differ by extra classes but never by the shared event's own name; D007 is what forces it) and D064 (the rename and the chosen name). Milestone-local decisions from RR05 are in the merged file's `## Decisions` section; RB05/RR05 are archived under `cairn/reviews/archive/`.

**Review:** Two passes, three-lens fan-out each (user-facing tier). Pass 1 returned the milestone on AC4 — `?tidymedia` attributed `tidymedia_ffmpeg_exit` to a path that never raises it — with F2/F3/F8/F9/F10 riding the return as fix-now work; one defect return. Pass 2 verified all six criteria against a fresh review-side probe that executed every site, and found no criterion failing. Four findings were fixed at the gate: three false *reasons* stated beside correct class names (`?normalize_audio_batch`, `?ffm_run`, `NEWS.md`) and AC1's sweep guard, which ran `git grep` from testthat's working directory and so only ever searched `tests/testthat`. That guard fix then reddened all four Ubuntu CI jobs — `R CMD check` runs from a copy that on CI sits inside the workspace checkout — and was repaired to anchor on the checkout's own top level. Three test-strength findings were routed to `cairn/references/instrument-findings.md` at this hygiene pass under its §7 disposition, and three more pruned there. Retired at hygiene: the M39 `@param jobs` enumeration lesson (ownership — tracking-rules' derived-claims rule covers it) and the M36 prelude-hoisting lesson (stalest, pruned for budget).
