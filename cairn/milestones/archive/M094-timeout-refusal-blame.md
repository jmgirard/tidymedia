# M094: An invalid `tidymedia.timeout` is refused by the function the caller typed

**Status:** done (2026-08-31, PR #98 https://github.com/jmgirard/tidymedia/pull/98)

**Goal:** `options(tidymedia.timeout = <invalid>)` made 47 of the 53 exports in `tm_timeout_domain()` abort naming a function the caller never typed; the refusal now fires from the frame that can name the caller.

**Outcome:** Every timeout-domain export re-calls `resolve_timeout()` at its own front door — D042's rule, sited by D074: as late as the verb allows but never after a probe or spawn, above the `run` gate, never on a path that reads no limit. `nvenc_available()` is `has_nvenc()`'s body with `call` threaded, so the one capability probe running while a command is BUILT refuses in the verb's name; `check_path_vector()` (`R/utils.R`) is the path check the five `get_*` scalars and `resolve_probe()`'s infile branch hoist above their re-call. Measured at review: 265 base + 95 variant + 265 axis cells, 0 wrong-blame; one message per invalid form across all 53, identical to the checker's own; 106 return-value/spawn comparisons against a recorded `ae5ff1c` fixture, 0 mismatches; 22 abort / 31 warn under a reached limit, 0 mismatches. Two calls read no limit and refuse nothing: `has_nvenc()` under a set `tidymedia.nvenc_encoders`, and a `probe_*()` shortcut handed a `probe` object.

**Decisions:** D073 (the option rollback under a sequential plan is `future`'s, not the carrier's) and D074 (the siting rule, the `run = FALSE` half, why it leaves D024 untouched).

**Review:** Three rounds, three-lens fan-out each. Round 1 returned on AC7 — CI red on macOS and Windows, two members reaching `run_program()`'s `Could not locate` check before the refusal — carrying F1–F10. Round 2 returned by maintainer judgment on G1: nine exports where the limit displaced the caller's own argument error, F1's class at untouched verbs; G2–G8 carried. Round 3 passed every criterion with fresh evidence, CI ten-for-ten. Of its eight findings, H4 (a nine called eight), H5 (a carve-out property stated two ways), H6 (a `$` partial match in a falsification test) and H1/H3's documentation half were fixed at the gate: `NEWS.md`, `?tidymedia`, D074 and `R/timeout.R` stopped promising a wrong argument is always reported first, and now name the two classes where it is not — a check below a probe the verb must make first, and one inside the per-row fan-out. H1/H3's code half, H2's cross-product instrument, H7 and H8 went to one candidate row.
