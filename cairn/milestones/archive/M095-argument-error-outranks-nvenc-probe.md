# M095: A wrong argument outranks the nvenc availability probe

**Status:** done (2026-08-31, PR #99 https://github.com/jmgirard/tidymedia/pull/99)

**Goal:** `standardize_video(hardware = "nvenc", pixel_format = "bad fmt!")` was told the nvenc encoder is missing instead of that its pixel format is malformed; the machine decided which error the caller saw, which is what D036 removed at the front door and never reached the builder.

**Outcome:** `apply_video_codec()` split into `check_video_codec()` + `emit_video_codec()` (`R/ffmpeg.R`), so a pipeline can validate the codec token where the combined call sat and resolve the encoder later. `standardize_pipeline()`, `format_for_web_pipeline()` and `anonymize_pipeline()` sink the resolution — and with it `resolve_hw_encoder()`'s `fallback` check and the `resolve_timeout()` read inside `nvenc_available()` — below every machine-independent check, so both displacements D074 property 1 measured are fixed at once. The instrument is `tm_nvenc_wrong_arg_cells()`/`tm_nvenc_sweep()` (`tests/testthat/helper-timeout-sweep.R`), computing its own domain off `tm_timeout_domain()` ∩ `hardware`-carrying formals crossed with five wrong forms, plus `data-raw/nvenc-probe-order-baseline.R`, which regenerates the contract table from any git ref. Measured at review: 730 cells, 496 kept and 234 dropped by measurement with each refusing frame named, 0 mismatches under both mocked encoder pools and under each of the five invalid limit forms, against 27 at the merge-base `b538e63` in the same harness; 1,848 contract rows at both refs, 203 compiled and 1,645 refused on each, 0 differences, every compiled command byte-identical.

**Decisions:** D075 (a build-time capability probe runs below every check that cannot depend on its answer; supersedes D074 property 1's disposition of that class, leaving D074's per-row fan-out class in force).

**Review:** One round, three-lens fan-out. All six criteria verified with fresh evidence; no defect return, no amendment return. Blame-history and prior-review lenses: no findings. Diff lens: eight. Five fixed at the gate — the corrected disclosure still overclaimed, promising the fix reached checks the per-row fan-out makes (`anonymize_video_batch()`'s `pixel_format` and `color` still report the missing encoder), and said nothing about `fallback`'s descent; `tm_nvenc_mismatch_master()` was asserted only by a length test nothing could fail; the dropped-cell comparison ran over de-duplicated strings with the 496/234 counts unasserted; and one pre-M095 sentence in `standardize_pipeline()`'s comment was stale. Two went to a candidate row (the AC2 grid never exercising the `NULL` codec sentinel, verified not a defect; and no invariant binding the new seam's halves, which alone would reintroduce M56's hole). One rejected: D074 cannot gain a forward pointer under IP4, and the bounded-`DECISIONS` back-reference is the mechanism that surfaces D075. Hygiene: `ROADMAP.md` found 2,852 bytes over its budget, an overrun M094's stamp had recorded as inside it; the six widest candidate rows compressed to clear it. Nothing retired.
