# M106: The codec seam's halves cannot be called apart, and the probe grid exercises the codec sentinel

**Status:** done (2026-09-04, PR #110 https://github.com/jmgirard/tidymedia/pull/110)

**Goal:** Make `emit_video_codec()` validate its own token before it asks the FFmpeg build anything, so no future pipeline can emit a codec it never checked, and cross the `video_codec = NULL` sentinel into the probe-order grid so that promise is measured over the sentinel too.

**Outcome:** `emit_video_codec()` (`R/ffmpeg.R:3402`) calls `check_video_codec()` as the first line of its body, above `resolve_hw_encoder()` — M095 left the emit half separately skippable, so a pipeline reaching it unchecked got "that encoder is not available" about a string that was never a codec name. `apply_video_codec()`'s own call stays and is now idempotent with it. `tests/testthat/helper-hw-encoder-ledger.R` + `test-hw-encoder-ledger.R` compute the resolver's call-site domain by `all.names(body(f))` over the namespace and check each site's stated disposition (literal / checked-above / emit-half) against its own parsed body in source order; `format_for_web_pipeline`, `anonymize_pipeline` and `emit_video_codec` are the three members. `data-raw/nvenc-probe-order-baseline.R` crosses `video_codec` as a fourth dimension (caller value / `NULL` sentinel / `absent`), 1,848 → 3,040 rows, and drops that argument from the wrong-form cell set so a wrong form cannot overwrite the crossed value. `test-codec-seam-bound.R` (20 cells: 5 wrong forms × 2 backends × 2 pools, each at 0 resolver probes) and `test-codec-sentinel-hardware.R` (the sentinel's three hardware behaviours) are new. No shipped call path changes behaviour — every verb already checked above the seam — so no `NEWS.md` entry; the 0-row wide grid diff between merge-base and head is that claim's measurement.

**Decisions:** none.

**Review:** Three-lens fan-out (user-facing tier). Blame-history and prior-review lenses: zero findings — M095's archived review logged both gaps this milestone closes, and no inline PR review comments exist anywhere in the repo. Diff-bug lens: eleven findings, none firing the return floor. Fixed at the gate, comments and whitespace only: the stale "27 rows" figure's scope, a sentinel test's overclaiming comment, the crossed loop's indentation. To candidate rows: the grid's dropped wrong-form `video_codec` cells; `apply_video_codec()`'s redundant check and its wrong recorded rationale; the ledger's positional rather than dataflow checked-above test; the mocked pool literal in three unbound places. Rejected with reason: a condition-class assertion (measured — all five refusals and the availability abort alike are `rlang_error`, so class discriminates nothing the message does not); the internal-only refusal reorder; a test nitpick; the closure/`do.call()` domain gap, which AC2's fixed mechanism puts outside the criterion.
