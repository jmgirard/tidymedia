# M107: A backend with no encoder for the codec's family is refused by the verb the caller typed, `fallback` or not

**Status:** done (2026-09-04, PR #111 https://github.com/jmgirard/tidymedia/pull/111)

**Goal:** An out-of-table `(backend, codec family)` pair is refused by the verb the caller typed on both `fallback` arms, and the encoder pools the codec seam's instruments mock stop being nvenc-only literals.

**Outcome:** `check_hardware_available()`'s family sweep moved ABOVE its `fallback = TRUE` early return (`R/ffmpeg.R`), so the `(family, backend)` table lookup runs on both arms through `hardware_encoder()` while the build-dependent `hardware_encoder_available()` probe below still returns early. An out-of-table pair — `(nvenc, prores)`, `(videotoolbox, prores)`, `(videotoolbox, av1)` — and an unmappable `video_codec` token now name the calling verb instead of `purrr::pmap`; 24 of 84 AC1 cells were red before the fix, all at the 8 fan-out members. `tm_hardware_encoder_pools()` (helper-timeout-sweep.R) derives three pool levels from `hardware_backend_families()` and is read by `tm_nvenc_encoder_pools()`, `seam_pools()` and `nvenc_order_pools`, so no encoder triple is spelled literally in a test or generator. `tm_nvenc_unmappable_codec()` added as its own helper rather than a sixth `tm_nvenc_wrong_forms()` entry, which would have moved four recorded tables and five counts. The probe grid crosses `hardware_backends()` × three pool levels (3,040 → 6,840 rows) and `nvenc-probe-order-merge-base.rds` was re-recorded forward at `96e973b`, its provenance now in the script header.

**Decisions:** D085 (an out-of-table pair is a wrong argument, not an absent encoder, so it is sited at the front door on both `fallback` arms; annotates D035, D074, D076).

**Review:** Three-lens fan-out (user-facing tier). [S] blame-history and [S] prior-review: no findings. [O] diff-bug: nine. Six fixed on the branch — the `fallback = FALSE` arm's message DOES change for a multi-family list (measured against master side by side; the new precedence is D075's, the NEWS claim was what was wrong), T2's replacement comment was false in both new claims, the refusal loses `In index: N` and the earlier rows' "falling back" messages, `$nvenc` → `[[`, and a re-hard-coded `expect_length(..., 3L)` derived. Two rejected (a pre-existing generator property; a stale AC3 tick). One routed to a candidate row: the AC1 sweep reaches only the scalar `video_codec`, never the jobs-column list branch, which one hand-written cell still binds. No return floor fired.
