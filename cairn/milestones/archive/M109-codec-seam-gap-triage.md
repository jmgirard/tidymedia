# M109: The codec seam's four instrument gaps are triaged under D072, and the two with a path to a user are closed

**Status:** done (2026-09-05, PR #113 https://github.com/jmgirard/tidymedia/pull/113)

**Goal:** Apply D072's rule to the four codec-seam instrument gaps M106 and M107 left as a candidate row: close the two whose gap lets a defect in shipped behaviour reach a user, and prune the other two with their reasons recorded.

**Outcome:** No `R/` change; the deliverable is two test instruments and a ledger. `nvenc_order_cells()` (`data-raw/nvenc-probe-order-baseline.R`) gained a `video_codec/<form>` cell class carrying the wrong form as the cell's own value beside the `caller`/`sentinel` cross rather than inside it, and the spec flag the runner reads was renamed `has_vc` -> `cross_vc`; the grid went 6,840 -> 7,200 rows and `nvenc-probe-order-merge-base.rds` was re-recorded at the ref it already carried (`96e973b`), every prior row reproducing with the same kind, outcome and blamed frame. `oot_forms()`/`oot_jobs()` (`tests/testthat/test-hardware-out-of-table-blame.R`) run each reachable jobs-taking member's out-of-table cell in a two-family jobs column as well as the scalar form — 84 scalar + 42 jobs cells — with the duplicated row given distinct output paths so no cell refuses for colliding outputs before reading the codec. Both closes are demonstrated by a planted defect: removing `check_video_codec()`'s token refusal moves 18 grid rows, narrowing `check_hardware_available()`'s family loop to `families[1]` reddens 21 of 126 sweep cells and no scalar one.

**Decisions:** The four-gap ledger triaged under D072 — (a) wrong-form `video_codec` cells and (d) the `is.list()` arm reached only through the scalar form both class `close`; (b) `apply_video_codec()`'s duplicate `check_video_codec()` and (c) `tm_hw_encoder_checked_before()`'s positional reading both class `prune`, each grading an artifact measured correct today. A superseding correction records that the wrong-form cells capture a refusal MESSAGE, never compiled bytes: all 360 rows are aborts.

**Review:** Three-lens fan-out (executable surface touched). 13 findings, all logged with dispositions. F1 (gap (a)'s "compiled bytes" claim) fixed now by the superseding correction. F2 (a rename guard and its control keyed on the same value) had a fix written, measured not to close the named blind spot, and reverted; rejected with that measurement recorded. The other eleven rejected as not live, unreachable, pre-existing, linter-class, or out of scope. No criterion moved. Blame-history and prior-PR-comments lenses found no regression; the GitHub inline-comment probe returned empty, and PR #113's own reviews, comments and threads were all empty at the gate. At hygiene, M60's renormalization half was retired (a completed one-time repo event) to make room for M109's jobs-column-duplication lesson; the squash-SHA trap stays.
