# M114: Verification, provenance and timeouts are taught in prose, not only on a reference page

**Status:** done (2026-09-06, PR #118 https://github.com/jmgirard/tidymedia/pull/118)

**Goal:** Give `verify_media()`, `ffm_manifest()` and the timeout surface the narrative documentation they never had — an advertised pkgdown pillar and a session-blocking hazard, both reachable only from reference pages.

**Outcome:** `vignettes/verification.Rmd`, "Checking results and bounding runs", plus its `_pkgdown.yml` `articles:` row and a `workflow.Rmd` cross-link. Teaches `verify_media()` (structural not perceptual; tolerance 0.1 on numerics, exact on strings; a missing property fails as `NA`; extra fields resolve container → video → audio), `ffm_batch(verify =)`'s non-aborting `verified` column, `ffm_batch(manifest = TRUE)` / `ffm_manifest()` / `checksums = TRUE` and the "no manifest attached" abort, and `with_timeout()` / `local_timeout()` with the bound base R actually gives — the limit plus up to 40 s per D056, not the limit. Three developer scripts under `tools/`: `vignette_chunk_guards.R` (is a chunk guarded at all — knits twice, the second pass on a PATH reaching none of the three programs), `vignette_chunk_program_identity.R` (is it guarded on the RIGHT program — traces each chunk's spawns against its guard expression), `build_vignettes_without_binaries.R` (builds on a pandoc-only PATH with the remembered-location config seam redirected, asserting `find_*()` all `NULL`).

**Decisions:** none.

**Review:** two passes. The first returned on AC1 (`ffm_batch(manifest = TRUE)`'s output rendered nowhere) and AC4 (the build ran with FFmpeg still reachable through `find_program()`'s config-file fallback), with 15 findings. The second passed all six; its [O] lens showed AC3's promise false — `extract_audio()`'s dropped-track check starts FFprobe, so three chunks guarded on FFmpeg alone started an unnamed program, one pre-existing at `tidymedia.Rmd:39`. Fixed, and the program-identity script added to verify what the guard sweep structurally cannot see. Also fixed at the gate: the D056 two-platform overclaim, an unmet "as below" promise, three false positives in the spawn matcher, a console-width claim, NEWS section order, the build script's undisclosed install side effect. One rejected (two `error = TRUE` chunks cannot fail the build — the sweep measures both at 0 spawns). Six follow-ups merged into one candidate row. Defect returns: 1.
