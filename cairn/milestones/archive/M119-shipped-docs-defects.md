# M119: The documentation defects the pre-CRAN audit measured are corrected

**Status:** done (2026-09-09, PR #123 https://github.com/jmgirard/tidymedia/pull/123)

**Goal:** The six shipped-documentation defects measured on 2026-09-07 are corrected, and building the vignettes writes nothing into the source tree.

**Outcome:** `vignettes/tidymedia.Rmd:43` said a task verb "returns the path it wrote"; it returns the compiled FFmpeg command, invisibly under `run = TRUE`, and the prose now says so. That vignette's setup chunk gained `root.dir = tempdir()` (verification.Rmd's pattern) and the untracked `vignettes/audio.m4a` it had left behind was removed. `find_program()` left the documented surface via `@usage NULL` on its roxygen block, `@param program` going with it and `_pkgdown.yml:127` listing `find_ffmpeg` instead; `\alias{find_program}` stays, so `?find_program` and the package's `[find_program()]` cross-references still resolve. `.github/SUPPORT.md`'s stripped template placeholders now name tidymedia, with the two tidyverse claims rewritten for a single maintainer. `urlchecker::url_check()` was run for the first time: no dead URL, and the redirecting Contributor Covenant link (`README.Rmd:218`) plus one table-of-contents FFmpeg anchor (`R/ffm.R:261`, `#toc-crop` → `#crop`) were repointed at their terminal targets. `inst/CITATION` was added, reading year and version from `meta` at `citation()` time so neither goes stale.

**Decisions:** none cross-cutting. Milestone-local: unexport-from-docs over exporting `find_program()`, under GP1 and D014; the `#toc-` comment block at `R/ffm.R:1678-1699` left alone as source, not shipped documentation.

**Review:** full three-lens fan-out (user-facing tier, `R/` touched). Blame-history and prior-review lenses: zero findings, the latter reporting no prior-review evidence on the touched files and an empty inline-comment probe. Diff-bug lens: 13 findings. Five fixed on the branch, each a claim the branch had itself added — the release note saying the citation carries the ORCID (`print.bibentry` drops `person()`'s `comment`, so it never renders); the release note saying vignette building no longer leaves media beside the sources (it never did for users); the vignette's "every task verb ... returns the command it ran", falsified by `separate_audio_video()` returning two; the vignette's wrong cause for the silent chunk (it is `eval`-guarded, so a CRAN reader sees no output for a different reason); and the orphaned `@return` on the find-a-program page. A stale ROADMAP clause went with them. Three deferred: eleven `find_program()` cross-references surviving in shipped help prose, a NEWS section calling it both user-facing and internal, and README.md's embedded session temp path (the last two verified pre-existing at `origin/master`). Four rejected as out of scope. PR conversation: no reviews, no unresolved threads, one codecov bot comment reporting coverage unchanged at 98.43% — noted, requests nothing. All ten CI legs green.
