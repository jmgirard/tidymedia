# M120: NEWS.md reads as release notes

**Status:** done (2026-09-10, PR #124 https://github.com/jmgirard/tidymedia/pull/124)

**Goal:** The development section of `NEWS.md` reads as one set of release notes rather than an append-only per-milestone log.

**Outcome:** The development section went from 1,982 lines under 27 `##` headings to 1,391 under six — Breaking changes, New features, Bug fixes, Performance, Documentation, Requirements. The 13 one-off announcement headings and the Configuration, Standardized names, Verification & provenance, Multi-input verbs and Safe execution headings dissolved into those six, and same-feature bullets merged: four loudnorm headings became one two-pass entry, twelve "reported against the function you called" bullets became one entry with a checks list, eleven scattered `audio_stream` bullets became two. All 40 exports added and all 14 removed since `4b04fad9` are named (`strip_metadata`, `strip_metadata_batch` and `concatenate_videos_batch` were missing and were filled from their roxygen), and `acodec` -> `audio_codec`, `ts_start`/`ts_stop` -> `start`/`end` are each named with their replacement. Eight stale function names in 19 mentions were corrected, one of which (`write_mediainfo_template()`) named no function anywhere in the package. The four released sections below the line are byte-identical.

**Decisions:** D091 — the changelog announces only what a caller of the last release could have written; a name that arrived and changed inside the same development cycle is dropped, not reported as a breaking change. Milestone-local: intra-cycle renames dropped rather than re-filed; AC4's domain rebased from three hand-picked decision entries onto a formals diff over the functions exported at both commits, after a criteria re-audit found a recall-fixed enumeration.

**Review:** two passes, full three-lens fan-out each (user-facing tier). One amendment return (AC4, narrowed to renames a 0.1.0 caller could have written) and one defect return. First pass: 12 [O] findings, ten branch-introduced defects in the new prose plus a `devtools::check()` NOTE on `centre`, a value the code rejects — returned at the user's choice, fixed as T5-T16, then a claim audit (300 claims, 8 corrected) and its re-read added T17-T23. Second pass: all five criteria pass with fresh evidence, AC1-AC4 each against a discriminating control; `devtools::check()` 0/0/0. 10 [O] findings — seven fixed on the branch at the user's direction (the "shifts one position" migration claim, false for four of the seven verbs it named, rebuilt as a measured table; eight exported Layer 1 builders called "internal"; a crossed argument list; "copies every stream by definition", which the compiled `-map "0:v?" -map "0:a?"` falsifies; an unconditional `-codec:a copy`; and two D091-class bullets deleted), two rejected as out of scope, one refuted by running the call — FFmpeg does print `Stream map '' matches no streams` verbatim. Blame-history and prior-review lenses: no regression. PR conversation: no reviews, no unresolved threads, one codecov bot comment — noted. All ten CI legs green.
