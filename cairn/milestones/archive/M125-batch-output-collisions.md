# M125: Batch runs refuse two jobs writing one output

**Status:** done (2026-09-11, PR #129 https://github.com/jmgirard/tidymedia/pull/129)

**Goal:** A call that fans jobs out through `ffm_batch()`, or a direct `ffm_batch()` call, refuses before any job runs when two of its jobs would write the same output path.

**Outcome:** `check_distinct_outputs()` (`R/ffm_batch.R`) is the shared collision abort, reached through `reject_duplicate_outputs()` by the task verbs; it compares text, non-NA paths exactly and leaves other outputs to `ffm_files()`'s type refusal. New front-door calls, each placed after the verb's other value checks and above its first program start, on `standardize_video_batch()`, `normalize_audio_batch()`, `anonymize_video_batch()`, `segment_video_batch()`, `extract_frame_batch()` and `segment_video()` (`outfiles` derived above the nvenc probe); `strip_metadata_batch()`'s inline copy now calls the helper. `ffm_batch()` refuses repeated pipeline outputs after `.f` runs and before any job, on both `run` values, exempting outputs that write no file via `writes_no_file()`: `-`, `pipe:` URLs, and `null` after the last `-f` among the output-option tokens. `test-batch-output-collision.R` holds 30 cells over the 16 exports reaching `ffm_batch()` (domain from the call graph, completeness-checked), each with a control, under both `run` values; 16 export cell tests and 7 `ffm_batch()` tests fail against `master`'s code. 16 help pages, `ffm_batch()`'s details and NEWS state the refusal.

**Decisions:** Plan gate: direct `ffm_batch()` refusal with no-file exemptions; exact-text path comparison; sweep every collision form. Implement gate: each new check last before the verb's first program start. No DECISIONS entry.

**Review:** Fresh evidence for AC1-AC6 (test 0 failures, check 0/0/0; CI 10/10 green). Fan-out: blame-history and prior-review lenses found nothing; diff-bug lens reported 13. Fixed at the gate: a numeric or NA `output` column hit a bare cli error or read as a collision (O4), and `segment_video()`'s help said "file name" for a path comparison (O5). Follow-ups: collision now reports ahead of the hardware check's machine-free part, the timeout check and two-pass knob checks (guard-ordering candidate row, item c); `-f null -` inside option tokens and unexempted `/dev/null`/network outputs (output-collision limits candidate row, items b and c). Refuted by execution: a `.mp4`/`.mp4.mp4` derived-name clash. Seven rejected with reasons. No returns. No lessons added or retired.
