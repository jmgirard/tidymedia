# M129: The prose sweep reads help pages correctly, and text repeated across the task function pages is written once, in plain English

**Status:** done (2026-09-14, PR #132 https://github.com/jmgirard/tidymedia/pull/132)

**Goal:** Text repeated across the task function help pages is written once, in plain English, and the prose sweep that checks it reads help pages correctly.

**Outcome:** The 33 paragraphs repeated across the 34 blocks of `R/ffmpeg.R` are now written once. Shared arguments stay in one owner block that other blocks take with `@inheritParams`. Other shared text comes from internal functions in the new `R/task-doc.R`, called inline with `` `r …` ``. Rendered text changed on 27 pages, and the sweep drops from 393 to 252 findings with no sentence on two pages. `tools/doc_prose_report.R` fixes the six parse gaps. They cover lowercase sentence starts, argument lines, the UTF-8 locale, `.Rmd` dashes, fences, code-span pipes and HTML tags. It exits 2 on an empty parse and 3 on a usage error. New `tools/roxygen_repeats.R` lists repeated roxygen paragraphs. `tools/test_doc_prose_report.R` and `tools/test_roxygen_repeats.R` plant each case. No behavior change. Two wording tests were re-pinned and none removed. The ledger is `### M129` in `cairn/references/plain-docs.md`.

**Decisions:** Implement gate: an argument shared by name stays in one owner block, and other shared text comes from functions in `R/task-doc.R`. No `@inherit`, `@template` or `@inheritSection` lines, because AC1 counts a repeated tag line. One base claim found false (a timed-out probe warns, not silent) went to the help-text claims row as item (h), per D093.

**Review:** One pass with the three-lens fan-out, no returns. Findings O1, O2, O6 and O7 were fixed at the gate. The `parallel` text went back to the base claim ("process the jobs"), and the webm text is one "because" sentence. A test comment and a ROADMAP count were corrected. A fresh reader found the fixes correct. O3, O4, O5 and O8 (tool edge cases) got a new candidate row. O9 (manifest called a column) is item (i) on the help-text claims row. P2 was rejected, because tests pin both sentence forms. CI 8/8 R checks green. Codecov patch and project were red, because `R/task-doc.R` runs only at document time. The user chose to merge, and the coverage fix is item (e) on the new tool row. No lessons added, because LESSONS is at its byte budget.
