% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{compare_videos}
\alias{compare_videos}
\title{Build a side-by-side comparison video}
\usage{
compare_videos(
  infiles,
  outfile,
  direction = c("horizontal", "vertical"),
  resize = TRUE,
  audio_input = NULL,
  video_codec = NULL,
  audio_codec = "copy",
  hardware = c("none", "nvenc", "videotoolbox"),
  fallback = FALSE,
  quality = NULL,
  run = TRUE
)
}
\arguments{
\item{infiles}{A character vector of two or more video file paths.
This function checks every path itself. A path that cannot be found or read aborts naming this function, and the error lists every such path. It is not reported against the internal builder that the path would otherwise reach.}

\item{outfile}{A string giving the path to write the comparison video to.}

\item{direction}{Either \code{"horizontal"} (side-by-side, the default) or
\code{"vertical"} (stacked top to bottom).}

\item{resize}{A logical indicating whether to resize the inputs to share an
edge. Only supported for exactly two inputs. (default = \code{TRUE})}

\item{audio_input}{The input file whose audio to keep, as a number that counts from \code{0}. \code{0} is the first file you pass and \code{1} is the second. This counts the function's inputs, not the audio tracks of one input. So it is a different index from \code{audio_stream} on the functions that take one input. \code{NULL} (default) selects no audio at all, so the output is silent. This differs from \code{audio_stream = NULL}, which still selects audio. An input number the call does not have gives an R error, before FFmpeg runs. See \code{\link{audio_stream}}. (default = \code{NULL})}

\item{video_codec}{A string naming the output video codec, or \code{NULL} (default) to leave it unset. Then the output container's default encoder is used, and the compiled command is the same as one that never named a codec.}

\item{audio_codec}{A string naming the codec for the carried audio track.
\code{"copy"} (default) stream-copies it through untouched. Name an encoder, such as \code{"aac"}, to transcode it. \code{NULL} leaves the codec unset, so the output container's default encoder is used. When \code{audio_input} is
\code{NULL}, no audio reaches the output, so nothing is emitted. Naming an
encoder in that case is an error.}

\item{hardware}{The encoder backend. \code{"none"} (default) uses the software \code{video_codec}. \code{"nvenc"} uses NVIDIA GPU encoding (H.264, HEVC and AV1), and \code{"videotoolbox"} uses Apple GPU encoding (H.264 and HEVC). A backend uses its own encoder for the family of \code{video_codec}. For example, \code{"libx264"} becomes \code{"h264_nvenc"} or \code{"h264_videotoolbox"}. With the default \code{video_codec = NULL}, the H.264 family is assumed. So a non-H.264 container, such as \code{.webm}, needs an explicit HEVC- or AV1-family \code{video_codec} (AV1 only under \code{"nvenc"}). See \code{\link{has_hardware_encoder}} for availability and its caveats. Resolving a hardware backend asks this FFmpeg build which encoders it has. So the first such call that re-encodes the video runs FFmpeg while the command is built, even under \code{run = FALSE}. The answer is remembered for the rest of the R session. See \code{\link{refresh_ffmpeg_capabilities}} to discard it.}

\item{fallback}{A logical. When a \code{hardware} other than \code{"none"} is requested but its encoder is unavailable, \code{TRUE} encodes in software with a message. \code{FALSE} (default) aborts instead. With \code{video_codec = NULL}, the fallback leaves the codec unset rather than picking one, so the codec never changes silently. A \code{video_codec} in a family that the backend has no encoder for is a wrong argument, not an absent encoder. So it aborts whatever \code{fallback} says.}

\item{quality}{A number, or \code{NULL} (default) to leave the encoder's own default in place. It is the encoder's own rate-control value, passed through unchanged. \code{libx264} and \code{libx265} read it as \code{-crf} (0 to 51). The nvenc encoders read it as \code{-cq} (0 to 51), and the videotoolbox encoders as \code{-q:v} (1 to 100). Each scale is its own: the same number means something different on each encoder. A value outside the encoder's range is refused. An encoder outside those seven, such as \code{libvpx-vp9}, is refused with \code{quality} set. So is a \code{video_codec} of \code{"copy"}, or of \code{NULL} under \code{hardware = "none"}. When \code{fallback = TRUE} falls back to software, the value is dropped and the message says so, because it belonged to the hardware encoder's scale.}

\item{run}{A logical: run the command through FFmpeg (\code{TRUE}, default)
or return the compiled command without running it (\code{FALSE}).}
}
\value{
The compiled FFmpeg command (invisibly when \code{run = TRUE}).
}
\description{
Stack two or more videos into a single comparison video. The videos go
side-by-side (\code{direction = "horizontal"}) or one above the other
(\code{direction = "vertical"}). This is a common need when reviewing
annotations or before/after processing. Built on the stacking pipeline
functions (\code{\link{ffm_hstack}} / \code{\link{ffm_vstack}}). The glossary
in \code{vignette("tidymedia")} explains media terms such as codec, encoder
and stream copy.
}
\details{
By default the two inputs are resized to share an edge (equal heights for a
horizontal stack, equal widths for a vertical one). Resizing currently
supports exactly two inputs, so pass \code{resize = FALSE} to compare more.
Audio is dropped unless \code{audio_input} names an input to carry; a carried
track is stream-copied unless \code{audio_codec} names an encoder.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
compare_videos(c(video, video), "compare.mp4", run = FALSE)
}
\seealso{
\code{\link[=ffm_hstack]{ffm_hstack()}} and \code{\link[=ffm_vstack]{ffm_vstack()}}, the pipeline functions it wraps;
\code{\link[=has_hardware_encoder]{has_hardware_encoder()}} for the \code{hardware} argument;
\code{\link[=picture_in_picture]{picture_in_picture()}} for insetting instead of stacking.

Other task functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task functions}
