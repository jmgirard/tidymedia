% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{compare_videos_batch}
\alias{compare_videos_batch}
\title{Build Many Comparison Videos From a Jobs Table}
\usage{
compare_videos_batch(
  jobs,
  direction = c("horizontal", "vertical"),
  resize = TRUE,
  audio = NULL,
  video_codec = NULL,
  audio_codec = "copy",
  hardware = c("none", "nvenc"),
  fallback = FALSE,
  run = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{jobs}{A data frame with one row per output and (at least) an
\code{inputs} list-column — each cell a character vector of \strong{two or more}
video paths — and an \code{output} column (destination path). Optional
\code{direction}, \code{resize}, \code{audio}, \code{video_codec}, and
\code{audio_codec} columns override the
like-named arguments per row (a row omitting one falls back to the
argument). In an \code{audio} column, \code{NA} means "drop audio" (the
column's way of writing the scalar's \code{NULL}); in a \code{video_codec}
or \code{audio_codec} column it means "leave the codec unset". Any two rows
resolving to the same output path are rejected; other columns are ignored.}

\item{direction, resize}{Defaults applied to every row lacking the
corresponding column. \code{direction} is \code{"horizontal"} (the default)
or \code{"vertical"}; a \code{direction} column is held to the same two
values, per row. See \code{\link[=compare_videos]{compare_videos()}} for their fuller meaning.}

\item{audio}{The 0-based index of the \emph{input} whose audio to keep -- \code{0} is the first file passed in, \code{1} the second. This counts the verb's inputs, not one input's audio streams, so it is a different index from \code{audio_stream} on the single-input verbs. \code{NULL} (default) maps no audio at all, so the output is silent -- unlike \code{audio_stream = NULL}, which always maps something. Naming an input the call does not have is an R error, raised before FFmpeg runs. Applied to every row lacking an \code{audio} column; an \code{NA} cell in that column means the same as \code{NULL} for that row, dropping that output's audio. Each row's value is validated against that row's input count. See \code{\link{audio_stream}}. (default = \code{NULL})}

\item{video_codec}{A string naming the output video codec, applied to every
row lacking a \code{video_codec} column, or \code{NULL} (default) to leave
it unset so each output keeps its container's default encoder.}

\item{audio_codec}{A string naming the codec for the carried audio track,
applied to every row lacking an \code{audio_codec} column. \code{"copy"}
(default) stream-copies it; name an encoder to transcode it, or \code{NULL}
to leave the codec unset. A row carrying no audio emits no \code{-codec:a};
naming an encoder on such a row is an error.}

\item{hardware, fallback}{The encoder backend and its fallback behavior,
applied to the whole batch (a property of the machine, not of a row, so
neither is read as a \code{jobs} column). See \code{\link[=compare_videos]{compare_videos()}}.
Resolving \code{"nvenc"} asks this FFmpeg build which encoders it has, so
a \code{"nvenc"} call that re-encodes the video runs the binary while the
command is built, even under \code{run = FALSE}.
Availability is checked at this verb's own front door, before any row
runs, so an unavailable encoder aborts naming this function rather than
the internal fan-out it would otherwise be reported against. A call that
also contradicts itself — naming an \code{audio_codec} with no audio carried into the output —
is refused for the contradiction first, whether or not this machine has
the encoder.
A per-row value error — an \code{audio} index past that row's input count,
a \code{direction} outside the two accepted values — likewise reports ahead
of the encoder check.}

\item{run}{A logical: run each command through FFmpeg (\code{TRUE}, default)
or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical: map over jobs in parallel with \pkg{furrr}
(\code{TRUE}) or sequentially (\code{FALSE}, default). See
\code{\link{ffm_batch}} for the \pkg{future} plan requirement.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}} (e.g.
\code{verify}, \code{manifest}, \code{progress}).}
}
\value{
The \code{jobs} tibble with an added \code{command} column and, when
\code{run = TRUE}, a \code{success} column (plus \code{verified} /
provenance manifest when requested via \code{...}). See
\code{\link{ffm_batch}}.
}
\description{
Stack videos side by side for many outputs from a single jobs tibble — the
\strong{batch} (table-driven) sibling of \code{\link[=compare_videos]{compare_videos()}} for when you have more
than one comparison to produce. Each row carries an \code{inputs} list-column
(each cell two or more video paths) plus an \code{output} column (D015).
This is a thin wrapper over \code{\link{ffm_batch}}: one reproducible stacking
command per row, sharing the pipeline with the scalar verb.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(inputs = list(c(video, video)), output = "compare.mp4")
compare_videos_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=compare_videos]{compare_videos()}}, the scalar verb it wraps; \code{\link[=ffm_batch]{ffm_batch()}}, the
batch runner; \code{\link[=has_nvenc]{has_nvenc()}} for the \code{hardware = "nvenc"} toggle;
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}} and \code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
the other fan-in batch siblings.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task verb functions}
