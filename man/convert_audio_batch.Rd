% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{convert_audio_batch}
\alias{convert_audio_batch}
\title{Convert the Audio of Many Files From a Jobs Table}
\usage{
convert_audio_batch(
  jobs,
  audio_codec = NULL,
  audio_stream = NULL,
  run = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{jobs}{A data frame with one row per input and (at least) an
\code{input} column (source path) and an \code{output} column (destination
path). An \code{output} column is \strong{required} — an audio destination
cannot be auto-named because its extension picks the output format. An
optional \code{audio_codec} column overrides the \code{audio_codec}
argument per row, where \code{NA} spells "use the highest-VBR-quality
default"; rows omitting it fall back to the argument. An optional
\code{audio_stream} column likewise overrides the \code{audio_stream}
argument per row, where \code{NA} keeps that row on the first audio track.
Two rows given the same \code{output} path are refused before any row
runs. Any other columns are
ignored — except a \code{format} column, retired with the argument of the
same name, which is an error rather than a silent no-op.}

\item{audio_codec}{The output audio codec applied to every row unless
\code{jobs} carries an \code{audio_codec} column. \code{NULL} (default)
infers the codec from each \code{output} extension at highest VBR quality;
name a codec (e.g. \code{"aac"}, \code{"flac"}) to pin \code{-c:a}.}

\item{audio_stream}{The audio track to take, as a number that counts from \code{0} among the \emph{audio tracks} of each row's input. \code{0} is the first audio track and \code{1} is the second. Other streams in the file, such as video, do not count. \code{NULL} (default) takes the \strong{first} audio track. Without an \code{audio_stream} column, the argument applies to every row. An \code{NA} cell in that column means \code{NULL} for that row. It does not fall back to the argument. The first-track family reads \code{NULL} as the first audio track only: \code{\link{extract_audio}}, \code{\link{convert_audio}} and \code{\link{normalize_audio}}, and their \code{_batch} forms. The every-track family reads it as every audio track: \code{\link{separate_audio_video}}, \code{\link{standardize_video}}, \code{\link{anonymize_video}}, \code{\link{crop_video}}, \code{\link{segment_video}} and \code{\link{format_for_web}}, and their \code{_batch} forms. A track the input does not have gives an FFmpeg error, not an R one. See \code{\link{audio_stream}} for how this differs from \code{audio_input}, the input index on \code{\link{compare_videos}} and \code{\link{picture_in_picture}}. (default = \code{NULL})}

\item{run}{A logical: run each command through FFmpeg (\code{TRUE}, default)
or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical: run the jobs in parallel with \pkg{furrr}
(\code{TRUE}) or one at a time (\code{FALSE}, default). See
\code{\link{ffm_batch}} for the \pkg{future} plan requirement.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}} (e.g.
\code{verify}, \code{manifest}, \code{progress}).}
}
\value{
The \code{jobs} tibble with an added \code{command} column. When \code{run = TRUE}, it also has a \code{success} column, plus \code{verified} or a provenance manifest, each when requested through \code{...}. See \code{\link{ffm_batch}}.
}
\description{
Extract or transcode the audio track of many input files from a single jobs
tibble — the \strong{batch} (table-driven) sibling of \code{\link[=convert_audio]{convert_audio()}} for when
you have more than one file. Each row is one input; \code{input} and
\code{output} columns are required. This is a thin wrapper over
\code{\link{ffm_batch}}: one reproducible compiled command per input, sharing
the same audio-map pipeline (and per-value \code{audio_codec} validation) as
the scalar verb.
}
\details{
When a row names no \code{audio_stream} and its input has tracks that the output will not carry, the function warns \strong{once} for the whole batch. The warning names every affected row. The check costs \strong{one FFprobe call per distinct input} it has to probe. A repeated input is probed once, and a row that names a track is not probed at all. The warning is given only when FFprobe is available and the input can be probed. Otherwise the check is skipped silently. Those probes run \strong{one at a time, before any row starts}, so \code{parallel} does not reach them. A sweep long enough to look like a hang reports its progress. The check never runs under \code{run = FALSE}, never changes any compiled command, and is skipped entirely when every row names a track. Suppress it by class with \code{suppressWarnings(classes = "tidymedia_dropped_audio")}.

To switch the check off and skip the whole sweep, use \code{options(tidymedia.check_tracks = FALSE)} for the session. Use \code{withr::local_options(tidymedia.check_tracks = FALSE)} for the rest of one function.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(input = c(video, video), output = c("a.mp3", "b.mp3"))
convert_audio_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=convert_audio]{convert_audio()}}, the scalar verb it wraps; \code{\link[=ffm_batch]{ffm_batch()}}, the batch
runner; \code{\link[=extract_audio_batch]{extract_audio_batch()}} to stream-copy audio in batch.

Other task functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task functions}
