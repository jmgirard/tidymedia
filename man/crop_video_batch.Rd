% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{crop_video_batch}
\alias{crop_video_batch}
\title{Crop Many Videos From a Jobs Table}
\usage{
crop_video_batch(
  jobs,
  width = NULL,
  height = NULL,
  x = "(in_w-out_w)/2",
  y = "(in_h-out_h)/2",
  video_codec = NULL,
  audio_codec = "copy",
  hardware = c("none", "nvenc"),
  fallback = FALSE,
  audio_stream = NULL,
  run = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{jobs}{A data frame with one row per input and (at least) an
\code{input} column (source path). An optional \code{output} column names
the destination; when absent, one is derived per row by appending
\code{_cropped} to each input's basename, keeping the input's extension
(e.g. \code{clip.mp4} becomes \code{clip_cropped.mp4}). Each crop dimension
— \code{width}, \code{height}, \code{x}, \code{y} — may also appear as a
column to override the corresponding argument per row; rows (or dimensions)
omitting the column fall back to the argument. A \code{video_codec} column
overrides that argument per row, with \code{NA} meaning "leave the codec
unset" (the column's way of writing the argument's \code{NULL}); an
\code{audio_codec} column works the same way. An \code{audio_stream} column
overrides that argument per row, with \code{NA} meaning "keep every audio
track" (the column's way of writing that argument's \code{NULL}). Any two
rows that resolve to the same output path are rejected. Any other columns
are ignored.}

\item{width, height}{The output crop size in pixels, applied to every row
unless \code{jobs} carries a column of the same name. Required: pass each as
an argument or supply the column (there is no default crop size).}

\item{x, y}{The offset in pixels of the crop's left/top edge, applied to every
row unless \code{jobs} carries a column of the same name. Default: centered.}

\item{video_codec}{A string naming the output video codec, applied to every
row lacking a \code{video_codec} column, or \code{NULL} (default) to leave
it unset so each output keeps its container's default encoder.}

\item{audio_codec}{A string naming the output audio codec, applied to every
row lacking an \code{audio_codec} column. \code{"copy"} (default)
stream-copies the audio; name an encoder to transcode it, or \code{NULL} to
leave the codec unset so each output keeps its container's default encoder.}

\item{hardware, fallback}{The encoder backend and its fallback behavior,
applied to the whole batch (a property of the machine, not of a row, so
neither is read as a \code{jobs} column). See \code{\link[=crop_video]{crop_video()}}.}

\item{audio_stream}{The 0-based index of the audio track to carry into each output, counted \emph{among that row's input's audio streams} -- \code{0} is the first audio track, \code{1} the second, whatever their positions among the file's streams. \code{NULL} (default) carries \strong{every} audio track. The argument applies to every row lacking an \code{audio_stream} column; an \code{NA} cell in that column means the same as \code{NULL} for that row, rather than falling back to the argument. The pass-through family reads \code{NULL} this way -- \code{\link{separate_audio_video}}, \code{\link{standardize_video}}, \code{\link{anonymize_video}}, \code{\link{crop_video}}, \code{\link{segment_video}} and \code{\link{format_for_web}}, plus their \code{_batch} siblings. The extraction family takes the first track only: \code{\link{extract_audio}}, \code{\link{convert_audio}} and \code{\link{normalize_audio}}, plus theirs. Subtitle and data streams are not carried either way. Naming a track the input does not have is an FFmpeg error, not an R one. See \code{\link{audio_stream}} for how this differs from \code{audio}, the input index on \code{\link{compare_videos}} and \code{\link{picture_in_picture}}. (default = \code{NULL})}

\item{run}{A logical: run each command through FFmpeg (\code{TRUE}, default)
or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical: map over jobs in parallel with \pkg{furrr}
(\code{TRUE}) or sequentially (\code{FALSE}, default). See
\code{\link{ffm_batch}} for the \pkg{future} plan requirement.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}} (e.g.
\code{verify}, \code{manifest}, \code{progress}).}
}
\value{
The \code{jobs} tibble with an added \code{command} column and, when
\code{run = TRUE}, a \code{success} column (plus \code{verified} /
provenance manifest when requested via \code{...}). See
\code{\link{ffm_batch}}.
}
\description{
Crop many input videos to a rectangular region from a single jobs tibble —
the \strong{batch} (table-driven) sibling of \code{\link[=crop_video]{crop_video()}} for when you have more
than one file. Each row is one input. This is a thin wrapper over
\code{\link{ffm_batch}}: one reproducible compiled command per input, sharing
the same crop pipeline (and its per-value dimension guards) as the scalar
verb.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(input = c(video, video), output = c("a.mp4", "b.mp4"),
                       width = c(160, 80), height = c(120, 60))
crop_video_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=crop_video]{crop_video()}}, the scalar verb it wraps; \code{\link[=ffm_batch]{ffm_batch()}}, the batch
runner; \code{\link[=has_nvenc]{has_nvenc()}} for the \code{hardware = "nvenc"} toggle;
\code{\link[=standardize_video_batch]{standardize_video_batch()}} to re-encode in batch.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}
}
\concept{task verb functions}
