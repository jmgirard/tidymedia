% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffm.R
\name{ffm_concat}
\alias{ffm_concat}
\title{Concatenate Multiple Inputs in an FFmpeg Pipeline}
\usage{
ffm_concat(object)
}
\arguments{
\item{object}{An FFmpeg pipeline (\code{ffm}) object created by
\code{ffm_files()} with more than one input file.}
}
\value{
\code{object} with an added instruction to concatenate the inputs.
}
\description{
Join the pipeline's input files one after another with FFmpeg's
\href{https://ffmpeg.org/ffmpeg-formats.html#concat-1}{concat demuxer}. Like
\code{\link{ffm_hstack}}, this is a pipeline function for several inputs. It
uses stream copy, so it is fast and lossless. But every input must share the
same parameters, such as codec, resolution and frame rate. The glossary in
\code{vignette("tidymedia")} explains media terms such as codec, re-encode
and stream copy.
}
\details{
To join inputs with different parameters, you must re-encode with the concat
filter. The package does not wrap that filter yet, so use a direct command,
such as \code{\link{ffmpeg}}.

The demuxer needs a list file that names the inputs. \code{ffm_concat()}
writes one to a temporary path at once and stores it in the pipeline, so the
compiled command can refer to it. It also copies codecs and maps all
streams, as \code{\link{ffm_copy}} would.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
# Join two inputs end-to-end (they must share codec/resolution/frame rate)
ffm_files(c(video, video), "output.mp4") |>
  ffm_concat() |>
  ffm_compile()
}
\seealso{
\code{\link[=concatenate_videos]{concatenate_videos()}}, the task function built on this function.

Other pipeline functions:
\code{\link[=ffm_batch]{ffm_batch()}},
\code{\link[=ffm_codec]{ffm_codec()}},
\code{\link[=ffm_compile]{ffm_compile()}},
\code{\link[=ffm_copy]{ffm_copy()}},
\code{\link[=ffm_crop]{ffm_crop()}},
\code{\link[=ffm_drawbox]{ffm_drawbox()}},
\code{\link[=ffm_drop]{ffm_drop()}},
\code{\link[=ffm_files]{ffm_files()}},
\code{\link[=ffm_fps]{ffm_fps()}},
\code{\link[=ffm_hstack]{ffm_hstack()}},
\code{\link[=ffm_jobs]{ffm_jobs()}},
\code{\link[=ffm_loudnorm]{ffm_loudnorm()}},
\code{\link[=ffm_map]{ffm_map()}},
\code{\link[=ffm_output_options]{ffm_output_options()}},
\code{\link[=ffm_overlay]{ffm_overlay()}},
\code{\link[=ffm_pixel_format]{ffm_pixel_format()}},
\code{\link[=ffm_run]{ffm_run()}},
\code{\link[=ffm_scale]{ffm_scale()}},
\code{\link[=ffm_seek]{ffm_seek()}},
\code{\link[=ffm_trim]{ffm_trim()}},
\code{\link[=ffm_vstack]{ffm_vstack()}},
\code{\link[=print.tidymedia_ffm]{print.tidymedia_ffm()}}
}
\concept{pipeline functions}
