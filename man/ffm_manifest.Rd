% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffm_manifest.R
\name{ffm_manifest}
\alias{ffm_manifest}
\title{Read a Batch Provenance Manifest}
\usage{
ffm_manifest(x, path = NULL)
}
\arguments{
\item{x}{A tibble returned by \code{\link[=ffm_batch]{ffm_batch()}} with \code{manifest = TRUE}.}

\item{path}{An optional file path. When supplied, the manifest is also
written there as CSV (via \code{utils::write.csv}, no row names) and
returned invisibly.}
}
\value{
The manifest \link[tibble:tibble-package]{tibble}; invisibly when
\code{path} is written.
}
\description{
Retrieve the reproducibility manifest recorded by \code{\link[=ffm_batch]{ffm_batch()}} when it was
run with \code{manifest = TRUE}. The manifest is a tibble with one row per
job. Each row has the compiled command and the FFmpeg and FFprobe versions
used. It also has the time of the run, the input and output paths, and the
output file size.
When the batch ran with \code{checksums = TRUE}, each row also has md5
checksums of the input and output files. With the manifest, a batch run
leaves a record that others can check.
}
\examples{
\dontshow{if (nzchar(Sys.which("ffmpeg"))) withAutoprint(\{ # examplesIf}
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(input = video, output = tempfile(fileext = ".mp3"))
res <- ffm_batch(jobs, manifest = TRUE, .f = function(input, output, ...) {
  ffm_files(input, output) |> ffm_drop("video")
})
ffm_manifest(res)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ffm_batch]{ffm_batch()}}, which records the manifest.

Other verification functions:
\code{\link[=verify_media]{verify_media()}}
}
\concept{verification functions}
