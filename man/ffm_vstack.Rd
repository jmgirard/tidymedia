% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffm.R
\name{ffm_vstack}
\alias{ffm_vstack}
\title{Vertically Stack Multiple Videos in an FFmpeg Pipeline}
\usage{
ffm_vstack(object, shortest = FALSE, resize = FALSE)
}
\arguments{
\item{object}{An FFmpeg pipeline (\code{ffm}) object created by
\code{ffm_files()}.}

\item{shortest}{A logical that says whether to trim the duration of all
videos to that of the shortest video. The default is \code{FALSE}.}

\item{resize}{A logical that says whether to resize the input videos to the
same width. Resizing takes longer, and for now it works only with two
inputs. It fits both inputs to the same aspect ratio, so it assumes the
inputs share one.}
}
\value{
\code{object} with an added instruction to stack the videos
vertically.
}
\description{
Add a complex video filter that stacks several videos vertically (one above
the other). It can also resize the videos to the same width.
}
\details{
This is the vertical form of \code{\link{ffm_hstack}}. Both are pipeline
functions for several inputs. They force the \code{-filter_complex} path and
manage their own stream labels. The glossary in \code{vignette("tidymedia")}
explains media terms such as stream.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
# Stack two inputs one above the other (pass more than one input to ffm_files())
ffm_files(c(video, video), "output.mp4") |>
  ffm_vstack() |>
  ffm_compile()
}
\seealso{
\code{\link[=ffm_hstack]{ffm_hstack()}} for horizontal stacking, and \code{\link[=compare_videos]{compare_videos()}}, the
task function built on both.

Other pipeline functions:
\code{\link[=ffm_batch]{ffm_batch()}},
\code{\link[=ffm_codec]{ffm_codec()}},
\code{\link[=ffm_compile]{ffm_compile()}},
\code{\link[=ffm_concat]{ffm_concat()}},
\code{\link[=ffm_copy]{ffm_copy()}},
\code{\link[=ffm_crop]{ffm_crop()}},
\code{\link[=ffm_drawbox]{ffm_drawbox()}},
\code{\link[=ffm_drop]{ffm_drop()}},
\code{\link[=ffm_files]{ffm_files()}},
\code{\link[=ffm_fps]{ffm_fps()}},
\code{\link[=ffm_hstack]{ffm_hstack()}},
\code{\link[=ffm_jobs]{ffm_jobs()}},
\code{\link[=ffm_loudnorm]{ffm_loudnorm()}},
\code{\link[=ffm_map]{ffm_map()}},
\code{\link[=ffm_output_options]{ffm_output_options()}},
\code{\link[=ffm_overlay]{ffm_overlay()}},
\code{\link[=ffm_pixel_format]{ffm_pixel_format()}},
\code{\link[=ffm_run]{ffm_run()}},
\code{\link[=ffm_scale]{ffm_scale()}},
\code{\link[=ffm_seek]{ffm_seek()}},
\code{\link[=ffm_trim]{ffm_trim()}},
\code{\link[=print.tidymedia_ffm]{print.tidymedia_ffm()}}
}
\concept{pipeline functions}
