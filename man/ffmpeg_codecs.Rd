% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{ffmpeg_codecs}
\alias{ffmpeg_codecs}
\title{Get a data frame of all installed codecs}
\usage{
ffmpeg_codecs(sort_by_type = TRUE)
}
\arguments{
\item{sort_by_type}{A logical. \code{TRUE} sorts the tibble by type and then
by name. \code{FALSE} sorts it by name only. (default = \code{TRUE})}
}
\value{
A \link[tibble:tibble-package]{tibble} with the following variables:
\item{name}{A character vector including the name/code of each codec}
\item{details}{A character vector including details about each codec}
\item{type}{A factor vector indicating whether each codec supports
\code{"Video"}, \code{"Audio"} or \code{"Subtitles"}} \item{decoding}{A
logical vector indicating whether each codec supports decoding}
\item{encoding}{A logical vector indicating whether each codec supports
encoding} \item{intraframe}{A logical vector indicating whether each codec
is an intra-frame-only codec} \item{lossy}{A logical vector indicating
whether each codec supports lossy compression} \item{lossless}{A logical
vector indicating whether each codec supports lossless compression}
}
\description{
Ask FFmpeg for its list of installed codecs, and return the list as a data
frame with information about each codec. The glossary in
\code{vignette("tidymedia")} explains media terms such as codec and encoder.
}
\examples{
\dontshow{if (nzchar(Sys.which("ffmpeg"))) withAutoprint(\{ # examplesIf}
head(ffmpeg_codecs())
ffmpeg_codecs(sort_by_type = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ffmpeg_encoders]{ffmpeg_encoders()}} for the encoder list, \code{\link[=ffm_codec]{ffm_codec()}} to set a
codec in a pipeline, and \code{\link[=ffmpeg]{ffmpeg()}} for the direct command.

Other capability functions:
\code{\link[=ffmpeg_encoders]{ffmpeg_encoders()}},
\code{\link[=hardware_encoder]{hardware_encoder()}},
\code{\link[=refresh_ffmpeg_capabilities]{refresh_ffmpeg_capabilities()}}
}
\concept{capability functions}
