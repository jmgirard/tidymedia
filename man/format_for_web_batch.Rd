% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{format_for_web_batch}
\alias{format_for_web_batch}
\title{Re-encode Many Videos for the Web From a Jobs Table}
\usage{
format_for_web_batch(
  jobs,
  hardware = c("none", "nvenc", "videotoolbox"),
  fallback = FALSE,
  audio_stream = NULL,
  run = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{jobs}{A data frame with one row per input and (at least) an
\code{input} column (source path). An optional \code{output} column names
the destination; when absent, one is derived per row by appending
\code{_web} to each input's basename with an \code{.mp4} extension (the web
re-encode always writes H.264/mp4), e.g. \code{clip.mkv} becomes
\code{clip_web.mp4}. Any two rows that resolve to the same output path are
rejected. An optional numeric \code{audio_stream} column (\code{NA} to keep
every audio track in that row) overrides the \code{audio_stream} argument
per row. Any other columns are ignored — including \code{video_codec} and
\code{audio_codec}, which the sibling batch verbs read as per-row overrides
but this one does not: the web recipe fixes both codecs by identity (H.264
video, AAC audio). For per-row codecs use a verb that exposes them, such as
\code{\link{standardize_video_batch}} or \code{\link{crop_video_batch}}.}

\item{hardware}{The encoder backend applied to every row: \code{"none"}
(default, software libx264), \code{"nvenc"} for NVIDIA GPU H.264
encoding, or \code{"videotoolbox"} for Apple GPU H.264 encoding.
Batch-wide (not a per-row column). See \code{\link{has_hardware_encoder}}.
Resolving a hardware backend asks this FFmpeg build which encoders it has,
so the first such call that re-encodes the video runs the binary
while the command is built, even under \code{run = FALSE}. The answer is
remembered for the rest of the R session; see
\code{\link{refresh_ffmpeg_capabilities}} to discard it.
Availability is checked at this verb's own front door, before any row
runs, so an unavailable encoder aborts naming this function rather than
the internal fan-out it would otherwise be reported against.}

\item{fallback}{A logical: when a non-\code{"none"} \code{hardware} is
requested but its encoder is unavailable, re-encode with software
libx264 and a message (\code{TRUE}) instead of aborting (\code{FALSE},
default).}

\item{audio_stream}{The 0-based index of the audio track to carry into each output, counted \emph{among that row's input's audio streams} -- \code{0} is the first audio track, \code{1} the second, whatever their positions among the file's streams. \code{NULL} (default) carries \strong{every} audio track. The argument applies to every row lacking an \code{audio_stream} column; an \code{NA} cell in that column means the same as \code{NULL} for that row, rather than falling back to the argument. The every-track family reads \code{NULL} this way -- \code{\link{separate_audio_video}}, \code{\link{standardize_video}}, \code{\link{anonymize_video}}, \code{\link{crop_video}}, \code{\link{segment_video}} and \code{\link{format_for_web}}, plus their \code{_batch} siblings. The first-track family takes one track only: \code{\link{extract_audio}}, \code{\link{convert_audio}} and \code{\link{normalize_audio}}, plus theirs. Subtitle and data streams are not carried either way. Naming a track the input does not have is an FFmpeg error, not an R one. See \code{\link{audio_stream}} for how this differs from \code{audio_input}, the input index on \code{\link{compare_videos}} and \code{\link{picture_in_picture}}. (default = \code{NULL})}

\item{run}{A logical: run each command through FFmpeg (\code{TRUE}, default)
or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical: map over jobs in parallel with \pkg{furrr}
(\code{TRUE}) or sequentially (\code{FALSE}, default). See
\code{\link{ffm_batch}} for the \pkg{future} plan requirement.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}} (e.g.
\code{verify}, \code{manifest}, \code{progress}).}
}
\value{
The \code{jobs} tibble with an added \code{command} column and, when
\code{run = TRUE}, a \code{success} column (plus \code{verified} /
provenance manifest when requested via \code{...}). See
\code{\link{ffm_batch}}.
}
\description{
Re-encode many input videos into a widely compatible, web-friendly form from
a single jobs tibble — the \strong{batch} (table-driven) sibling of
\code{\link[=format_for_web]{format_for_web()}} for when you have more than one file. Each row is one
input. This is a thin wrapper over \code{\link{ffm_batch}}: one reproducible
compiled command per input, sharing the same fixed H.264/AAC/\code{+faststart}
pipeline as the scalar verb (no per-row knobs).
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(input = c(video, video), output = c("a.mp4", "b.mp4"))
format_for_web_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=format_for_web]{format_for_web()}}, the scalar verb it wraps; \code{\link[=ffm_batch]{ffm_batch()}}, the
batch runner; \code{\link[=standardize_video_batch]{standardize_video_batch()}} for a configurable re-encode.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task verb functions}
