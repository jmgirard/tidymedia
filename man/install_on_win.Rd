% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/program_management.R
\name{install_on_win}
\alias{install_on_win}
\title{Install FFmpeg on Windows}
\usage{
install_on_win(
  download_url = NULL,
  install_dir = NULL,
  confirm = TRUE,
  archive_checksum = NULL
)
}
\arguments{
\item{download_url}{A string with the address of the FFmpeg archive. If
\code{NULL}, the call uses the latest "essentials" build from gyan.dev, a \verb{.7z}
archive.}

\item{install_dir}{A string with the folder to install FFmpeg into. If
\code{NULL}, the call uses the \code{ffmpeg} folder under
\code{tools::R_user_dir("tidymedia", "data")}. CRAN allows packages to keep user
data in that place.}

\item{confirm}{\code{TRUE} or \code{FALSE}. Whether to ask before the call downloads
or installs anything. Defaults to \code{TRUE}. In a session where no one can
answer, \code{TRUE} gives an error that names the same items as the question.
Pass \code{confirm = FALSE} to install without the question.}

\item{archive_checksum}{A string with the expected SHA-256 checksum of the
archive, as 64 hexadecimal characters in upper or lower case. Defaults to
\code{NULL}. If you give a checksum, the call uses it for any source and
downloads no checksum. If it is \code{NULL} and \code{download_url} is not the
default source, the call checks nothing and says so.}
}
\value{
\code{TRUE} when the install finished. \code{FALSE} when you said no to the
question, or when the call could not create the install folder. Other
failures give an error. The section "Errors" lists the error classes of
the install steps.
}
\description{
\code{install_on_win()} downloads a Windows build of FFmpeg and unpacks it. Then
it saves the locations of \code{ffmpeg}, \code{ffprobe} and \code{ffplay}, as
\code{\link[=set_program]{set_program()}} does. After that, the package can find these programs.

By default, the call downloads the latest "essentials" build from gyan.dev.
It unpacks the build into the \code{ffmpeg} folder under
\code{tools::R_user_dir("tidymedia", "data")}.

By default, the call asks you to confirm before it does anything. The
question names each file it will download and the folder it will unpack
into. It also names
the saved program locations that the install can replace. If you say no, the
call returns \code{FALSE} and changes nothing.

This function works on Windows only. On any other system, it gives an error
before it asks, writes or downloads anything. The error names the system it
found. On macOS, you can install FFmpeg with \verb{brew install ffmpeg}. On Linux,
you can use \verb{sudo apt-get install ffmpeg}. On any system, \code{\link[=set_program]{set_program()}}
tells the package where an installed FFmpeg is.
}
\details{
Before the call unpacks the archive, it checks the archive against a SHA-256
checksum. A checksum is a fingerprint of the file's contents. For the
default source, the call downloads the checksum that gyan.dev publishes next
to each build. That file has the archive's address with \code{.sha256} added. For
any other source, give the checksum in \code{archive_checksum}.

The published checksum comes from the same site as the archive, over the
same connection. So the check finds a damaged or incomplete download. It
does not find a source that someone has tampered with.

After the unpack, the call checks each program before it saves any location.
The path must be one that R finds as a program. It must be a file, not a
folder, and the file must not be empty. The call does not run the program.
So a build for the wrong type of processor can pass this check.

The package needs \code{ffmpeg} and \code{ffprobe}. If either one fails the check, the
call saves no location and gives an error. The error names each failed
program and its full path. If \code{ffplay} is missing or fails the check, the
install finishes. A message says that the call did not save \code{ffplay}.
}
\section{What a failed install leaves behind}{

When the call gives an error, it tries to leave the install folder as it
found it. It removes the files that a failed unpack wrote. It removes a
folder that the call created. It does not touch the files that were already
in the folder, with one exception.

The exception is a file of yours that the failed unpack wrote over. The call
removes that file too, because the file no longer holds what you put there.

On Windows, the removal can fail. After a failed unpack, the unpack library
can still hold a file open. Windows does not delete a file that is open.

The error names by full path the entries of the first case below that
applies:
\itemize{
\item each unpacked file that the call could not remove
\item each folder that the call created and could not remove
\item each file of yours that the call removed
}

Two errors come after a successful unpack: \code{tidymedia_program_not_extracted}
and \code{tidymedia_program_unusable}. These errors leave the unpacked files in
the folder, and they say so.

The call learns which files the unpack made from the archive's own list and
from the folder. A program that the list names but that is not in the folder
counts as not unpacked. For example, antivirus software can remove a program
right after the unpack. The error then says that the unpack reported writing
that file.

If none of the unpacked files are in the folder,
\code{tidymedia_program_not_extracted} follows the usual rule. The call removes a
folder that it created, and the error says so.
}

\section{Errors}{

The call gives an error of its own class in these cases:
\itemize{
\item \code{tidymedia_wrong_platform}: the session is not running on Windows.
\item \code{tidymedia_confirmation_unavailable}: the call must ask you to confirm,
but no one can answer in this session.
\item \code{tidymedia_download_unavailable}: the archive did not download, or nothing
readable arrived.
\item \code{tidymedia_checksum_unavailable}: the call could not download or read the
published checksum.
\item \code{tidymedia_checksum_mismatch}: the downloaded archive does not match its
checksum.
\item \code{tidymedia_archive_unreadable}: the call could not unpack the archive.
\item \code{tidymedia_program_not_extracted}: \code{ffmpeg} or \code{ffprobe} is not at the path
where the install would put it.
\item \code{tidymedia_program_unusable}: the archive made \code{ffmpeg} or \code{ffprobe}, but
the file cannot be used.
}
}

\examples{
\dontrun{
# Download and install a static FFmpeg build (Windows)
install_on_win()
}
}
\seealso{
\code{\link[=set_program]{set_program()}} to save the location of a program you already have,
and \code{\link[=find_ffmpeg]{find_ffmpeg()}} to check where the package finds a program.

Other program management functions:
\code{\link[=find_ffmpeg]{find_ffmpeg()}},
\code{\link[=program_status]{program_status()}},
\code{\link[=set_program]{set_program()}},
\code{\link[=unset_program]{unset_program()}}
}
\concept{program management functions}
