% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{picture_in_picture}
\alias{picture_in_picture}
\title{Inset one video over another (picture-in-picture)}
\usage{
picture_in_picture(
  main,
  overlay,
  outfile,
  position = c("topright", "topleft", "bottomright", "bottomleft", "center"),
  scale = 0.25,
  margin = 16,
  audio = NULL,
  video_codec = NULL,
  audio_codec = "copy",
  hardware = c("none", "nvenc"),
  fallback = FALSE,
  run = TRUE
)
}
\arguments{
\item{main}{A string giving the path to the background (full-size) video.}

\item{overlay}{A string giving the path to the inset video.}

\item{outfile}{A string giving the path to write the result to.}

\item{position}{Where to place the inset: one of \code{"topright"} (default),
\code{"topleft"}, \code{"bottomright"}, \code{"bottomleft"}, or
\code{"center"}.}

\item{scale}{The inset's width as a fraction of the main video's width, aspect
preserved (\code{0 < scale <= 1}). (default = \code{0.25})}

\item{margin}{The gap in pixels between the inset and the video edges (ignored
for \code{position = "center"}). (default = \code{16})}

\item{audio}{The 0-based index of the \emph{input} whose audio to keep -- \code{0} is the first file passed in, \code{1} the second. This counts the verb's inputs, not one input's audio streams, so it is a different index from \code{audio_stream} on the single-input verbs. \code{NULL} (default) maps no audio at all, so the output is silent -- unlike \code{audio_stream = NULL}, which always maps something. Naming an input the call does not have is an R error, raised before FFmpeg runs. See \code{\link{audio_stream}}. (default = \code{NULL})}

\item{video_codec}{A string naming the output video codec, or \code{NULL}
(default) to leave it unset, so the output container's default encoder is
used and the compiled command is unchanged from one that never named a
codec.}

\item{audio_codec}{A string naming the codec for the carried audio track.
\code{"copy"} (default) stream-copies it through untouched; name an encoder
(e.g. \code{"aac"}) to transcode it, or pass \code{NULL} to leave the codec
unset so the output container's default encoder is used. Nothing is emitted
when \code{audio} is \code{NULL}, since no audio reaches the output; naming
an encoder in that case is an error.}

\item{hardware}{The encoder backend: \code{"none"} (default, the software
\code{video_codec}) or \code{"nvenc"} for NVIDIA GPU encoding. When
\code{"nvenc"}, the nvenc encoder for \code{video_codec}'s family is used
(e.g. \code{"libx264"} becomes \code{"h264_nvenc"}); with the default
\code{video_codec = NULL} the H.264 family is assumed, so a non-H.264
container (e.g. \code{.webm}) needs an explicit HEVC- or AV1-family
\code{video_codec}. See \code{\link{has_nvenc}} for availability and its
caveats.
Resolving \code{"nvenc"} asks this FFmpeg build which encoders it has, so
a \code{"nvenc"} call runs the binary while the command is built, even
under \code{run = FALSE}.}

\item{fallback}{A logical: when \code{hardware = "nvenc"} but nvenc is
unavailable, encode in software with a message (\code{TRUE}) instead of
aborting (\code{FALSE}, default). With \code{video_codec = NULL} the
fallback leaves the codec unset rather than picking one, so the codec never
changes silently.}

\item{run}{A logical: run the command through FFmpeg (\code{TRUE}, default)
or return the compiled command without running it (\code{FALSE}).}
}
\value{
The compiled FFmpeg command (invisibly when \code{run = TRUE}).
}
\description{
Composite a smaller \code{overlay} video onto a \code{main} video in one
corner (or the center) — the classic picture-in-picture layout for pairing a
speaker with a screen recording, or a stimulus with a webcam. Built on the
blessed \code{\link{ffm_overlay}} verb, which resizes the overlay to a
fraction of the main video's width and positions it.
}
\details{
Audio is dropped unless \code{audio} names an input to carry (\code{0} = the
main video, \code{1} = the overlay). A carried track is
stream-copied unless \code{audio_codec} names an encoder.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
picture_in_picture(video, video, "pip.mp4", run = FALSE)
}
\seealso{
\code{\link[=ffm_overlay]{ffm_overlay()}}, the builder it wraps; \code{\link[=has_nvenc]{has_nvenc()}} for the
\code{hardware = "nvenc"} toggle; \code{\link[=compare_videos]{compare_videos()}} for
side-by-side stacking.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task verb functions}
