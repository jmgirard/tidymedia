% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{picture_in_picture_batch}
\alias{picture_in_picture_batch}
\title{Inset One Video Over Another For Many Outputs From a Jobs Table}
\usage{
picture_in_picture_batch(
  jobs,
  position = c("topright", "topleft", "bottomright", "bottomleft", "center"),
  scale = 0.25,
  margin = 16,
  audio_input = NULL,
  video_codec = NULL,
  audio_codec = "copy",
  hardware = c("none", "nvenc", "videotoolbox"),
  fallback = FALSE,
  quality = NULL,
  run = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{jobs}{A data frame with one row per output and (at least) \code{main}
(background path), \code{overlay} (inset path), and \code{output}
(destination path) columns. Optional \code{position}, \code{scale},
\code{margin}, \code{audio_input}, \code{video_codec}, and \code{audio_codec}
columns override the
like-named arguments
per row (a row omitting one falls back to the argument).
In an \code{audio_input} column, \code{NA} means "drop audio", the column's way of writing the scalar's \code{NULL}. In a \code{video_codec} or \code{audio_codec} column, it means "leave the codec unset". A numeric \code{quality} column overrides the
\code{quality} argument per row (see \code{quality}). Two rows given the
same \code{output}
path are refused before any row runs; other columns are ignored.}

\item{position, scale, margin}{Defaults applied to every row lacking the
corresponding column. \code{position} is one of \code{"topright"} (the
default), \code{"topleft"}, \code{"bottomright"}, \code{"bottomleft"} or
\code{"center"}; a \code{position} column is held to those same five
values, per row. See \code{\link[=picture_in_picture]{picture_in_picture()}} for their fuller meaning.}

\item{audio_input}{The input file whose audio to keep, as a number that counts from \code{0}. \code{0} is the first file you pass and \code{1} is the second. This counts the function's inputs, not the audio tracks of one input. So it is a different index from \code{audio_stream} on the functions that take one input. \code{NULL} (default) selects no audio at all, so the output is silent. This differs from \code{audio_stream = NULL}, which still selects audio. An input number the call does not have gives an R error, before FFmpeg runs. Without an \code{audio_input} column, the argument applies to every row. An \code{NA} cell in that column means \code{NULL} for that row, so that output has no audio. See \code{\link{audio_stream}}. (default = \code{NULL})}

\item{video_codec}{A string naming the output video codec, applied to every
row lacking a \code{video_codec} column. \code{NULL} (default) leaves it
unset, so each output keeps its container's default encoder.}

\item{audio_codec}{A string naming the codec for the carried audio track,
applied to every row lacking an \code{audio_codec} column. \code{"copy"}
(default) stream-copies it. Name an encoder to re-encode it, or
\code{NULL} to leave the codec unset. A row carrying no audio emits no
\code{-codec:a}, and naming an encoder on such a row is an error.}

\item{hardware, fallback}{The encoder backend and its fallback behavior, applied to the whole batch. They are a property of the machine, not of a row, so neither is read as a \code{jobs} column. See \code{\link[=picture_in_picture]{picture_in_picture()}}.
Resolving a hardware backend asks this FFmpeg build which encoders it has. So the first such call that re-encodes the video runs FFmpeg while the command is built, even under \code{run = FALSE}. The answer is remembered for the rest of the R session. See \code{\link{refresh_ffmpeg_capabilities}} to discard it. This function checks that the encoder is available before any row runs. So an unavailable encoder aborts naming this function, not the internal step that runs the rows.
A call can also contradict itself by naming an \code{audio_codec} with no audio carried into the output. Such a call is refused for the contradiction first, whether or not this machine has the encoder.
A per-row value error likewise reports ahead of the encoder check. Examples
are a negative \code{margin}, an \code{audio_input} index outside the two
inputs, and a \code{position} outside the five accepted values.
A value error and a contradiction resolve the same way whether the value arrived as an argument or in a \code{jobs} column. The contradiction reports first.}

\item{quality}{A number, or \code{NULL} (default), applied to each row unless \code{jobs} carries a numeric \code{quality} column. In that column, \code{NA} leaves that row's encoder default in place, whatever the argument says. The value is the encoder's own rate-control value, passed through unchanged. Each cell is checked against the encoder its own row resolves to. A wrong cell is refused before any row runs, and the error names this function and the row. See \code{\link[=picture_in_picture]{picture_in_picture()}} for the encoders, their flags and ranges, and the values it refuses.}

\item{run}{A logical: run each command through FFmpeg (\code{TRUE}, default)
or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical: process the jobs in parallel with \pkg{furrr}
(\code{TRUE}) or one at a time (\code{FALSE}, default). See
\code{\link{ffm_batch}} for the \pkg{future} plan requirement.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}} (e.g.
\code{verify}, \code{manifest}, \code{progress}).}
}
\value{
The \code{jobs} tibble with an added \code{command} column. When \code{run = TRUE}, it also has a \code{success} column, plus \code{verified} or a provenance manifest, each when requested through \code{...}. See \code{\link{ffm_batch}}.
}
\description{
Composite an inset (overlay) video onto a main video for many outputs from a
single jobs tibble. This is the \strong{batch} (table-driven) form of
\code{\link[=picture_in_picture]{picture_in_picture()}}, for when you have more than one to produce. Its two
inputs have distinct roles, so \code{jobs} carries fixed \code{main} and
\code{overlay} columns (not a list-column) plus an \code{output} column.
This is a thin wrapper over \code{\link{ffm_batch}}: one reproducible overlay
command per row, sharing the pipeline with \code{\link[=picture_in_picture]{picture_in_picture()}}. The
glossary in \code{vignette("tidymedia")} explains media terms such as codec,
encoder and stream copy.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(main = video, overlay = video, output = "pip.mp4")
picture_in_picture_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=picture_in_picture]{picture_in_picture()}}, the one-output function it wraps;
\code{\link[=ffm_batch]{ffm_batch()}}, the batch runner; \code{\link[=has_hardware_encoder]{has_hardware_encoder()}} for the
\code{hardware} argument. \code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}} and
\code{\link[=compare_videos_batch]{compare_videos_batch()}}, the other batch functions that take several
inputs per row.

Other task functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task functions}
