% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{sample_frames}
\alias{sample_frames}
\title{Sample frames from a video at a fixed rate}
\usage{
sample_frames(
  infile,
  outdir,
  fps = NULL,
  interval = NULL,
  format = "png",
  prefix = NULL,
  run = TRUE
)
}
\arguments{
\item{infile}{A string containing the path to a video file.}

\item{outdir}{A string naming the directory to write the image sequence to.
The function creates it (recursively) if it does not exist.}

\item{fps}{The sampling rate, in frames per second: either a positive number
or an FFmpeg framerate expression string (for example \code{"30000/1001"}).
Provide exactly one of \code{fps} or \code{interval}.}

\item{interval}{The number of seconds between sampled frames (a positive
number). The function uses the reciprocal as the frame rate. Provide
exactly one of \code{fps} or \code{interval}.}

\item{format}{A string giving the output image file extension (one of
\code{"png"}, \code{"jpg"}, \code{"jpeg"}, \code{"bmp"}, \code{"tif"},
\code{"tiff"}, \code{"webp"}). (default = \code{"png"})}

\item{prefix}{A string used as the basename stem of each image, or
\code{NULL} to derive it from \code{infile}'s basename. (default =
\code{NULL})}

\item{run}{A logical: run the command through FFmpeg (\code{TRUE}, default)
or return the compiled command without running it (\code{FALSE}).}
}
\value{
The compiled FFmpeg command (invisibly when \code{run = TRUE}).
}
\description{
Sample a video into a numbered image sequence. Sample at a fixed rate
(\code{fps}) or at a fixed interval (\code{interval}, seconds between
frames). This is the first step for per-frame coding and for computer-vision
feature pipelines. Provide exactly one of \code{fps} or \code{interval}.
}
\details{
\code{\link{extract_frame}} saves one frame, and
\code{\link{extract_frame_batch}} saves a set of frames that you list. This
function is different. It builds a \emph{single} FFmpeg command whose output
is a printf-style file name pattern. FFmpeg's \code{image2} muxer fills the
pattern. FFmpeg decides the frame count when it decodes the video, and you
do not list the frames. The function writes frames to \code{outdir} as
\code{<prefix>_<n>.<format>}, where \code{<n>} is a zero-padded integer
starting at 1. The glossary in \code{vignette("tidymedia")} explains media
terms such as frame rate.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
# run = FALSE returns the reproducible command instead of executing it
sample_frames(video, tempdir(), fps = 2, run = FALSE)
}
\seealso{
\code{\link[=ffm_fps]{ffm_fps()}}, the pipeline function it uses to set the sampling rate.
\code{\link[=extract_frame]{extract_frame()}} for a single frame, and \code{\link[=extract_frame_batch]{extract_frame_batch()}} for a
set of frames that you list. \code{\link[=sample_frames_batch]{sample_frames_batch()}} for the many-file
form.

Other task functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}
}
\concept{task functions}
