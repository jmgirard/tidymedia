% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{segment_video}
\alias{segment_video}
\title{Segment Video}
\usage{
segment_video(
  infile,
  start,
  end,
  outfiles = NULL,
  reencode = TRUE,
  video_codec = NULL,
  audio_codec = "copy",
  hardware = c("none", "nvenc", "videotoolbox"),
  fallback = FALSE,
  audio_stream = NULL,
  run = TRUE,
  parallel = FALSE
)
}
\arguments{
\item{infile}{A string containing the path to a video file.}

\item{start}{A vector containing one or more timestamps indicating the
start of each segment to create. Can be either a numeric vector indicating
seconds or a character vector with time duration syntax. Must have the same
length as \code{end}.}

\item{end}{A vector containing one or more timestamps indicating the stop
of each segment to create. Can be either a numeric vector indicating
seconds or a character vector with time duration syntax. Must have the same
length as \code{start}.}

\item{outfiles}{Either NULL or a character vector indicating the filename
(with extension) for each segment to create. If NULL, will append a
zero-padded integer to \code{infile}. If not NULL, must have the same
length as \code{start}, and each element must be a single string -- so a
list of strings is accepted as well as a character vector, and a missing
value or a number in any position is refused by this function rather than
by the per-segment fan-out below it.}

\item{reencode}{A logical passed to \code{\link{ffm_seek}}: cut each segment
frame-accurately by re-encoding (\code{TRUE}, default) or with a fast,
lossless copy that snaps to keyframes (\code{FALSE}). See \code{ffm_seek}
for the trade-off.}

\item{video_codec}{A string naming the output video codec, or \code{NULL}
(default) to leave it unset, so the output container's default encoder is
used and the compiled command is unchanged from one that never named a
codec. A stream copy runs no encoder, so naming a codec (or a
\code{hardware} backend) alongside \code{reencode = FALSE} is an error.}

\item{audio_codec}{A string naming the output audio codec. \code{"copy"}
(default) stream-copies the audio through untouched; name an encoder (e.g.
\code{"aac"}) to transcode it, or pass \code{NULL} to leave the codec unset
so the output container's default encoder is used. A stream copy
(\code{reencode = FALSE}) always copies the audio, so any other value is an
error there. Stream-copying fails if the output container cannot hold the
source audio codec (e.g. FLAC in \code{.mp4}) — name an encoder instead.}

\item{hardware}{The encoder backend: \code{"none"} (default, the
software \code{video_codec}), \code{"nvenc"} for NVIDIA GPU encoding
(H.264, HEVC and AV1), or \code{"videotoolbox"} for Apple GPU encoding
(H.264 and HEVC). Uses that backend's encoder for \code{video_codec}'s
family (e.g. \code{"libx264"} becomes \code{"h264_nvenc"} or
\code{"h264_videotoolbox"}); with the default \code{video_codec = NULL}
the H.264 family is assumed, so a non-H.264 container (e.g.
\code{.webm}) needs an explicit HEVC- or AV1-family \code{video_codec}
(AV1 only under \code{"nvenc"}). See \code{\link{has_hardware_encoder}}
for availability and its caveats.
Resolving a hardware backend asks this FFmpeg build which encoders it has,
so the first such call that re-encodes the video runs the binary
while the command is built, even under \code{run = FALSE}. The answer is
remembered for the rest of the R session; see
\code{\link{refresh_ffmpeg_capabilities}} to discard it.
Availability is checked at this verb's own front door, before any row
runs, so an unavailable encoder aborts naming this function rather than
the internal fan-out it would otherwise be reported against. A call that
also contradicts itself — asking for GPU encoding on a cut that stream-copies —
is refused for the contradiction first, whether or not this machine has
the encoder.
The stream-copy conflict named under \code{reencode} is caught first, so
such a call aborts without probing.}

\item{fallback}{A logical: when a non-\code{"none"} \code{hardware} is
requested but its encoder is unavailable, encode in software with a
message (\code{TRUE}) instead of aborting (\code{FALSE}, default). With
\code{video_codec = NULL} the fallback leaves the codec unset rather
than picking one, so the codec never changes silently.
A \code{video_codec} in a family that backend has no encoder for is a
wrong argument rather than an absent encoder, so it aborts whatever
\code{fallback} says.}

\item{audio_stream}{The 0-based index of the audio track to carry into the output, counted \emph{among the input's audio streams} -- \code{0} is the first audio track, \code{1} the second, whatever their positions among the file's streams. \code{NULL} (default) carries \strong{every} audio track. The every-track family reads \code{NULL} this way -- \code{\link{separate_audio_video}}, \code{\link{standardize_video}}, \code{\link{anonymize_video}}, \code{\link{crop_video}}, \code{\link{segment_video}} and \code{\link{format_for_web}}, plus their \code{_batch} siblings. The first-track family takes one track only: \code{\link{extract_audio}}, \code{\link{convert_audio}} and \code{\link{normalize_audio}}, plus theirs. Subtitle and data streams are not carried either way. Naming a track the input does not have is an FFmpeg error, not an R one. See \code{\link{audio_stream}} for how this differs from \code{audio_input}, the input index on \code{\link{compare_videos}} and \code{\link{picture_in_picture}}. (default = \code{NULL})}

\item{run}{A logical: run each segment's command (\code{TRUE}, default) or
only compile them (\code{FALSE}).}

\item{parallel}{A logical passed to \code{\link{ffm_batch}}: cut segments in
parallel with \pkg{furrr} (\code{TRUE}) or sequentially (\code{FALSE},
default). Parallelism follows the active \code{\link[future:plan]{future}}
plan; \code{TRUE} under the default sequential plan runs one segment at a
time and warns. Set a plan first, e.g.
\code{future::plan(future::multisession)}.}
}
\value{
The \link[tibble:tibble-package]{tibble} returned by
\code{\link{ffm_batch}}: one row per segment with its \code{command} (and,
when \code{run = TRUE}, \code{success}).
}
\description{
Use FFmpeg to quickly break a single video file into multiple smaller video
files (with the same encoding) based on pairs of start and stop timestamps.
Segment video files will be named by taking the name of \code{infile} and
appending a suffix of an underscore (_) and an integer indicating which
segment (based on the order provided in \code{start} and \code{end}).
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
# Two segments; run = FALSE compiles one command per segment
segment_video(video, start = c(0, 0.5), end = c(0.5, 1), run = FALSE)
}
\references{
https://ffmpeg.org/ffmpeg-utils.html#time-duration-syntax
}
\seealso{
\code{\link[=ffm_seek]{ffm_seek()}}, the builder it uses to cut; \code{\link[=ffm_batch]{ffm_batch()}}, the runner;
\code{\link[=has_hardware_encoder]{has_hardware_encoder()}} for the \code{hardware} toggle;
\code{\link[=segment_video_batch]{segment_video_batch()}} for the many-file form.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task verb functions}
