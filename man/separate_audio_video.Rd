% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{separate_audio_video}
\alias{separate_audio_video}
\title{Split a media file into separate audio and video files}
\usage{
separate_audio_video(
  infile,
  audiofile,
  videofile,
  audio_codec = "copy",
  video_codec = "copy",
  hardware = c("none", "nvenc"),
  fallback = FALSE,
  audio_stream = NULL,
  run = TRUE
)
}
\arguments{
\item{infile}{A string containing the path to a media file.}

\item{audiofile}{A string containing the path of the audio file to write.}

\item{videofile}{A string containing the path of the video file to write.}

\item{audio_codec}{A string naming the encoder for \code{audiofile}, passed
to FFmpeg's \code{-codec:a}. The default \code{"copy"} stream-copies the
audio losslessly; a codec name (e.g. \code{"libmp3lame"}) transcodes it;
\code{NULL} emits no \code{-codec:a}, leaving the encoder to the
\code{audiofile} extension.}

\item{video_codec}{A string naming the encoder for \code{videofile}, passed
to FFmpeg's \code{-codec:v}. The default \code{"copy"} stream-copies the
video losslessly; a codec name (e.g. \code{"libx264"}) transcodes it;
\code{NULL} emits no \code{-codec:v}, leaving the encoder to the
\code{videofile} extension.}

\item{hardware}{The encoder backend for \code{videofile}: \code{"none"}
(default, the software \code{video_codec}) or \code{"nvenc"} for NVIDIA GPU
encoding, which uses the nvenc encoder for \code{video_codec}'s family
(e.g. \code{"libx264"} becomes \code{"h264_nvenc"}), assuming the H.264
family when \code{video_codec = NULL}. Only video is encoded on the GPU, so
this never affects \code{audiofile}. Because this verb's video default is a
stream copy, which runs no encoder at all, \code{hardware = "nvenc"}
alongside \code{video_codec = "copy"} is an error: name an encoder or pass
\code{video_codec = NULL}. See \code{\link{has_nvenc}} for availability and
its caveats.}

\item{fallback}{A logical: when \code{hardware = "nvenc"} but nvenc is
unavailable, encode in software with a message (\code{TRUE}) instead of
aborting (\code{FALSE}, default). With \code{video_codec = NULL} the
fallback leaves the codec unset rather than injecting one.}

\item{audio_stream}{The 0-based index of the audio track to write to \code{audiofile}, counted \emph{among the input's audio streams} -- \code{0} is the first audio track, \code{1} the second, whatever their positions among the file's streams. \code{NULL} (default) keeps \strong{every} audio track. The pass-through family reads \code{NULL} this way -- \code{\link{separate_audio_video}}, \code{\link{standardize_video}}, \code{\link{anonymize_video}}, \code{\link{crop_video}}, \code{\link{segment_video}} and \code{\link{format_for_web}}, plus their \code{_batch} siblings. The extraction family takes the first track only: \code{\link{extract_audio}}, \code{\link{convert_audio}} and \code{\link{normalize_audio}}, plus theirs. A container that holds several audio streams (\code{.mka}, \code{.m4a}) receives them all, while a single-stream container (\code{.aac}, \code{.mp3}, \code{.wav}) makes FFmpeg fail -- name a track to write one of those. \code{videofile} is never affected. Naming a track the input does not have is an FFmpeg error, not an R one. See \code{\link{audio_stream}} for how this differs from \code{audio}, the input index on \code{\link{compare_videos}} and \code{\link{picture_in_picture}}. (default = \code{NULL})}

\item{run}{A logical: run the commands through FFmpeg (\code{TRUE}, default)
or return the compiled commands without running them (\code{FALSE}).}
}
\value{
A named character vector of the two compiled commands
(\code{audio}, \code{video}); invisible when \code{run = TRUE}.
}
\description{
By default each stream is copied, not re-encoded (\code{audio_codec =
"copy"}, \code{video_codec = "copy"}): separation is lossless and fast, but
each output container must support the source codec (e.g. write AAC audio
from an MP4 to \code{.aac} or \code{.m4a}, not \code{.mp3}). Name an encoder
instead (\code{audio_codec = "libmp3lame"}) to transcode that stream, or pass
\code{NULL} to emit no codec option at all and let the output extension pick
the encoder. Each argument governs only its own output file. Where the video
is re-encoded, \code{hardware = "nvenc"} moves that encode onto an NVIDIA
GPU; the audio output is never affected.
}
\section{When the audio output fails}{

Because the default keeps every audio track, writing a multi-track input to a
container that holds only one (\code{.aac}, \code{.mp3}, \code{.wav}) makes
FFmpeg fail. When that happens and no \code{audio_stream} was named, the error
additionally reports how many audio tracks \code{infile} carries and names the
two ways out — \code{audio_stream} to write one track, or a container such as
\code{.mka} or \code{.m4a} to keep them all. FFmpeg's own error and exit status
are still reported beneath it, and remain the authority on why the command
failed: the extra report is attached to \emph{any} failing audio command on a
multi-track input, not only to a container refusal.

Counting the tracks means running FFprobe, so this is \strong{best-effort}: it
is added when FFprobe is available and \code{infile} can be probed, and
omitted silently otherwise, leaving FFmpeg's own error alone. It never runs
under \code{run = FALSE}, never changes the compiled commands, and is skipped
entirely when \code{audio_stream} names a track — with one track mapped, the
track count cannot be what FFmpeg objected to.
}

\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
separate_audio_video(video, "audio.aac", "video.mp4", run = FALSE)
# transcode the audio to MP3 while copying the video through untouched
separate_audio_video(video, "audio.mp3", "video.mp4",
                     audio_codec = "libmp3lame", run = FALSE)
# write only the second audio track (this sample has one, so compile only)
separate_audio_video(video, "audio.aac", "video.mp4",
                     audio_stream = 1, run = FALSE)
}
\seealso{
\code{\link[=ffm_map]{ffm_map()}} and \code{\link[=ffm_codec]{ffm_codec()}}, the builders it wraps;
\code{\link[=has_nvenc]{has_nvenc()}} for the \code{hardware = "nvenc"} toggle;
\code{\link[=extract_audio]{extract_audio()}} to pull out just the audio;
\code{\link[=probe_audio]{probe_audio()}} to list an input's audio tracks.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}
}
\concept{task verb functions}
