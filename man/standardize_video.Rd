% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{standardize_video}
\alias{standardize_video}
\title{Standardize a video to a reproducible format}
\usage{
standardize_video(
  infile,
  outfile,
  width = NULL,
  height = NULL,
  fps = NULL,
  video_codec = "libx264",
  audio_codec = "copy",
  pixel_format = "yuv420p",
  hardware = c("none", "nvenc", "videotoolbox"),
  fallback = FALSE,
  audio_stream = NULL,
  run = TRUE
)
}
\arguments{
\item{infile}{A string containing the path to a video file.}

\item{outfile}{A string containing the path of the video file to write.}

\item{width}{The output width in pixels (a positive number), or \code{NULL}
(default) to leave the width unconstrained.}

\item{height}{The output height in pixels (a positive number), or \code{NULL}
(default) to leave the height unconstrained.}

\item{fps}{The output frame rate (a positive number or FFmpeg framerate
expression such as \code{"30000/1001"}), or \code{NULL} (default) to keep
the input frame rate.}

\item{video_codec}{A string naming the output video codec (default
\code{"libx264"}), or \code{NULL} to emit no \code{-codec:v} and let the
output container's default encoder decide. \code{NULL} is how you opt out
of the H.264 default for a container that does not hold it — for a
\code{.webm} output, pass \code{video_codec = NULL} \emph{and}
\code{audio_codec = NULL}, since the default \code{audio_codec = "copy"}
would otherwise carry a codec WebM cannot hold.}

\item{audio_codec}{A string naming the output audio codec (default
\code{"copy"}, i.e. stream-copy the source audio unchanged). Name a real
encoder (e.g. \code{"aac"}) when the source audio codec cannot be copied
into the output container, or \code{NULL} to emit no \code{-codec:a} and
let the container's default encoder decide.}

\item{pixel_format}{A string naming the output pixel format (default
\code{"yuv420p"}).}

\item{hardware}{The encoder backend: \code{"none"} (default, the
software \code{video_codec}), \code{"nvenc"} for NVIDIA GPU encoding
(H.264, HEVC and AV1), or \code{"videotoolbox"} for Apple GPU encoding
(H.264 and HEVC). Uses that backend's encoder for \code{video_codec}'s
family (e.g. \code{"libx264"} becomes \code{"h264_nvenc"} or
\code{"h264_videotoolbox"}); see \code{\link{has_hardware_encoder}} for
availability and its caveats. Applies to video only: \code{audio_codec}
is never hardware-accelerated.
Resolving a hardware backend asks this FFmpeg build which encoders it has,
so the first such call that re-encodes the video runs the binary
while the command is built, even under \code{run = FALSE}. The answer is
remembered for the rest of the R session; see
\code{\link{refresh_ffmpeg_capabilities}} to discard it.}

\item{fallback}{A logical: when a non-\code{"none"} \code{hardware} is
requested but its encoder is unavailable, re-encode with the software
\code{video_codec} and a message (\code{TRUE}) instead of aborting
(\code{FALSE}, default). Keeps output reproducible by never changing the
codec silently.}

\item{audio_stream}{The 0-based index of the audio track to carry into the output, counted \emph{among the input's audio streams} -- \code{0} is the first audio track, \code{1} the second, whatever their positions among the file's streams. \code{NULL} (default) carries \strong{every} audio track. The every-track family reads \code{NULL} this way -- \code{\link{separate_audio_video}}, \code{\link{standardize_video}}, \code{\link{anonymize_video}}, \code{\link{crop_video}}, \code{\link{segment_video}} and \code{\link{format_for_web}}, plus their \code{_batch} siblings. The first-track family takes one track only: \code{\link{extract_audio}}, \code{\link{convert_audio}} and \code{\link{normalize_audio}}, plus theirs. Subtitle and data streams are not carried either way. Naming a track the input does not have is an FFmpeg error, not an R one. See \code{\link{audio_stream}} for how this differs from \code{audio_input}, the input index on \code{\link{compare_videos}} and \code{\link{picture_in_picture}}. (default = \code{NULL})}

\item{run}{A logical: run the command through FFmpeg (\code{TRUE}, default)
or return the compiled command without running it (\code{FALSE}).}
}
\value{
The compiled FFmpeg command (invisibly when \code{run = TRUE}).
}
\description{
Re-encode a video to a consistent, reproducible format for analysis
pipelines: a single video codec, pixel format, and (optionally) resolution
and frame rate, with \code{+faststart} for smooth playback. Unlike
\code{\link{format_for_web}} (a fixed web-delivery recipe), every part of the
standard is a parameter, so a lab can pin its own house format once and apply
it across a dataset.
}
\details{
The default standard \code{standardize_video(infile, outfile)} re-encodes to
H.264 video (\code{video_codec = "libx264"}) with \code{pixel_format = "yuv420p"}
and \code{-movflags +faststart}, keeping the source resolution and frame
rate. Audio is stream-copied unchanged (\code{-c:a copy}) unless
\code{audio_codec} names an encoder; loudness standardization stays out of
scope (see \code{\link{normalize_audio}}). The same input therefore always
compiles to a byte-identical command.

Resolution follows \code{width}/\code{height}: supplying both forces exact
output dimensions; supplying only one preserves the aspect ratio and rounds
the other to the nearest even number (FFmpeg's \code{-2}); supplying neither
keeps the source resolution but rounds odd dimensions down to the nearest
even value (a \code{yuv420p}/\code{libx264} requirement, and a no-op for
already-even input) so the output always encodes.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
# The documented default standard (H.264 / yuv420p / +faststart)
standardize_video(video, "std.mp4", run = FALSE)
# Pin resolution and frame rate too
standardize_video(video, "std.mp4", width = 1280, height = 720, fps = 30,
                  run = FALSE)
# Carry only the second audio track instead of all of them
standardize_video(video, "std.mp4", audio_stream = 1, run = FALSE)
}
\seealso{
\code{\link[=ffm_scale]{ffm_scale()}}, \code{\link[=ffm_codec]{ffm_codec()}}, and \code{\link[=ffm_pixel_format]{ffm_pixel_format()}}, among the
builders it wraps; \code{\link[=has_hardware_encoder]{has_hardware_encoder()}} for the \code{hardware}
toggle;
\code{\link[=standardize_video_batch]{standardize_video_batch()}} for the many-file form.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}

Other audio selection functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link{audio_stream}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}}
}
\concept{audio selection functions}
\concept{task verb functions}
