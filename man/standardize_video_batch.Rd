% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{standardize_video_batch}
\alias{standardize_video_batch}
\title{Standardize Many Videos From a Jobs Table}
\usage{
standardize_video_batch(
  jobs,
  width = NULL,
  height = NULL,
  fps = NULL,
  video_codec = "libx264",
  pixel_format = "yuv420p",
  run = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{jobs}{A data frame with one row per input and (at least) an
\code{input} column (source path). An optional \code{output} column names
the destination; when absent, one is derived per row by appending
\code{_standardized} to each input's basename, keeping the input's
extension (e.g. \code{clip.mkv} becomes \code{clip_standardized.mkv}).
Because standardization is one-input-to-one-output, a duplicated
\code{input} with no \code{output} column would collide and is rejected.
Each of the five standardization knobs — \code{width}, \code{height},
\code{fps}, \code{video_codec}, \code{pixel_format} — may also appear as a
column to override the corresponding argument on a per-row basis; rows (or
knobs) that omit the column fall back to the argument's value. Any other
columns are ignored.}

\item{width, height}{Optional target dimensions applied to every row, unless
\code{jobs} carries a column of the same name (see \code{jobs}). When only
one is given the other is derived to preserve aspect ratio; when neither is
given the frame is floor-cropped to even dimensions so odd-sized sources
encode. (default = \code{NULL})}

\item{fps}{Optional target frame rate applied to every row, unless
\code{jobs} carries an \code{fps} column. (default = \code{NULL}, i.e.
leave the frame rate unchanged)}

\item{video_codec}{A string naming the video codec applied to every row, unless
\code{jobs} carries a \code{video_codec} column. (default = \code{"libx264"})}

\item{pixel_format}{A string naming the pixel format applied to every row,
unless \code{jobs} carries a \code{pixel_format} column.
(default = \code{"yuv420p"})}

\item{run}{A logical: run each input's command through FFmpeg (\code{TRUE},
default) or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical passed to \code{\link{ffm_batch}}: standardize in
parallel with \pkg{furrr} (\code{TRUE}) or sequentially (\code{FALSE},
default). Parallelism follows the active \code{\link[future:plan]{future}}
plan; \code{TRUE} under the default sequential plan runs one input at a
time and warns. Set a plan first, e.g.
\code{future::plan(future::multisession)}.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}}, such as
\code{verify}, \code{manifest}, \code{checksums}, and \code{progress}.}
}
\value{
The \link[tibble:tibble-package]{tibble} returned by
\code{\link{ffm_batch}}: \code{jobs} with an added \code{command} column
(and, when \code{output} was derived, the resolved \code{output} column;
when \code{run = TRUE}, a \code{success} column, plus any columns the
forwarded arguments add, e.g. \code{verified}).
}
\description{
Re-encode many input files to a reproducible format from a single jobs tibble
— the \strong{batch} (table-driven) sibling of \code{\link[=standardize_video]{standardize_video()}} for when you
have
more than one video to standardize. Each row is one input; the only required
column names its source. This is a thin wrapper over \code{\link{ffm_batch}}:
one reproducible compiled command per input.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(
  input  = c(video, video),
  output = c("a.mp4", "b.mp4"),
  width  = c(640, 320)
)
# run = FALSE compiles one command per input without calling FFmpeg
standardize_video_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=standardize_video]{standardize_video()}} for the single-input form; \code{\link[=ffm_batch]{ffm_batch()}} for
the batch runner and the arguments forwarded through \code{...};
\code{\link[=segment_video_batch]{segment_video_batch()}} and \code{\link[=extract_frame_batch]{extract_frame_batch()}} for the other
table-driven siblings.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=strip_metadata]{strip_metadata()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}
}
\concept{task verb functions}
