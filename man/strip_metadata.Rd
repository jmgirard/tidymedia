% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{strip_metadata}
\alias{strip_metadata}
\title{Strip identifying metadata from a media file}
\usage{
strip_metadata(infile, outfile, run = TRUE)
}
\arguments{
\item{infile}{A string containing the path to a media file.}

\item{outfile}{A string containing the path of the de-identified file to
write. Use the same container extension as \code{infile} so the copied
streams remux cleanly.}

\item{run}{A logical: run the command through FFmpeg (\code{TRUE}, default)
or return the compiled command without running it (\code{FALSE}).}
}
\value{
The compiled FFmpeg command (invisibly when \code{run = TRUE}).
}
\description{
Remove a media file's container and global metadata tags (creation time,
GPS/location, device make and model, title, comment, and the like) together
with any chapters, writing a de-identified copy — the front door for
IRB/de-identification of research recordings. The audio and video streams are
\strong{stream-copied}, not re-encoded, so the operation is lossless and fast and
the picture and sound are bit-for-bit unchanged (including any rotation
display matrix, which is stream side data, not a metadata tag).
}
\details{
The output is muxed bit-exactly (\code{-fflags +bitexact}) so FFmpeg does not
re-stamp the container with a fresh \code{creation_time} or an
\code{encoder} tag naming its own version — either of which would defeat
de-identification and reproducibility.

Because the streams are copied rather than re-encoded, identifiers embedded
\strong{inside} the encoded bitstream, and per-stream metadata such as
\code{handler_name} or \code{language}, are not removed. Removing those would
require re-encoding (out of scope; use the \code{\link{ffmpeg}} escape hatch)
or per-stream metadata mapping that must probe the file first.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
strip_metadata(video, "clean.mp4", run = FALSE)
}
\seealso{
\code{\link[=anonymize_video]{anonymize_video()}} to remove faces or regions from the picture (the
visual de-identification sibling); \code{\link[=probe_container]{probe_container()}} and
\code{\link[=mediainfo_query]{mediainfo_query()}} to inspect a file's metadata before and after;
\code{\link[=ffm_copy]{ffm_copy()}} and \code{\link[=ffm_output_options]{ffm_output_options()}}, the builders it wraps;
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}} for the many-file form.

Other task verb functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata_batch]{strip_metadata_batch()}}
}
\concept{task verb functions}
