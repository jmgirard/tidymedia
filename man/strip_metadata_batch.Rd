% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffmpeg.R
\name{strip_metadata_batch}
\alias{strip_metadata_batch}
\title{Strip Metadata From Many Files From a Jobs Table}
\usage{
strip_metadata_batch(jobs, run = TRUE, parallel = FALSE, ...)
}
\arguments{
\item{jobs}{A data frame with one row per input. It needs at least an
\code{input} column, the source path. An optional \code{output} column
names the destination. Without it, each row's output name adds
\code{_stripped} to the input's base name and keeps its extension. For
example, \code{clip.mkv} becomes \code{clip_stripped.mkv}. Any two rows
with the \strong{same} output path are rejected, so one file cannot silently
overwrite another. That happens with a duplicated \code{input} and no
\code{output} column, or with a repeated explicit \code{output}. Any other
columns are ignored, because the scrub has no per-row settings.}

\item{run}{A logical: run each input's command through FFmpeg (\code{TRUE},
default) or only compile them for inspection (\code{FALSE}).}

\item{parallel}{A logical passed to \code{\link{ffm_batch}}: scrub in
parallel with \pkg{furrr} (\code{TRUE}) or sequentially (\code{FALSE},
default). Parallelism follows the active \code{\link[future:plan]{future}}
plan; \code{TRUE} under the default sequential plan runs one input at a
time and warns. Set a plan first, e.g.
\code{future::plan(future::multisession)}.}

\item{...}{Additional arguments forwarded to \code{\link{ffm_batch}}, such as
\code{verify}, \code{manifest}, \code{checksums}, and \code{progress}.}
}
\value{
The \link[tibble:tibble-package]{tibble} returned by \code{\link{ffm_batch}}: \code{jobs} with an added \code{command} column. When \code{output} was derived, it also has the resolved \code{output} column. When \code{run = TRUE}, it has a \code{success} column, plus any columns the forwarded arguments add, such as \code{verified}.
}
\description{
De-identify many files, using one jobs table. This is the \strong{batch} form of
\code{\link[=strip_metadata]{strip_metadata()}}, for when you have more than one file to scrub. Each row
is one input, and the only required column names its source. The function
is a thin wrapper over \code{\link{ffm_batch}}. It builds one reproducible
stream-copy strip command for each input. Each command uses the same steps
as \code{strip_metadata()}, so it is bit-exact and drops the same metadata.
The glossary in \code{vignette("tidymedia")} explains media terms such as
stream and stream copy.
}
\examples{
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
jobs <- tibble::tibble(input = video, output = "clean.mp4")
# run = FALSE compiles one command per input without calling FFmpeg
strip_metadata_batch(jobs, run = FALSE)
}
\seealso{
\code{\link[=strip_metadata]{strip_metadata()}} for the single-input form; \code{\link[=ffm_batch]{ffm_batch()}} for the
batch runner and the arguments forwarded through \code{...};
\code{\link[=standardize_video_batch]{standardize_video_batch()}} and \code{\link[=anonymize_video_batch]{anonymize_video_batch()}} for other batch
task functions.

Other task functions:
\code{\link[=anonymize_video]{anonymize_video()}},
\code{\link[=anonymize_video_batch]{anonymize_video_batch()}},
\code{\link[=compare_videos]{compare_videos()}},
\code{\link[=compare_videos_batch]{compare_videos_batch()}},
\code{\link[=concatenate_videos]{concatenate_videos()}},
\code{\link[=concatenate_videos_batch]{concatenate_videos_batch()}},
\code{\link[=convert_audio]{convert_audio()}},
\code{\link[=convert_audio_batch]{convert_audio_batch()}},
\code{\link[=crop_video]{crop_video()}},
\code{\link[=crop_video_batch]{crop_video_batch()}},
\code{\link[=extract_audio]{extract_audio()}},
\code{\link[=extract_audio_batch]{extract_audio_batch()}},
\code{\link[=extract_frame]{extract_frame()}},
\code{\link[=extract_frame_batch]{extract_frame_batch()}},
\code{\link[=format_for_web]{format_for_web()}},
\code{\link[=format_for_web_batch]{format_for_web_batch()}},
\code{\link[=normalize_audio]{normalize_audio()}},
\code{\link[=normalize_audio_batch]{normalize_audio_batch()}},
\code{\link[=picture_in_picture]{picture_in_picture()}},
\code{\link[=picture_in_picture_batch]{picture_in_picture_batch()}},
\code{\link[=sample_frames]{sample_frames()}},
\code{\link[=sample_frames_batch]{sample_frames_batch()}},
\code{\link[=segment_video]{segment_video()}},
\code{\link[=segment_video_batch]{segment_video_batch()}},
\code{\link[=separate_audio_video]{separate_audio_video()}},
\code{\link[=separate_audio_video_batch]{separate_audio_video_batch()}},
\code{\link[=standardize_video]{standardize_video()}},
\code{\link[=standardize_video_batch]{standardize_video_batch()}},
\code{\link[=strip_metadata]{strip_metadata()}}
}
\concept{task functions}
