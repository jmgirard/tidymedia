% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{verify_media}
\alias{verify_media}
\title{Verify a Media File Against Expected Properties}
\usage{
verify_media(
  file,
  duration = NULL,
  width = NULL,
  height = NULL,
  video_codec = NULL,
  audio_codec = NULL,
  sample_rate = NULL,
  ...,
  tolerance = 0.1
)
}
\arguments{
\item{file}{A string naming a single media file to verify.}

\item{duration}{Expected container duration in seconds (numeric).}

\item{width, height}{Expected video-frame dimensions in pixels (numeric).}

\item{video_codec, audio_codec}{Expected codec names for the first video and
audio stream (strings, e.g. \code{"h264"}, \code{"aac"}).}

\item{sample_rate}{Expected audio sample rate in Hz (numeric).}

\item{...}{Further expectations given as \code{name = value}, each checked against
the FFprobe field of that name (container first, then video, then audio).}

\item{tolerance}{The absolute tolerance for numeric checks (default \code{0.1}).}
}
\value{
A \link[tibble:tibble-package]{tibble} with one row per checked property
and columns \code{file}, \code{check}, \code{expected}, \code{actual}, and \code{pass} (logical).
}
\description{
Probe a media file and check its structural metadata against a set of
expectations, returning a tidy pass/fail tibble with one row per checked
property. This turns "the command is reproducible" into "the \emph{result} is what
I asked for": use it after an encode to confirm the output really has the
duration, dimensions, codecs, and so on that the pipeline was meant to
produce.
}
\details{
Checks are structural (drawn from FFprobe metadata), not perceptual: this
does not measure visual or audio quality. The named arguments cover the most
common properties; pass any other FFprobe field by name through \code{...} (for
example \code{pix_fmt = "yuv420p"} or \code{bit_rate = 141800}). Extra names are
resolved against the probe columns in the order container, then video stream,
then audio stream, and the first match wins.

Numeric checks pass when \code{abs(actual - expected) <= tolerance}; with the
default \code{tolerance} of \code{0.1} this means integer properties (width, height,
sample rate) must match exactly while \code{duration} is allowed a little slack
(e.g. for keyframe-snapped cuts). String checks (the codecs) must match
exactly. A property whose stream or column is absent yields an \code{NA} actual
value and a failing check.
}
\examples{
\dontshow{if (nzchar(Sys.which("ffprobe"))) withAutoprint(\{ # examplesIf}
video <- system.file("extdata", "sample.mp4", package = "tidymedia")
verify_media(video, width = 320, height = 240, video_codec = "h264")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ffm_run]{ffm_run()}} and \code{\link[=ffm_batch]{ffm_batch()}}, which accept a \verb{verify =} spec.
}
\concept{verification functions}
